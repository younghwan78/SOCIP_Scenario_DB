---
plan: "07-02"
phase: 7
wave: 1
depends_on: []
title: "runner 확장 + ORM 변환 레이어 — SimRunResult.ip_power, loaders.py"
objective: >
  SimRunResult에 ip_power 필드를 추가하고, runner.py Step 5 루프에서 IP별 active power를 수집한다.
  신규 db/loaders.py에 load_runner_inputs_from_db() 함수를 구현하여
  ORM row → run_simulation() 입력 Pydantic 변환을 담당한다.
files_modified:
  - src/scenario_db/sim/models.py
  - src/scenario_db/sim/runner.py
  - src/scenario_db/db/loaders.py
  - tests/unit/test_loaders.py
requirements:
  - SAPI-01
  - SAPI-04
autonomous: true

must_haves:
  truths:
    - "SimRunResult.ip_power: dict[str, float] 필드가 존재하고 기본값 {} 이다"
    - "runner.py Step 5 루프에서 ip_power[ip_name] = power_mw 가 수집된다"
    - "load_runner_inputs_from_db() 가 (pipeline, ip_catalog, dvfs_tables, variant_port_config, sim_config, sensor_spec) 튜플을 반환한다"
    - "scenario/variant 없음 시 load_runner_inputs_from_db()가 None을 반환한다"
    - "기존 41개 sim 테스트가 회귀 없이 통과한다"
  artifacts:
    - path: "src/scenario_db/sim/models.py"
      provides: "SimRunResult.ip_power 필드 추가"
      contains: "ip_power"
    - path: "src/scenario_db/sim/runner.py"
      provides: "Step 5에 ip_power 수집 로직"
      contains: "ip_power"
    - path: "src/scenario_db/db/loaders.py"
      provides: "load_runner_inputs_from_db() 신규 함수"
      exports:
        - load_runner_inputs_from_db
        - compute_params_hash
    - path: "tests/unit/test_loaders.py"
      provides: "loaders 단위 테스트"
  key_links:
    - from: "runner.py Step 5 루프"
      to: "SimRunResult.ip_power"
      via: "ip_power[ip_name] = power_mw"
      pattern: "ip_power\\[ip_name\\]"
    - from: "load_runner_inputs_from_db()"
      to: "Scenario/ScenarioVariant/IpCatalog ORM"
      via: "db.query().filter_by() + model_validate()"
      pattern: "model_validate"
---

<objective>
Wave 1b: runner 확장 + ORM 변환 레이어 구현. 07-01-PLAN과 병렬 실행 가능 (file 충돌 없음).

목적:
  1. SimRunResult에 ip_power 필드 추가 (D-06) — PowerAnalysisResponse.per_ip 제공용
  2. runner.py Step 5 루프에 ip_power 수집 코드 추가 (backward-compatible)
  3. load_runner_inputs_from_db() — DB ORM → run_simulation() Pydantic 입력 변환 (D-07)
  4. compute_params_hash() — SHA256 계산 헬퍼 (D-02)
  5. 단위 테스트로 변환 로직 검증

출력 아티팩트:
  - src/scenario_db/sim/models.py (ip_power 필드 추가)
  - src/scenario_db/sim/runner.py (Step 5 ip_power 수집)
  - src/scenario_db/db/loaders.py (신규)
  - tests/unit/test_loaders.py (신규)
</objective>

<execution_context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\07-simulation-api\07-CONTEXT.md
</execution_context>

<context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\ROADMAP.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\STATE.md

<interfaces>
<!-- Wave 1b에서 읽고 수정할 코드 계약 -->

From src/scenario_db/sim/models.py (현재 SimRunResult 끝부분):
```python
class SimRunResult(BaseScenarioModel):
    scenario_id: str
    variant_id: str
    total_power_mw: float
    total_power_ma: float
    bw_total_mbs: float
    hw_time_max_ms: float
    feasible: bool
    infeasible_reason: str | None = None
    resolved: dict[str, ResolvedIPConfig] = Field(default_factory=dict)
    dma_breakdown: list[PortBWResult] = Field(default_factory=list)
    timing_breakdown: list[IPTimingResult] = Field(default_factory=list)
    vdd_power: dict[str, float] = Field(default_factory=dict)
    # ip_power 필드 없음 — 이 Plan에서 추가
```

From src/scenario_db/sim/runner.py (Step 5 루프 핵심 부분):
```python
    # Step 5: Active Power 계산 + VDD 집계
    vdd_power: dict[str, float] = {}
    total_power_mw: float = 0.0

    for node in pipeline.nodes:
        node_id = node.id
        if node_id not in resolved:
            continue
        params = ip_params.get(node_id)
        if params is None:
            continue
        r = resolved[node_id]
        # ... width/height 계산 ...
        ip_name = _resolve_ip_name(node.ip_ref, ip_catalog)
        power_mw = calc_active_power(...)
        total_power_mw += power_mw
        vdd_power[r.vdd] = vdd_power.get(r.vdd, 0.0) + power_mw

    # ... return SimRunResult(..., vdd_power=vdd_power) — ip_power 없음
```

From src/scenario_db/db/models/definition.py:
```python
class Scenario(Base):
    __tablename__ = "scenarios"
    id             = Column(Text, primary_key=True)
    pipeline       = Column(JSONB, nullable=False)
    sensor         = Column(JSONB)  # nullable

class ScenarioVariant(Base):
    __tablename__ = "scenario_variants"
    scenario_id    = Column(Text, ForeignKey("scenarios.id"), primary_key=True)
    id             = Column(Text, primary_key=True)
    sim_port_config = Column(JSONB)
    sim_config      = Column(JSONB)
```

From src/scenario_db/db/models/capability.py (IpCatalog ORM):
```python
class IpCatalogEntry(Base):
    __tablename__ = "ip_catalog"
    id        = Column(Text, primary_key=True)
    sim_params = Column(JSONB)  # nullable
    metadata_ = Column("metadata", JSONB, nullable=False)
```

From src/scenario_db/models/definition/usecase.py:
```python
class Pipeline(BaseScenarioModel):
    nodes: list[PipelineNode] = Field(default_factory=list)
    edges: list[PipelineEdge] = Field(default_factory=list)
    sw_stack: list[SwStackNode] = Field(default_factory=list)

class IPPortConfig(BaseScenarioModel):
    mode: str = "Normal"
    inputs: list[PortInputConfig] = Field(default_factory=list)
    outputs: list[PortInputConfig] = Field(default_factory=list)

class SimGlobalConfig(BaseScenarioModel):
    asv_group: int = 4
    sw_margin: float = 0.25
    bw_power_coeff: float = 80.0
    vbat: float = 4.0
    pmic_eff: float = 0.85
    h_blank_margin: float = 0.05
    dvfs_overrides: dict[str, int] = Field(default_factory=dict)

class SensorSpec(BaseScenarioModel):
    ip_ref: DocumentId
    frame_width: int; frame_height: int
    fps: float; v_valid_ratio: float = 0.85
```

From src/scenario_db/models/capability/hw.py:
```python
class IpCatalog(BaseScenarioModel):
    id: DocumentId; schema_version: SchemaVersion; kind: Literal["ip"]
    category: str; hierarchy: IpHierarchy; capabilities: IpCapabilities
    rtl_version: str | None = None
    compatible_soc: list[DocumentId] = Field(default_factory=list)
    sim_params: IPSimParams | None = None
```

From src/scenario_db/sim/dvfs_resolver.py (DVFS 로드 패턴):
```python
class DvfsResolver:
    def __init__(self, dvfs_tables: dict[str, DVFSTable], asv_group: int = 4):
        ...
    # 내부적으로 DVFS_CONFIG_PATH YAML을 로드하는 별도 함수 또는 메서드 존재
```

From src/scenario_db/config.py (DVFS_CONFIG_PATH):
```python
DVFS_CONFIG_PATH: Path  # 환경변수 불필요, 모듈 수준 상수
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SimRunResult.ip_power 필드 추가 + runner.py Step 5 수정</name>
  <files>
    src/scenario_db/sim/models.py
    src/scenario_db/sim/runner.py
  </files>

  <read_first>
    - src/scenario_db/sim/models.py — SimRunResult 현재 전체 필드 목록 (정확한 수정 위치 파악)
    - src/scenario_db/sim/runner.py — Step 5 루프 전체 (ip_name 변수가 어디서 정의되는지, vdd_power 수집 패턴)
    - tests/sim/ 디렉터리 — 기존 테스트 목록 확인 (회귀 방지용)
  </read_first>

  <action>
**A. `src/scenario_db/sim/models.py` 수정 — SimRunResult 끝에 ip_power 필드 추가:**

`vdd_power: dict[str, float] = Field(default_factory=dict)` 줄 바로 아래에 추가:
```python
    ip_power: dict[str, float] = Field(default_factory=dict)   # Phase 7 추가 (D-06) — ip_name -> active_power_mw
```

**B. `src/scenario_db/sim/runner.py` 수정 — Step 5 루프에 ip_power 수집 추가:**

Step 5 루프 상단에 `vdd_power: dict[str, float] = {}` 선언 바로 아래에:
```python
    ip_power: dict[str, float] = {}          # Phase 7 추가 (D-06)
```

루프 내부 `vdd_power[r.vdd] = vdd_power.get(r.vdd, 0.0) + power_mw` 줄 바로 아래에:
```python
        ip_name = _resolve_ip_name(node.ip_ref, ip_catalog)
        ip_power[ip_name] = ip_power.get(ip_name, 0.0) + power_mw  # D-06
```

단, ip_name이 이미 Step 5 루프 내에 정의되어 있는지 확인하고 없으면 위 라인을 추가.
실제 runner.py를 읽어 ip_name이 Step 5 루프 내에 없음을 확인한 후:
`ip_name = _resolve_ip_name(node.ip_ref, ip_catalog)` 라인을 power_mw 계산 직후에 추가.

두 개의 `return SimRunResult(...)` 호출 모두에 `ip_power=ip_power` 인수 추가:
- 빈 timing_breakdown early return 케이스 (기본값 ip_power={} 사용하도록)
- 정상 return 케이스
  </action>

  <verify>
    <automated>
      uv run pytest tests/sim/ -v -q
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "ip_power" src/scenario_db/sim/models.py` → 결과 있음
    - `grep "default_factory=dict" src/scenario_db/sim/models.py` → ip_power 라인 포함
    - `grep "ip_power" src/scenario_db/sim/runner.py` → 최소 2줄 (선언 + 수집)
    - `uv run pytest tests/sim/ -v -q` → 41개 테스트 전체 pass (0 failures, 0 errors)
    - SimRunResult 인스턴스 생성 시 ip_power 필드가 빈 dict로 초기화됨 (기존 테스트 통과)
  </acceptance_criteria>

  <done>
    SimRunResult에 ip_power 필드가 추가되고, runner.py가 IP별 active power를 수집한다.
    기존 41개 sim 테스트 전체 회귀 없음.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: db/loaders.py 신규 구현 + 단위 테스트</name>
  <files>
    src/scenario_db/db/loaders.py
    tests/unit/test_loaders.py
  </files>

  <read_first>
    - src/scenario_db/db/models/definition.py — Scenario, ScenarioVariant ORM 컬럼 전체
    - src/scenario_db/db/models/capability.py — IpCatalog ORM (ip_catalog 테이블 컬럼명)
    - src/scenario_db/db/repositories/scenario_graph.py — ORM→Pydantic 변환 패턴 (model_validate + from_attributes 패턴)
    - src/scenario_db/sim/dvfs_resolver.py — DVFS_CONFIG_PATH 로드 구현 확인 (어떤 함수/메서드로 YAML 로드하는지)
    - src/scenario_db/config.py — DVFS_CONFIG_PATH 상수 위치
    - src/scenario_db/models/definition/usecase.py — Pipeline, IPPortConfig, SimGlobalConfig, SensorSpec 시그니처
    - src/scenario_db/models/capability/hw.py — IpCatalog(Pydantic) 시그니처
  </read_first>

  <behavior>
    - load_runner_inputs_from_db(db, scenario_id, variant_id) → tuple[Pipeline, dict, dict, dict, SimGlobalConfig, SensorSpec|None] | None
      - scenario 없음 → None 반환
      - variant 없음 → None 반환
      - pipeline = Pipeline.model_validate(scenario.pipeline)
      - sensor_spec = SensorSpec.model_validate(scenario.sensor) if scenario.sensor else None
      - ip_refs = {node.ip_ref for node in pipeline.nodes}
      - ip_catalog_rows = db.query(IpCatalogEntry).filter(IpCatalogEntry.id.in_(ip_refs)).all()
      - ip_catalog = {row.id: IpCatalog.model_validate(row, from_attributes=True) for row in ip_catalog_rows}
      - dvfs_tables = _load_dvfs_tables() (DVFS YAML 로드)
      - variant_port_config = {k: IPPortConfig.model_validate(v) for k,v in variant.sim_port_config.items()} if variant.sim_port_config else {}
      - sim_config = SimGlobalConfig.model_validate(variant.sim_config) if variant.sim_config else SimGlobalConfig()
    - compute_params_hash(req: SimulateRequest) → str (64자 hex SHA256)
      - payload = json.dumps({scenario_id, variant_id, fps, dvfs_overrides, asv_group}, sort_keys=True)
      - return hashlib.sha256(payload.encode()).hexdigest()
    - apply_request_overrides(sim_config, req) → SimGlobalConfig
      - req.dvfs_overrides가 있으면 sim_config.dvfs_overrides를 req.dvfs_overrides로 교체
      - req.asv_group으로 sim_config.asv_group 교체
      - 나머지 필드(sw_margin, bw_power_coeff 등)는 DB 값 유지
  </behavior>

  <action>
`src/scenario_db/db/loaders.py` 신규 파일 작성:

```python
"""ORM → run_simulation() 입력 Pydantic 변환 레이어 (D-07).

DB/ORM 의존: Scenario, ScenarioVariant, IpCatalogEntry ORM row
출력: run_simulation() 인수로 직접 전달 가능한 Pydantic 객체
"""
from __future__ import annotations

import hashlib
import json
import logging
from typing import TYPE_CHECKING

import yaml
from sqlalchemy.orm import Session

from scenario_db.config import DVFS_CONFIG_PATH
from scenario_db.db.models.capability import IpCatalogEntry
from scenario_db.db.models.definition import Scenario, ScenarioVariant
from scenario_db.models.capability.hw import IpCatalog
from scenario_db.models.definition.usecase import (
    IPPortConfig,
    Pipeline,
    SensorSpec,
    SimGlobalConfig,
)
from scenario_db.sim.models import DVFSLevel, DVFSTable

if TYPE_CHECKING:
    from scenario_db.api.schemas.simulation import SimulateRequest

logger = logging.getLogger(__name__)


def _load_dvfs_tables() -> dict[str, DVFSTable]:
    """DVFS_CONFIG_PATH YAML → {domain: DVFSTable} 딕셔너리 로드.

    Phase 6 DvfsResolver 내부 로직과 동일한 YAML 구조 파싱.
    """
    with open(DVFS_CONFIG_PATH, encoding="utf-8") as f:
        raw = yaml.safe_load(f)

    tables: dict[str, DVFSTable] = {}
    for domain_entry in raw.get("dvfs_domains", []):
        domain = domain_entry["domain"]
        levels = [
            DVFSLevel(
                level=lv["level"],
                speed_mhz=lv["speed_mhz"],
                voltages={int(k): v for k, v in lv["voltages"].items()},
            )
            for lv in domain_entry.get("levels", [])
        ]
        tables[domain] = DVFSTable(domain=domain, levels=levels)
    return tables


def compute_params_hash(req: "SimulateRequest") -> str:
    """SimulateRequest → SHA256 hex digest (D-02).

    sort_keys=True로 dvfs_overrides 딕셔너리 순서 독립성 보장.
    """
    payload = json.dumps(
        {
            "scenario_id": req.scenario_id,
            "variant_id": req.variant_id,
            "fps": req.fps,
            "dvfs_overrides": req.dvfs_overrides,
            "asv_group": req.asv_group,
        },
        sort_keys=True,
    )
    return hashlib.sha256(payload.encode()).hexdigest()


def apply_request_overrides(
    sim_config: SimGlobalConfig,
    req: "SimulateRequest",
) -> SimGlobalConfig:
    """request의 dvfs_overrides, asv_group을 DB sim_config에 오버라이드 (D-03).

    나머지 필드(sw_margin, bw_power_coeff, vbat, pmic_eff, h_blank_margin)는 DB 값 유지.
    """
    overridden = sim_config.model_dump()
    overridden["asv_group"] = req.asv_group
    if req.dvfs_overrides is not None:
        overridden["dvfs_overrides"] = req.dvfs_overrides
    return SimGlobalConfig.model_validate(overridden)


def load_runner_inputs_from_db(
    db: Session,
    scenario_id: str,
    variant_id: str,
) -> tuple[Pipeline, dict[str, IpCatalog], dict[str, DVFSTable], dict[str, IPPortConfig], SimGlobalConfig, SensorSpec | None] | None:
    """DB ORM row → run_simulation() 인수 변환 (D-07).

    Returns:
        (pipeline, ip_catalog, dvfs_tables, variant_port_config, sim_config, sensor_spec)
        또는 scenario/variant 없음 시 None
    """
    scenario = db.query(Scenario).filter_by(id=scenario_id).one_or_none()
    if scenario is None:
        logger.warning("scenario '%s' not found in DB", scenario_id)
        return None

    variant = (
        db.query(ScenarioVariant)
        .filter_by(scenario_id=scenario_id, id=variant_id)
        .one_or_none()
    )
    if variant is None:
        logger.warning("variant '%s' not found for scenario '%s'", variant_id, scenario_id)
        return None

    # Pipeline 변환
    pipeline = Pipeline.model_validate(scenario.pipeline)

    # Sensor spec 변환
    sensor_spec: SensorSpec | None = None
    if scenario.sensor is not None:
        sensor_spec = SensorSpec.model_validate(scenario.sensor)

    # ip_catalog 조회 — pipeline 내 ip_ref 기준
    ip_refs = {node.ip_ref for node in pipeline.nodes}
    catalog_rows = (
        db.query(IpCatalogEntry)
        .filter(IpCatalogEntry.id.in_(ip_refs))
        .all()
    )
    ip_catalog: dict[str, IpCatalog] = {
        row.id: IpCatalog.model_validate(row, from_attributes=True)
        for row in catalog_rows
    }

    # DVFS 테이블 로드
    dvfs_tables = _load_dvfs_tables()

    # Variant port config 변환
    variant_port_config: dict[str, IPPortConfig] = {}
    if variant.sim_port_config:
        for node_id, cfg in variant.sim_port_config.items():
            variant_port_config[node_id] = IPPortConfig.model_validate(cfg)

    # SimGlobalConfig 변환
    sim_config = (
        SimGlobalConfig.model_validate(variant.sim_config)
        if variant.sim_config
        else SimGlobalConfig()
    )

    return pipeline, ip_catalog, dvfs_tables, variant_port_config, sim_config, sensor_spec
```

`tests/unit/test_loaders.py` 작성:
- compute_params_hash() 단위 테스트:
  - 동일 입력 → 동일 해시 (결정론적)
  - dvfs_overrides 딕셔너리 키 순서 다를 때 동일 해시 (sort_keys=True 검증)
  - 다른 scenario_id → 다른 해시
  - 반환값 길이 64 (SHA256 hex)
- apply_request_overrides() 단위 테스트:
  - asv_group이 req 값으로 교체됨
  - dvfs_overrides=None이면 기존 값 유지
  - dvfs_overrides={"cam": 3}이면 교체됨
  - sw_margin 등 기타 필드는 원래 값 유지
- load_runner_inputs_from_db()는 실제 DB 불필요 테스트를 작성:
  - MagicMock으로 db.query().filter_by().one_or_none()를 모킹
  - scenario=None 케이스 → None 반환
  - variant=None 케이스 → None 반환
  </action>

  <verify>
    <automated>
      uv run pytest tests/unit/test_loaders.py -v
    </automated>
  </verify>

  <acceptance_criteria>
    - `src/scenario_db/db/loaders.py` 파일 존재
    - `grep "def load_runner_inputs_from_db" src/scenario_db/db/loaders.py` → 결과 있음
    - `grep "def compute_params_hash" src/scenario_db/db/loaders.py` → 결과 있음
    - `grep "def apply_request_overrides" src/scenario_db/db/loaders.py` → 결과 있음
    - `grep "sort_keys=True" src/scenario_db/db/loaders.py` → 결과 있음 (결정론적 해시)
    - `grep "_load_dvfs_tables" src/scenario_db/db/loaders.py` → 결과 있음
    - `uv run pytest tests/unit/test_loaders.py -v` → 전체 pass
    - compute_params_hash 테스트에서 64자 hex 검증 assert 존재
    - apply_request_overrides 테스트에서 asv_group 교체 검증 assert 존재
    - load_runner_inputs_from_db None 케이스 테스트 2개 이상 존재
  </acceptance_criteria>

  <done>
    load_runner_inputs_from_db(), compute_params_hash(), apply_request_overrides()가
    구현되고 단위 테스트가 통과한다.
    기존 505개 테스트 회귀 없음.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loaders.py → DB | scenario_id, variant_id로 ORM 조회 — SQL injection 불가 (SQLAlchemy parameterized) |
| DVFS_CONFIG_PATH YAML | 파일시스템 접근 — 서버 시작 시 1회 로드, 런타임 조작 불가 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Spoofing | load_runner_inputs_from_db scenario_id | mitigate | db.query().filter_by(id=scenario_id) — SQLAlchemy parameterized query, SQL injection 차단 |
| T-07-05 | Tampering | compute_params_hash | accept | SHA256은 단방향 — 해시 충돌 가능성 무시할 수준 (2^-128) |
| T-07-06 | Information Disclosure | DVFS_CONFIG_PATH YAML | accept | HW 설정 파일이지만 외부 노출 경로 없음 — 서버 내부 파일시스템 접근만 |
</threat_model>

<verification>
## Wave 1b 전체 검증

```bash
# 1. sim 테스트 — ip_power 추가 후 회귀 없음
uv run pytest tests/sim/ -v -q

# 2. loaders 단위 테스트
uv run pytest tests/unit/test_loaders.py -v

# 3. 임포트 검사
uv run python -c "
from scenario_db.sim.models import SimRunResult
r = SimRunResult(scenario_id='s', variant_id='v', total_power_mw=100.0,
    total_power_ma=50.0, bw_total_mbs=5000.0, hw_time_max_ms=30.0, feasible=True)
assert r.ip_power == {}, f'ip_power default not empty dict: {r.ip_power}'
print('SimRunResult.ip_power ok')
"

# 4. compute_params_hash 결정론적 검증
uv run python -c "
from scenario_db.db.loaders import compute_params_hash
from scenario_db.api.schemas.simulation import SimulateRequest
req = SimulateRequest(scenario_id='s1', variant_id='v1', fps=30.0)
h1 = compute_params_hash(req)
h2 = compute_params_hash(req)
assert h1 == h2 and len(h1) == 64, f'hash not deterministic or wrong length: {h1}'
print('compute_params_hash ok', h1[:16])
"

# 5. 전체 단위 테스트
uv run pytest tests/unit/ -q
```
</verification>

<success_criteria>
- src/scenario_db/sim/models.py: ip_power: dict[str, float] = Field(default_factory=dict) 존재
- src/scenario_db/sim/runner.py: ip_power 선언 + ip_power[ip_name] = power_mw + SimRunResult에 ip_power= 인수
- src/scenario_db/db/loaders.py: load_runner_inputs_from_db, compute_params_hash, apply_request_overrides 3함수 존재
- tests/unit/test_loaders.py: compute_params_hash, apply_request_overrides, load_runner_inputs_from_db None 케이스 테스트
- 기존 41개 sim 테스트 전체 pass
- 전체 505개 테스트 회귀 없음
</success_criteria>

<output>
완료 후 `.planning/phases/07-simulation-api/07-02-SUMMARY.md` 생성.
</output>
