---
plan: "07-03"
phase: 7
wave: 2
depends_on:
  - "07-01"
  - "07-02"
title: "FastAPI 라우터 + 통합 테스트 — /simulation/ 5개 엔드포인트 + 전체 테스트 검증"
objective: >
  07-01(스키마+repository)과 07-02(runner 확장+loaders)를 조합하여
  /api/v1/simulation/ 라우터 5개 엔드포인트를 구현하고, app.py에 등록한다.
  TestClient 기반 통합 테스트로 캐싱 포함 전체 플로우를 검증하고 505+ 테스트 회귀 없음을 확인한다.
files_modified:
  - src/scenario_db/api/routers/simulation.py
  - src/scenario_db/api/app.py
  - tests/integration/test_simulation_api.py
requirements:
  - SAPI-01
  - SAPI-02
  - SAPI-03
  - SAPI-04
  - SAPI-05
  - SAPI-06
autonomous: true

must_haves:
  truths:
    - "POST /api/v1/simulation/run 이 SimulateRequest를 받아 동기 계산 후 SimulateResponse를 반환한다"
    - "GET /api/v1/simulation/results/{evidence_id} 가 dma_breakdown + timing_breakdown 포함 Evidence JSON을 반환한다"
    - "GET /api/v1/simulation/bw-analysis?evidence_id=xxx 가 bw_mbs 내림차순 PortBWResult 목록을 반환한다"
    - "GET /api/v1/simulation/power-analysis?evidence_id=xxx 가 total_power/per_ip/per_vdd/bw_power를 반환한다"
    - "GET /api/v1/simulation/timing-analysis?evidence_id=xxx 가 critical_ip/hw_time_max_ms/per_ip/feasible을 반환한다"
    - "동일 params_hash로 두 번 POST /run 하면 두 번째 응답에서 cached=True를 반환한다"
    - "존재하지 않는 evidence_id 조회 시 404를 반환한다"
  artifacts:
    - path: "src/scenario_db/api/routers/simulation.py"
      provides: "5개 엔드포인트 라우터"
      exports:
        - router
    - path: "src/scenario_db/api/app.py"
      provides: "simulation router 등록"
      contains: "simulation_router"
    - path: "tests/integration/test_simulation_api.py"
      provides: "TestClient 통합 테스트"
  key_links:
    - from: "POST /simulation/run"
      to: "find_by_params_hash() → cache HIT"
      via: "params_hash 계산 후 DB 조회"
      pattern: "find_by_params_hash"
    - from: "POST /simulation/run"
      to: "load_runner_inputs_from_db() → run_simulation()"
      via: "scenario/variant ORM 로드 후 runner 호출"
      pattern: "load_runner_inputs_from_db"
    - from: "GET /bw-analysis"
      to: "Evidence.dma_breakdown JSONB"
      via: "get_evidence() → dma_breakdown → PortBWResult 파싱 → 정렬"
      pattern: "sorted.*bw_mbs.*reverse"
    - from: "app.py"
      to: "simulation.router"
      via: "include_router(simulation_router.router, prefix='/api/v1')"
      pattern: "include_router.*simulation"
---

<objective>
Wave 2: FastAPI 라우터 구현 + 통합 테스트. Wave 1 (07-01 + 07-02) 완료 후 실행.

목적:
  1. api/routers/simulation.py — 5개 엔드포인트 구현
     - POST /simulation/run (캐싱 포함 전체 플로우)
     - GET  /simulation/results/{evidence_id}
     - GET  /simulation/bw-analysis?evidence_id=xxx
     - GET  /simulation/power-analysis?evidence_id=xxx
     - GET  /simulation/timing-analysis?evidence_id=xxx
  2. app.py — simulation router 등록
  3. tests/integration/test_simulation_api.py — TestClient 통합 테스트 (캐싱 포함)
  4. 전체 테스트 (505 + 신규) 회귀 없음 확인

출력 아티팩트:
  - src/scenario_db/api/routers/simulation.py (신규)
  - src/scenario_db/api/app.py (수정)
  - tests/integration/test_simulation_api.py (신규)
</objective>

<execution_context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\07-simulation-api\07-CONTEXT.md
</execution_context>

<context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\ROADMAP.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\STATE.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\07-simulation-api\07-01-SUMMARY.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\07-simulation-api\07-02-SUMMARY.md

<interfaces>
<!-- Wave 2에서 소비하는 Wave 1 결과물 계약 -->

From src/scenario_db/api/schemas/simulation.py (07-01 결과):
```python
class SimulateRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    scenario_id: str; variant_id: str; fps: float = 30.0
    dvfs_overrides: dict[str, int] | None = None; asv_group: int = 4

class SimulateResponse(BaseModel):
    evidence_id: str; params_hash: str; cached: bool
    feasible: bool; total_power_mw: float; bw_total_mbs: float; hw_time_max_ms: float

class BwAnalysisResponse(BaseModel):
    evidence_id: str; ports: list[PortBWResult]; total_bw_mbs: float

class PowerAnalysisResponse(BaseModel):
    evidence_id: str; total_power_mw: float; total_power_ma: float
    per_ip: dict[str, float]; per_vdd: dict[str, float]; bw_power_mw: float

class TimingAnalysisResponse(BaseModel):
    evidence_id: str; feasible: bool; hw_time_max_ms: float
    critical_ip: str | None; per_ip: list[IPTimingResult]
```

From src/scenario_db/db/repositories/simulation.py (07-01 결과):
```python
def save_sim_evidence(db, evidence_id, req, result, params_hash) -> Evidence
def find_by_params_hash(db, params_hash) -> Evidence | None
```

From src/scenario_db/db/loaders.py (07-02 결과):
```python
def load_runner_inputs_from_db(db, scenario_id, variant_id) \
    -> tuple[Pipeline, dict[str,IpCatalog], dict[str,DVFSTable], dict[str,IPPortConfig], SimGlobalConfig, SensorSpec|None] | None

def compute_params_hash(req: SimulateRequest) -> str  # 64자 SHA256 hex

def apply_request_overrides(sim_config: SimGlobalConfig, req: SimulateRequest) -> SimGlobalConfig
```

From src/scenario_db/sim/runner.py (07-02 결과):
```python
def run_simulation(
    scenario_id, variant_id, pipeline, ip_catalog, dvfs_tables,
    variant_port_config, sim_config, sensor_spec=None, fps=30.0
) -> SimRunResult
# SimRunResult.ip_power: dict[str, float] 포함 (Phase 7 추가)
```

From src/scenario_db/db/repositories/evidence.py (기존):
```python
def get_evidence(db: Session, evidence_id: str) -> Evidence | None
```

From src/scenario_db/api/routers/runtime.py (패턴 참조):
```python
router = APIRouter(tags=["runtime"])

@router.get("/scenarios/{scenario_id}/variants/{variant_id}/graph", response_model=...)
def get_graph(scenario_id, variant_id, db: Session = Depends(get_db)):
    graph = get_canonical_graph(db, scenario_id, variant_id)
    if graph is None:
        raise HTTPException(status_code=404, detail="...")
    return graph
```

From src/scenario_db/api/app.py (현재):
```python
for r in [capability.router, definition.router, evidence.router, decision.router,
          view_router.router, runtime_router.router]:
    app.include_router(r, prefix="/api/v1")
```

From src/scenario_db/models/evidence/simulation.py:
```python
class PortBWResult(BaseScenarioModel):
    ip: str; port: str; direction: Literal["read", "write"]
    bw_mbs: float; bw_power_mw: float

class IPTimingResult(BaseScenarioModel):
    ip: str; hw_time_ms: float
    required_clock_mhz: float; set_clock_mhz: float; set_voltage_mv: float; feasible: bool
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: simulation.py 라우터 5개 엔드포인트 + app.py 등록</name>
  <files>
    src/scenario_db/api/routers/simulation.py
    src/scenario_db/api/app.py
  </files>

  <read_first>
    - src/scenario_db/api/routers/runtime.py — APIRouter 패턴, get_db Depends 패턴, HTTPException 404 패턴 전체
    - src/scenario_db/api/app.py — include_router 루프 현재 상태 (정확한 수정 위치)
    - src/scenario_db/api/deps.py — get_db 함수 시그니처
    - src/scenario_db/db/repositories/evidence.py — get_evidence() 시그니처
    - src/scenario_db/api/schemas/simulation.py — (07-01에서 생성된) 5개 스키마 클래스 임포트 확인
    - src/scenario_db/db/repositories/simulation.py — (07-01에서 생성된) save/find 함수 시그니처
    - src/scenario_db/db/loaders.py — (07-02에서 생성된) 3개 함수 시그니처
  </read_first>

  <action>
**A. `src/scenario_db/api/routers/simulation.py` 신규 작성:**

```python
"""Phase 7 — /simulation/ 라우터 (SAPI-01~06).

엔드포인트:
  POST /simulation/run                         — 동기 계산 + 캐싱 (D-02)
  GET  /simulation/results/{evidence_id}       — Evidence 상세 조회
  GET  /simulation/bw-analysis                 — BW 분석 (bw_mbs 내림차순)
  GET  /simulation/power-analysis              — Power 분석 (IP별/VDD별)
  GET  /simulation/timing-analysis             — Timing 분석 (critical IP)
"""
from __future__ import annotations

from uuid import uuid4

from fastapi import APIRouter, Depends, HTTPException, Query
from sqlalchemy.orm import Session

from scenario_db.api.deps import get_db
from scenario_db.api.schemas.simulation import (
    BwAnalysisResponse,
    PowerAnalysisResponse,
    SimulateRequest,
    SimulateResponse,
    TimingAnalysisResponse,
)
from scenario_db.db.loaders import (
    apply_request_overrides,
    compute_params_hash,
    load_runner_inputs_from_db,
)
from scenario_db.db.repositories.evidence import get_evidence
from scenario_db.db.repositories.simulation import find_by_params_hash, save_sim_evidence
from scenario_db.models.evidence.simulation import IPTimingResult, PortBWResult
from scenario_db.sim.runner import run_simulation

router = APIRouter(tags=["simulation"])


@router.post("/simulation/run", response_model=SimulateResponse, status_code=200)
def run_sim(
    req: SimulateRequest,
    db: Session = Depends(get_db),
) -> SimulateResponse:
    """SimulateRequest → 동기 계산 → SimulationEvidence 저장 + 캐시 히트 처리 (SAPI-01, SAPI-06).

    Flow:
      1. params_hash 계산
      2. cache 조회 — HIT이면 즉시 반환 (cached=True)
      3. DB에서 runner 입력 로드
      4. run_simulation() 호출
      5. Evidence 저장
      6. SimulateResponse 반환 (cached=False)
    """
    params_hash = compute_params_hash(req)

    # SAPI-06: 캐시 HIT 확인
    cached_row = find_by_params_hash(db, params_hash)
    if cached_row is not None:
        kpi = cached_row.kpi or {}
        return SimulateResponse(
            evidence_id=cached_row.id,
            params_hash=params_hash,
            cached=True,
            feasible=bool(kpi.get("feasible", 0)),
            total_power_mw=float(kpi.get("total_power_mw", 0.0)),
            bw_total_mbs=float(kpi.get("bw_total_mbs", 0.0)),
            hw_time_max_ms=float(kpi.get("hw_time_max_ms", 0.0)),
        )

    # D-07: DB → runner 입력 변환
    inputs = load_runner_inputs_from_db(db, req.scenario_id, req.variant_id)
    if inputs is None:
        raise HTTPException(
            status_code=404,
            detail=f"scenario '{req.scenario_id}' or variant '{req.variant_id}' not found",
        )

    pipeline, ip_catalog, dvfs_tables, variant_port_config, sim_config, sensor_spec = inputs

    # D-03: request override 적용 (dvfs_overrides, asv_group)
    sim_config = apply_request_overrides(sim_config, req)

    # D-09: 계산 실행
    result = run_simulation(
        scenario_id=req.scenario_id,
        variant_id=req.variant_id,
        pipeline=pipeline,
        ip_catalog=ip_catalog,
        dvfs_tables=dvfs_tables,
        variant_port_config=variant_port_config,
        sim_config=sim_config,
        sensor_spec=sensor_spec,
        fps=req.fps,
    )

    # D-04: evidence_id 자동 생성
    evidence_id = f"evd-sim-{uuid4().hex[:8]}"

    # D-08: Evidence 저장
    save_sim_evidence(db, evidence_id, req, result, params_hash)

    return SimulateResponse(
        evidence_id=evidence_id,
        params_hash=params_hash,
        cached=False,
        feasible=result.feasible,
        total_power_mw=result.total_power_mw,
        bw_total_mbs=result.bw_total_mbs,
        hw_time_max_ms=result.hw_time_max_ms,
    )


@router.get(
    "/simulation/results/{evidence_id}",
    response_model=None,  # Evidence raw dict 반환
)
def get_sim_result(
    evidence_id: str,
    db: Session = Depends(get_db),
) -> dict:
    """SimulationEvidence 상세 반환 — dma_breakdown + timing_breakdown 포함 (SAPI-02).

    response_model=None: Evidence ORM → raw dict (기존 evidence 라우터 패턴 동일).
    """
    row = get_evidence(db, evidence_id)
    if row is None:
        raise HTTPException(status_code=404, detail=f"evidence '{evidence_id}' not found")
    return {
        "id": row.id,
        "kind": row.kind,
        "schema_version": row.schema_version,
        "scenario_ref": row.scenario_ref,
        "variant_ref": row.variant_ref,
        "overall_feasibility": row.overall_feasibility,
        "kpi": row.kpi,
        "dma_breakdown": row.dma_breakdown or [],
        "timing_breakdown": row.timing_breakdown or [],
        "ip_breakdown": row.ip_breakdown or {},
        "params_hash": row.params_hash,
    }


@router.get("/simulation/bw-analysis", response_model=BwAnalysisResponse)
def get_bw_analysis(
    evidence_id: str = Query(..., description="Evidence ID"),
    db: Session = Depends(get_db),
) -> BwAnalysisResponse:
    """PortBWResult 목록을 bw_mbs 내림차순으로 반환 (SAPI-03, D-05)."""
    row = get_evidence(db, evidence_id)
    if row is None:
        raise HTTPException(status_code=404, detail=f"evidence '{evidence_id}' not found")

    raw_ports: list[dict] = row.dma_breakdown or []
    ports = [PortBWResult.model_validate(p) for p in raw_ports]
    ports_sorted = sorted(ports, key=lambda p: p.bw_mbs, reverse=True)
    total_bw_mbs = sum(p.bw_mbs for p in ports_sorted)

    return BwAnalysisResponse(
        evidence_id=evidence_id,
        ports=ports_sorted,
        total_bw_mbs=total_bw_mbs,
    )


@router.get("/simulation/power-analysis", response_model=PowerAnalysisResponse)
def get_power_analysis(
    evidence_id: str = Query(..., description="Evidence ID"),
    db: Session = Depends(get_db),
) -> PowerAnalysisResponse:
    """total_power/per_ip/per_vdd/bw_power 반환 (SAPI-04, D-05, D-09).

    per_ip: ip_breakdown.ip_power (D-06 — runner.py Step 5에서 수집)
    per_vdd: ip_breakdown.vdd_power
    bw_power_mw: sum(dma_breakdown[].bw_power_mw)
    """
    row = get_evidence(db, evidence_id)
    if row is None:
        raise HTTPException(status_code=404, detail=f"evidence '{evidence_id}' not found")

    kpi: dict = row.kpi or {}
    ip_breakdown: dict = row.ip_breakdown or {}
    raw_ports: list[dict] = row.dma_breakdown or []

    per_ip: dict[str, float] = ip_breakdown.get("ip_power", {})
    per_vdd: dict[str, float] = ip_breakdown.get("vdd_power", {})
    bw_power_mw = sum(float(p.get("bw_power_mw", 0.0)) for p in raw_ports)

    return PowerAnalysisResponse(
        evidence_id=evidence_id,
        total_power_mw=float(kpi.get("total_power_mw", 0.0)),
        total_power_ma=float(kpi.get("total_power_ma", 0.0)),
        per_ip=per_ip,
        per_vdd=per_vdd,
        bw_power_mw=bw_power_mw,
    )


@router.get("/simulation/timing-analysis", response_model=TimingAnalysisResponse)
def get_timing_analysis(
    evidence_id: str = Query(..., description="Evidence ID"),
    db: Session = Depends(get_db),
) -> TimingAnalysisResponse:
    """critical_ip/hw_time_max_ms/per_ip/feasible 반환 (SAPI-05, D-05, D-09).

    per_ip: timing_breakdown — hw_time_ms 내림차순
    critical_ip: hw_time_ms 최대 IP 이름
    """
    row = get_evidence(db, evidence_id)
    if row is None:
        raise HTTPException(status_code=404, detail=f"evidence '{evidence_id}' not found")

    kpi: dict = row.kpi or {}
    raw_timing: list[dict] = row.timing_breakdown or []

    timing_results = [IPTimingResult.model_validate(t) for t in raw_timing]
    timing_sorted = sorted(timing_results, key=lambda t: t.hw_time_ms, reverse=True)
    critical_ip: str | None = timing_sorted[0].ip if timing_sorted else None

    return TimingAnalysisResponse(
        evidence_id=evidence_id,
        feasible=bool(kpi.get("feasible", 0)),
        hw_time_max_ms=float(kpi.get("hw_time_max_ms", 0.0)),
        critical_ip=critical_ip,
        per_ip=timing_sorted,
    )
```

**B. `src/scenario_db/api/app.py` 수정:**

파일을 읽은 후 `from scenario_db.api.routers import runtime as runtime_router` 줄 바로 아래에:
```python
from scenario_db.api.routers import simulation as simulation_router
```
를 추가한다.

`for r in [...]` 루프의 마지막 `runtime_router.router` 뒤에 `simulation_router.router` 추가:
```python
    for r in [
        capability.router,
        definition.router,
        evidence.router,
        decision.router,
        view_router.router,
        runtime_router.router,
        simulation_router.router,   # Phase 7 추가 (D-01)
    ]:
        app.include_router(r, prefix="/api/v1")
```
  </action>

  <verify>
    <automated>
      uv run python -c "
from scenario_db.api.app import create_app
app = create_app()
routes = [r.path for r in app.routes]
assert any('/simulation/run' in p for p in routes), f'POST /simulation/run not found in {routes}'
assert any('/simulation/results' in p for p in routes), 'GET /results not found'
assert any('bw-analysis' in p for p in routes), 'bw-analysis not found'
assert any('power-analysis' in p for p in routes), 'power-analysis not found'
assert any('timing-analysis' in p for p in routes), 'timing-analysis not found'
print('all 5 routes registered ok')
"
    </automated>
  </verify>

  <acceptance_criteria>
    - `src/scenario_db/api/routers/simulation.py` 파일 존재
    - `grep "router = APIRouter" src/scenario_db/api/routers/simulation.py` → 결과 있음
    - `grep "simulation/run" src/scenario_db/api/routers/simulation.py` → 결과 있음
    - `grep "bw-analysis" src/scenario_db/api/routers/simulation.py` → 결과 있음
    - `grep "simulation_router" src/scenario_db/api/app.py` → 결과 있음
    - `grep "cached=True" src/scenario_db/api/routers/simulation.py` → 결과 있음 (캐싱 로직)
    - `grep "find_by_params_hash" src/scenario_db/api/routers/simulation.py` → 결과 있음
    - `grep "load_runner_inputs_from_db" src/scenario_db/api/routers/simulation.py` → 결과 있음
    - Python 임포트 에러 없음 (create_app() 호출 성공)
    - 5개 라우트 등록 검증 Python 스크립트 통과
  </acceptance_criteria>

  <done>
    5개 엔드포인트가 등록되고, app.py 라우터 목록에 simulation_router가 추가된다.
    create_app()이 에러 없이 실행된다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 통합 테스트 작성 + 전체 테스트 통과 확인</name>
  <files>
    tests/integration/test_simulation_api.py
  </files>

  <read_first>
    - tests/integration/ 디렉터리 기존 파일 1개 — TestClient 픽스처 패턴, conftest.py 위치 확인
    - tests/conftest.py 또는 tests/integration/conftest.py — db, client 픽스처 시그니처
    - src/scenario_db/api/routers/simulation.py — (Task 1에서 생성된) 5개 엔드포인트 경로 및 응답 필드
    - src/scenario_db/api/schemas/simulation.py — 응답 스키마 필드 목록
  </read_first>

  <behavior>
    - test_post_run_cache_miss: POST /api/v1/simulation/run → 200, cached=False, evidence_id 반환
    - test_post_run_cache_hit: 동일 payload로 두 번째 POST → cached=True, 동일 evidence_id 반환
    - test_get_results: GET /api/v1/simulation/results/{evidence_id} → 200, dma_breakdown/timing_breakdown 키 존재
    - test_get_results_not_found: GET /api/v1/simulation/results/bad-id → 404
    - test_bw_analysis: GET /api/v1/simulation/bw-analysis?evidence_id=xxx → 200, ports 정렬 검증 (bw_mbs 내림차순)
    - test_bw_analysis_not_found: 없는 evidence_id → 404
    - test_power_analysis: GET /api/v1/simulation/power-analysis?evidence_id=xxx → 200, per_ip/per_vdd/bw_power_mw 키 존재
    - test_timing_analysis: GET /api/v1/simulation/timing-analysis?evidence_id=xxx → 200, critical_ip/feasible/hw_time_max_ms 존재
    - test_run_invalid_scenario: 존재하지 않는 scenario_id → 404
  </behavior>

  <action>
기존 통합 테스트 conftest.py의 db 픽스처와 TestClient 패턴을 먼저 파악한 후 작성.

`tests/integration/test_simulation_api.py` 작성:

```python
"""Phase 7 — /simulation/ 엔드포인트 통합 테스트 (SAPI-01~06, D-11).

TestClient 기반. SQLite in-memory DB (기존 테스트 패턴과 동일).
run_simulation()과 load_runner_inputs_from_db()는 pytest.monkeypatch로 모킹하거나,
기존 conftest의 demo 픽스처 데이터(scenario/variant/ip_catalog)를 활용한다.

접근 전략:
  - 기존 conftest fixture에 demo scenario + variant + ip_catalog 데이터가 있으면 재사용
  - 없으면 simulation 엔드포인트 전용 mock 픽스처 추가
  - monkeypatch로 run_simulation()의 반환값을 SimRunResult fixture로 교체하여
    DVFS YAML 파일 의존성 없이 테스트
"""
from __future__ import annotations

import pytest
from fastapi.testclient import TestClient

# conftest에서 client, db_session 픽스처를 가져오는 패턴 (기존 통합 테스트 동일)
# from tests.integration.conftest import client  (픽스처이므로 자동 주입)
```

테스트 전략 — 두 가지 접근 중 기존 conftest 분석 후 선택:

**Option A: monkeypatch 방식 (권장)**
- `run_simulation()` 을 `monkeypatch.setattr`로 고정된 `SimRunResult` 반환하도록 패치
- `load_runner_inputs_from_db()` 를 패치하여 DB 데이터 없이 동작
- 실제 DB에 Evidence row는 저장됨 (save_sim_evidence는 실제 호출)

**Option B: demo data 방식**
- 기존 conftest의 demo scenario + variant를 POST /run에 그대로 사용
- `DVFS_CONFIG_PATH` 파일이 존재해야 하는 제약 있음

기존 통합 테스트 conftest.py를 읽어 어느 방식이 적합한지 판단 후 구현.

각 테스트 케이스 구현 시 반드시 포함할 검증:
1. `test_post_run_cache_miss_and_hit`:
   - 동일 payload 두 번 POST
   - 첫 번째: `assert resp1.json()["cached"] == False`
   - 두 번째: `assert resp2.json()["cached"] == True`
   - `assert resp1.json()["evidence_id"] == resp2.json()["evidence_id"]`

2. `test_bw_analysis_sorted`:
   - bw-analysis 응답의 ports 리스트에서 연속된 두 항목 비교
   - `assert ports[i]["bw_mbs"] >= ports[i+1]["bw_mbs"]` (내림차순)

3. `test_timing_analysis_critical_ip`:
   - timing_breakdown이 비어있지 않으면 critical_ip가 None이 아님
   - critical_ip가 per_ip 목록의 첫 번째 항목 ip와 동일

4. 404 케이스 3개:
   - `/results/nonexistent-id` → 404
   - `/bw-analysis?evidence_id=nonexistent-id` → 404
   - `/timing-analysis?evidence_id=nonexistent-id` → 404
  </action>

  <verify>
    <automated>
      uv run pytest tests/integration/test_simulation_api.py -v
    </automated>
  </verify>

  <acceptance_criteria>
    - `tests/integration/test_simulation_api.py` 파일 존재
    - `grep "test_post_run" tests/integration/test_simulation_api.py` → 결과 있음
    - `grep "cached" tests/integration/test_simulation_api.py` → 결과 있음 (캐시 검증)
    - `grep "bw_mbs" tests/integration/test_simulation_api.py` → 결과 있음 (정렬 검증)
    - `grep "404" tests/integration/test_simulation_api.py` → 결과 있음 (3개 이상)
    - `uv run pytest tests/integration/test_simulation_api.py -v` → 전체 pass (0 failures)
    - 테스트 함수 수: 최소 8개 이상
    - 캐시 히트 테스트에서 두 번째 요청의 cached=True 검증 assert 존재
  </acceptance_criteria>

  <done>
    통합 테스트가 모두 통과하고, params_hash 캐싱 플로우가 검증된다.
    GET 분석 엔드포인트의 404 처리가 검증된다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 7 전체 구현 완료:
    - Wave 1a: migration 0003, 5개 Pydantic 스키마, simulation repository
    - Wave 1b: SimRunResult.ip_power, runner.py Step 5 수집, loaders.py
    - Wave 2: simulation.py 라우터 5개 엔드포인트, app.py 등록, 통합 테스트
  </what-built>
  <how-to-verify>
    1. 전체 테스트 실행:
       `uv run pytest tests/ -v --tb=short 2>&1 | tail -30`
       → 기존 505개 + 신규 테스트 전체 pass 확인

    2. FastAPI 서버 시작 및 엔드포인트 확인:
       `uv run uvicorn scenario_db.api.app:app --reload --port 8000`
       → http://localhost:8000/docs 에서 /simulation/ 라우터 5개 엔드포인트 확인

    3. 캐싱 플로우 수동 확인 (demo 데이터가 있는 경우):
       ```bash
       curl -s -X POST http://localhost:8000/api/v1/simulation/run \
         -H "Content-Type: application/json" \
         -d '{"scenario_id":"demo-isp","variant_id":"FHD30","fps":30}' | python -m json.tool
       # cached: false

       curl -s -X POST http://localhost:8000/api/v1/simulation/run \
         -H "Content-Type: application/json" \
         -d '{"scenario_id":"demo-isp","variant_id":"FHD30","fps":30}' | python -m json.tool
       # cached: true
       ```

    4. 분석 엔드포인트 404 확인:
       ```bash
       curl -s -o /dev/null -w "%{http_code}" \
         "http://localhost:8000/api/v1/simulation/results/nonexistent"
       # → 404
       ```
  </how-to-verify>
  <resume-signal>
    전체 테스트 통과하고 5개 엔드포인트가 /docs에서 보이면 "approved" 입력.
    이슈가 있으면 구체적인 오류 메시지 제공.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→POST /simulation/run | SimulateRequest: extra='forbid'로 unknown field 차단 |
| Evidence JSONB→클라이언트 | dma_breakdown/timing_breakdown/ip_breakdown JSONB 그대로 반환 — Pydantic 재검증 후 응답 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | POST /run request body | mitigate | SimulateRequest model_config extra='forbid' — unknown field 즉시 422 반환 |
| T-07-08 | Information Disclosure | GET /results/{id} raw dict | accept | evidence_id는 random UUID prefix — 추측 불가. 서비스 내부 데이터이나 외부 공개 API 아님 |
| T-07-09 | Elevation of Privilege | load_runner_inputs_from_db | mitigate | scenario_id/variant_id는 DB 조회로 검증 — 존재하지 않으면 404. 임의 계산 불가 |
| T-07-10 | Denial of Service | 동기 run_simulation() | accept | 동기 계산 범위 내 결정 (Deferred 비동기). params_hash 캐싱으로 동일 요청 반복 방어 |
</threat_model>

<verification>
## Phase 7 전체 검증 명령

```bash
# 1. migration 파일 확인
uv run python -c "import alembic.versions; print('migration import ok')"

# 2. 라우터 등록 검증
uv run python -c "
from scenario_db.api.app import create_app
app = create_app()
routes = [r.path for r in app.routes]
sim_routes = [p for p in routes if 'simulation' in p]
assert len(sim_routes) >= 5, f'Expected 5+ simulation routes, got: {sim_routes}'
print('routes:', sim_routes)
"

# 3. 전체 단위 테스트
uv run pytest tests/unit/ -q --tb=short

# 4. 전체 통합 테스트
uv run pytest tests/integration/ -q --tb=short

# 5. sim 테스트 회귀 없음
uv run pytest tests/sim/ -q

# 6. 전체 테스트 (회귀 최종 확인)
uv run pytest tests/ -q --tb=short 2>&1 | tail -20

# 7. 스키마 5종 임포트
uv run python -c "
from scenario_db.api.schemas.simulation import (
    SimulateRequest, SimulateResponse,
    BwAnalysisResponse, PowerAnalysisResponse, TimingAnalysisResponse
)
print('5 schemas imported ok')
"

# 8. ip_power 수집 확인 (runner.py)
grep -c "ip_power\[ip_name\]" src/scenario_db/sim/runner.py
# 결과: 1 이상

# 9. find_by_params_hash 캐싱 쿼리 검증
grep "evidence.simulation" src/scenario_db/db/repositories/simulation.py
# 결과: 있음
```
</verification>

<success_criteria>
- POST /api/v1/simulation/run: SimulateRequest → 200, SimulateResponse (evidence_id, cached, feasible 등)
- GET /api/v1/simulation/results/{id}: 200 (evidence 상세), 없으면 404
- GET /api/v1/simulation/bw-analysis?evidence_id=xxx: 200 (ports bw_mbs 내림차순), 없으면 404
- GET /api/v1/simulation/power-analysis?evidence_id=xxx: 200 (per_ip/per_vdd/bw_power_mw), 없으면 404
- GET /api/v1/simulation/timing-analysis?evidence_id=xxx: 200 (critical_ip/feasible), 없으면 404
- 동일 params_hash 두 번째 요청: cached=True
- app.py: simulation_router 등록 확인
- tests/integration/test_simulation_api.py: 8개 이상 테스트, 전체 pass
- 기존 505개 테스트 회귀 없음
- SAPI-01~SAPI-06 전체 요구사항 커버
</success_criteria>

<output>
완료 후 `.planning/phases/07-simulation-api/07-03-SUMMARY.md` 생성.
STATE.md 업데이트: Phase 7 COMPLETE, progress 7/7.
</output>
