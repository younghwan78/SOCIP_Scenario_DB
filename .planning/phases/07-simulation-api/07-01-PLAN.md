---
plan: "07-01"
phase: 7
wave: 1
depends_on: []
title: "인프라 + 스키마 레이어 — migration 0003, Pydantic schemas, simulation repository"
objective: >
  Alembic migration 0003으로 Evidence 테이블에 params_hash 컬럼을 추가하고,
  Simulation API 전용 Pydantic 응답 스키마 5종과 DB 저장/조회 repository를 구현한다.
  Wave 2 라우터가 의존하는 계약(contracts)을 먼저 확립한다.
files_modified:
  - alembic/versions/0003_params_hash.py
  - src/scenario_db/api/schemas/simulation.py
  - src/scenario_db/db/repositories/simulation.py
  - tests/unit/test_simulation_schemas.py
requirements:
  - SAPI-01
  - SAPI-02
  - SAPI-03
  - SAPI-04
  - SAPI-05
  - SAPI-06
autonomous: true

must_haves:
  truths:
    - "Evidence 테이블에 params_hash TEXT 컬럼이 존재하고 alembic upgrade head로 적용 가능하다"
    - "SimulateRequest / SimulateResponse Pydantic 모델이 extra='forbid'로 round-trip 직렬화 통과한다"
    - "BwAnalysisResponse / PowerAnalysisResponse / TimingAnalysisResponse Pydantic 모델이 round-trip 통과한다"
    - "save_sim_evidence() 가 Evidence ORM row를 DB에 저장하고 id를 반환한다"
    - "find_by_params_hash() 가 params_hash + kind='evidence.simulation' 조건으로 최신 Evidence row를 반환한다"
  artifacts:
    - path: "alembic/versions/0003_params_hash.py"
      provides: "Evidence.params_hash 컬럼 migration"
      contains: "revision = \"0003\""
    - path: "src/scenario_db/api/schemas/simulation.py"
      provides: "5종 Pydantic 스키마"
      exports:
        - SimulateRequest
        - SimulateResponse
        - BwAnalysisResponse
        - PowerAnalysisResponse
        - TimingAnalysisResponse
    - path: "src/scenario_db/db/repositories/simulation.py"
      provides: "save_sim_evidence(), find_by_params_hash()"
      exports:
        - save_sim_evidence
        - find_by_params_hash
    - path: "tests/unit/test_simulation_schemas.py"
      provides: "스키마 단위 테스트"
  key_links:
    - from: "find_by_params_hash()"
      to: "Evidence.params_hash 컬럼"
      via: "db.query(Evidence).filter_by(params_hash=..., kind='evidence.simulation')"
      pattern: "filter.*params_hash.*evidence\\.simulation"
    - from: "save_sim_evidence()"
      to: "Evidence ORM"
      via: "db.add(evidence_row); db.commit()"
      pattern: "db\\.add.*db\\.commit"
---

<objective>
Wave 1a: Simulation API의 인프라 + 계약(contracts) 레이어 구축.

목적: Wave 2 라우터(07-03-PLAN)가 의존하는 두 가지 계약을 먼저 확립한다.
  1. DB 계약 — Evidence 테이블에 params_hash 컬럼 (migration 0003)
  2. API 계약 — 요청/응답 Pydantic 스키마 5종
  3. Repository 계약 — save_sim_evidence() / find_by_params_hash()

출력 아티팩트:
  - alembic/versions/0003_params_hash.py
  - src/scenario_db/api/schemas/simulation.py
  - src/scenario_db/db/repositories/simulation.py
  - tests/unit/test_simulation_schemas.py
</objective>

<execution_context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\07-simulation-api\07-CONTEXT.md
</execution_context>

<context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\ROADMAP.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\STATE.md

<interfaces>
<!-- Wave 1a에서 사용하는 기존 코드 계약 -->

From alembic/versions/0002_schema_extensions.py:
```python
revision = "0002"
down_revision = "0001"
branch_labels = None
depends_on = None

def upgrade() -> None:
    op.add_column("evidence", sa.Column("dma_breakdown", JSONB, nullable=True))

def downgrade() -> None:
    op.drop_column("evidence", "dma_breakdown")
```

From src/scenario_db/db/models/evidence.py:
```python
class Evidence(Base):
    __tablename__ = "evidence"
    id                  = Column(Text, primary_key=True)
    schema_version      = Column(Text, nullable=False)
    kind                = Column(Text, nullable=False)
    scenario_ref        = Column(Text, ForeignKey("scenarios.id"), nullable=False)
    variant_ref         = Column(Text, nullable=False)
    execution_context   = Column(JSONB, nullable=False)
    aggregation         = Column(JSONB, nullable=False)
    kpi                 = Column(JSONB, nullable=False)
    run_info            = Column(JSONB)
    ip_breakdown        = Column(JSONB)
    dma_breakdown       = Column(JSONB)
    timing_breakdown    = Column(JSONB)
    overall_feasibility = Column(Text)
    yaml_sha256         = Column(Text, nullable=False)
    # params_hash 없음 — 이 Plan에서 migration으로 추가
```

From src/scenario_db/db/repositories/evidence.py:
```python
def get_evidence(db: Session, evidence_id: str) -> Evidence | None:
    return db.query(Evidence).filter_by(id=evidence_id).one_or_none()
```

From src/scenario_db/models/evidence/simulation.py:
```python
class PortBWResult(BaseScenarioModel):
    ip: str; port: str; direction: Literal["read", "write"]
    bw_mbs: float; bw_mbs_worst: float | None = None
    bw_power_mw: float; format: str | None = None
    compression: str | None = None; llc_enabled: bool = False

class IPTimingResult(BaseScenarioModel):
    ip: str; hw_time_ms: float; required_clock_mhz: float
    set_clock_mhz: float; set_voltage_mv: float; feasible: bool
```

From src/scenario_db/models/common.py (패턴):
```python
class BaseScenarioModel(BaseModel):
    model_config = ConfigDict(extra="forbid")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration 0003 — Evidence.params_hash 컬럼 추가</name>
  <files>alembic/versions/0003_params_hash.py</files>

  <read_first>
    - alembic/versions/0002_schema_extensions.py — revision/down_revision 체인 패턴, op.add_column() 사용법
    - src/scenario_db/db/models/evidence.py — Evidence 테이블 현재 컬럼 목록 확인
  </read_first>

  <action>
`alembic/versions/0003_params_hash.py` 파일을 아래 정확한 내용으로 작성한다.

```python
"""Add params_hash column to evidence table

Revision ID: 0003
Revises: 0002
Create Date: 2026-05-17

D-02: params_hash TEXT — SHA256(scenario_id+variant_id+fps+dvfs_overrides+asv_group)
      캐싱 캐시 히트 여부 판별에 사용. nullable=True (기존 evidence 행 보호).
"""
from __future__ import annotations

import sqlalchemy as sa
from alembic import op

revision = "0003"
down_revision = "0002"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column(
        "evidence",
        sa.Column("params_hash", sa.Text, nullable=True),
    )


def downgrade() -> None:
    op.drop_column("evidence", "params_hash")
```

추가로 `src/scenario_db/db/models/evidence.py`를 수정하여 Evidence 클래스에
`params_hash = Column(Text)` 컬럼을 추가한다 (nullable, yaml_sha256 바로 아래).

Evidence 클래스 끝 부분 (sweep_value_hint Computed 컬럼 아래) 에:
```python
    params_hash = Column(Text)  # sim only — Phase 7 추가 (D-02)
```
  </action>

  <verify>
    <automated>
      python -c "import ast, pathlib; src=pathlib.Path('alembic/versions/0003_params_hash.py').read_text(); tree=ast.parse(src); print('parse ok')"
    </automated>
  </verify>

  <acceptance_criteria>
    - `alembic/versions/0003_params_hash.py` 파일 존재
    - 파일 내 `revision = "0003"` 문자열 존재
    - 파일 내 `down_revision = "0002"` 문자열 존재
    - 파일 내 `op.add_column` 과 `"params_hash"` 문자열 존재
    - `src/scenario_db/db/models/evidence.py` 내 `params_hash` 문자열 존재
    - Python 구문 에러 없음 (ast.parse 통과)
  </acceptance_criteria>

  <done>
    migration 0003 파일이 생성되고 Evidence ORM 모델에 params_hash 컬럼이 추가된다.
    기존 데이터는 nullable=True로 보호된다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pydantic 스키마 5종 + repository 2종 + 단위 테스트</name>
  <files>
    src/scenario_db/api/schemas/simulation.py
    src/scenario_db/db/repositories/simulation.py
    tests/unit/test_simulation_schemas.py
  </files>

  <read_first>
    - src/scenario_db/models/evidence/simulation.py — PortBWResult, IPTimingResult 임포트 경로
    - src/scenario_db/models/common.py — BaseScenarioModel 정의
    - src/scenario_db/db/repositories/evidence.py — get_evidence() 패턴 (filter_by 사용법)
    - src/scenario_db/db/models/evidence.py — Evidence ORM 필드 목록 (Task 1 수정 후)
    - src/scenario_db/api/schemas/ 디렉터리의 기존 파일 1개 (네이밍 컨벤션 확인)
  </read_first>

  <behavior>
    - SimulateRequest: extra='forbid', scenario_id/variant_id(str), fps=30.0, dvfs_overrides(dict|None)=None, asv_group=4
    - SimulateResponse: evidence_id, params_hash, cached(bool), feasible, total_power_mw, bw_total_mbs, hw_time_max_ms
    - BwAnalysisResponse: evidence_id, ports(list[PortBWResult]), total_bw_mbs
    - PowerAnalysisResponse: evidence_id, total_power_mw, total_power_ma, per_ip(dict), per_vdd(dict), bw_power_mw
    - TimingAnalysisResponse: evidence_id, feasible, hw_time_max_ms, critical_ip(str|None), per_ip(list[IPTimingResult])
    - save_sim_evidence(db, evidence_id, req, result, params_hash) -> Evidence (db.add + db.commit)
    - find_by_params_hash(db, params_hash) -> Evidence | None (filter by params_hash AND kind='evidence.simulation', order_by id desc)
  </behavior>

  <action>
**A. `src/scenario_db/api/schemas/simulation.py` 작성:**

```python
"""Phase 7 Simulation API — 요청/응답 Pydantic 스키마 (D-03, D-09, D-10)."""
from __future__ import annotations

from pydantic import BaseModel, ConfigDict

from scenario_db.models.evidence.simulation import IPTimingResult, PortBWResult


class SimulateRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")
    scenario_id: str
    variant_id: str
    fps: float = 30.0
    dvfs_overrides: dict[str, int] | None = None
    asv_group: int = 4


class SimulateResponse(BaseModel):
    model_config = ConfigDict(extra="forbid")
    evidence_id: str
    params_hash: str
    cached: bool
    feasible: bool
    total_power_mw: float
    bw_total_mbs: float
    hw_time_max_ms: float


class BwAnalysisResponse(BaseModel):
    model_config = ConfigDict(extra="forbid")
    evidence_id: str
    ports: list[PortBWResult]
    total_bw_mbs: float


class PowerAnalysisResponse(BaseModel):
    model_config = ConfigDict(extra="forbid")
    evidence_id: str
    total_power_mw: float
    total_power_ma: float
    per_ip: dict[str, float]
    per_vdd: dict[str, float]
    bw_power_mw: float


class TimingAnalysisResponse(BaseModel):
    model_config = ConfigDict(extra="forbid")
    evidence_id: str
    feasible: bool
    hw_time_max_ms: float
    critical_ip: str | None
    per_ip: list[IPTimingResult]
```

**B. `src/scenario_db/db/repositories/simulation.py` 작성:**

```python
"""Simulation Evidence DB 저장/조회 (D-08, D-02, SAPI-06)."""
from __future__ import annotations

from datetime import datetime

from sqlalchemy.orm import Session

from scenario_db.api.schemas.simulation import SimulateRequest
from scenario_db.db.models.evidence import Evidence
from scenario_db.sim.models import SimRunResult


def save_sim_evidence(
    db: Session,
    evidence_id: str,
    req: SimulateRequest,
    result: SimRunResult,
    params_hash: str,
) -> Evidence:
    """SimRunResult를 Evidence ORM row로 변환하여 DB에 저장한다 (D-08).

    yaml_sha256 = params_hash (YAML 파일 없는 simulation evidence 한정).
    """
    row = Evidence(
        id=evidence_id,
        schema_version="0.1.0",
        kind="evidence.simulation",
        scenario_ref=req.scenario_id,
        variant_ref=req.variant_id,
        execution_context={
            "silicon_rev": "api-sim",
            "sw_baseline_ref": "n/a",
            "thermal": "nominal",
        },
        aggregation={"strategy": "single"},
        kpi={
            "total_power_mw": result.total_power_mw,
            "total_power_ma": result.total_power_ma,
            "bw_total_mbs": result.bw_total_mbs,
            "hw_time_max_ms": result.hw_time_max_ms,
            "feasible": int(result.feasible),
        },
        run_info={
            "timestamp": datetime.utcnow().isoformat(),
            "tool": "scenario_db.sim.runner",
            "source": "simulated",
        },
        dma_breakdown=[r.model_dump() for r in result.dma_breakdown],
        timing_breakdown=[t.model_dump() for t in result.timing_breakdown],
        ip_breakdown={
            "vdd_power": result.vdd_power,
            "ip_power": result.ip_power,
        },
        overall_feasibility="feasible" if result.feasible else "infeasible",
        yaml_sha256=params_hash,
        params_hash=params_hash,
    )
    db.add(row)
    db.commit()
    db.refresh(row)
    return row


def find_by_params_hash(db: Session, params_hash: str) -> Evidence | None:
    """params_hash + kind='evidence.simulation' 으로 최신 Evidence 조회 (D-02, SAPI-06).

    동일 params_hash 행이 여러 개일 경우 id 내림차순(가장 최근 생성) 반환.
    """
    return (
        db.query(Evidence)
        .filter(
            Evidence.params_hash == params_hash,
            Evidence.kind == "evidence.simulation",
        )
        .order_by(Evidence.id.desc())
        .first()
    )
```

**C. `tests/unit/test_simulation_schemas.py` 작성:**

5개 스키마 각각에 대해:
- 최소 필드로 인스턴스 생성 후 model_dump() 라운드트립 통과 테스트
- SimulateRequest: extra 필드 추가 시 ValidationError 발생 확인
- SimulateRequest: dvfs_overrides=None 기본값 확인
- SimulateRequest: asv_group=4 기본값 확인
- SimulateRequest: fps=30.0 기본값 확인
  </action>

  <verify>
    <automated>
      uv run pytest tests/unit/test_simulation_schemas.py -v
    </automated>
  </verify>

  <acceptance_criteria>
    - `src/scenario_db/api/schemas/simulation.py` 내 5개 클래스 존재: `SimulateRequest`, `SimulateResponse`, `BwAnalysisResponse`, `PowerAnalysisResponse`, `TimingAnalysisResponse`
    - `grep -c "class Simulate" src/scenario_db/api/schemas/simulation.py` → 2 이상
    - `src/scenario_db/db/repositories/simulation.py` 내 `save_sim_evidence` 함수 존재
    - `src/scenario_db/db/repositories/simulation.py` 내 `find_by_params_hash` 함수 존재
    - `grep "ip_power" src/scenario_db/db/repositories/simulation.py` → 결과 있음 (D-06 ip_power 저장)
    - `grep "order_by" src/scenario_db/db/repositories/simulation.py` → 결과 있음 (최신 행 우선)
    - `uv run pytest tests/unit/test_simulation_schemas.py -v` → 전체 pass (0 failures)
    - `SimulateRequest`의 extra field 테스트가 ValidationError를 기대하며 pass
  </acceptance_criteria>

  <done>
    5개 Pydantic 스키마와 2개 repository 함수가 구현되고,
    스키마 단위 테스트가 모두 통과한다.
    기존 505개 테스트 회귀 없음.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→POST /run | 클라이언트가 scenario_id, variant_id를 조작 가능 — DB 조회로 검증 필요 |
| params_hash cache | SHA256은 암호학적으로 안전하나, DB 데이터 변경 시 캐시 무효화 없음 (Phase 7 범위 외로 accept) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | SimulateRequest.scenario_id | mitigate | loaders.py에서 DB 조회 시 존재 여부 검증 — 없으면 404 반환 (D-07) |
| T-07-02 | Information Disclosure | params_hash cache | accept | SHA256 해시는 역추적 불가, evidence_id는 internal ID — 외부 노출 위험 낮음 |
| T-07-03 | Denial of Service | POST /run 동기 계산 | accept | 동기 계산은 현 Phase 결정 (Deferred); 악의적 대용량 pipeline은 loaders 404로 차단 |
</threat_model>

<verification>
## Wave 1a 전체 검증

```bash
# 1. migration 파일 구문 검사
uv run python -c "import alembic.versions.0003_params_hash"

# 2. 스키마 임포트 검사
uv run python -c "from scenario_db.api.schemas.simulation import SimulateRequest, SimulateResponse, BwAnalysisResponse, PowerAnalysisResponse, TimingAnalysisResponse; print('schemas ok')"

# 3. repository 임포트 검사
uv run python -c "from scenario_db.db.repositories.simulation import save_sim_evidence, find_by_params_hash; print('repo ok')"

# 4. 단위 테스트
uv run pytest tests/unit/test_simulation_schemas.py -v

# 5. 기존 테스트 회귀 없음
uv run pytest tests/ -x --ignore=tests/integration -q
```
</verification>

<success_criteria>
- alembic/versions/0003_params_hash.py: revision="0003", down_revision="0002", params_hash Text nullable
- src/scenario_db/db/models/evidence.py: params_hash = Column(Text) 추가
- api/schemas/simulation.py: 5개 클래스 정의, PortBWResult/IPTimingResult re-import
- db/repositories/simulation.py: save_sim_evidence() (db.add+db.commit), find_by_params_hash() (filter+order_by)
- tests/unit/test_simulation_schemas.py: 전체 pass
- 기존 505개 테스트 회귀 없음
</success_criteria>

<output>
완료 후 `.planning/phases/07-simulation-api/07-01-SUMMARY.md` 생성.
</output>
