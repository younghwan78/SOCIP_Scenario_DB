---
phase: 03-runtime-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scenario_db/api/routers/view.py
  - src/scenario_db/view/service.py
autonomous: true
requirements:
  - API-04

must_haves:
  truths:
    - "view router의 mode 파라미터가 service.py에 전달된다"
    - "mode='topology'이면 501 응답이 반환된다"
    - "mode='architecture'이면 DB projection 기반 최소 ViewResponse가 반환된다"
    - "project_level0()에서 db=None 분기(build_sample_level0 호출)가 제거된다"
    - "DB에 없는 scenario/variant 요청 시 404가 반환된다"
  artifacts:
    - path: "src/scenario_db/api/routers/view.py"
      provides: "mode 파라미터를 project_level0에 전달하는 수정된 라우터"
      contains: "mode=mode"
    - path: "src/scenario_db/view/service.py"
      provides: "sample fallback 제거 + mode 분기 + DB projection 기반 architecture 구현"
  key_links:
    - from: "src/scenario_db/api/routers/view.py"
      to: "src/scenario_db/view/service.project_level0"
      via: "직접 함수 호출 with mode=mode"
      pattern: "project_level0.*mode=mode"
    - from: "src/scenario_db/view/service.project_level0"
      to: "src/scenario_db/db/repositories/view_projection.get_view_projection"
      via: "직접 함수 호출"
      pattern: "get_view_projection"
---

<objective>
`view.py` 라우터가 `mode` 파라미터를 `project_level0()`에 전달하도록 수정하고, `service.py`에서 sample fallback(`db=None` 분기)을 제거한 뒤 `mode` 분기(architecture=DB projection, topology=NotImplementedError)를 구현한다.

Purpose: API-04 — view router가 sample data 없이 실제 DB projection을 반환하는 경로를 확보한다.
Output: `src/scenario_db/api/routers/view.py` (수정), `src/scenario_db/view/service.py` (수정)
</objective>

<execution_context>
@E:/15_ClaudeCode_IP_Modeling/02_ScenarioDB/.planning/phases/03-runtime-api/03-CONTEXT.md
</execution_context>

<context>
@E:/15_ClaudeCode_IP_Modeling/02_ScenarioDB/.planning/ROADMAP.md
@E:/15_ClaudeCode_IP_Modeling/02_ScenarioDB/.planning/phases/03-runtime-api/03-CONTEXT.md

<interfaces>
<!-- executor가 직접 사용할 인터페이스 -->

From src/scenario_db/db/repositories/view_projection.py:
```python
def get_view_projection(
    db: Session,
    scenario_id: str,
    variant_id: str,
) -> dict | None:
    # 반환 구조:
    # {
    #   "scenario_id": str,
    #   "variant_id": str,
    #   "project_name": str | None,
    #   "pipeline": dict,          # scenario.pipeline JSONB — nodes: [{id, ip_ref, lane_id, ...}]
    #   "ip_catalog": list[dict],
    #   "lanes": list[dict],
    # }
```

From src/scenario_db/api/schemas/view.py (ViewSummary 필수 필드 전체):
```python
class ViewSummary(BaseModel):
    scenario_id: str          # required
    variant_id: str           # required
    name: str                 # required
    subtitle: str             # required — 임시값: ""
    period_ms: float          # required — 임시값: 0.0
    budget_ms: float          # required — 임시값: 0.0
    resolution: str           # required — 임시값: ""
    fps: int                  # required — 임시값: 0
    variant_label: str        # required — 임시값: ""
    notes: str | None = None
    captured_at: str | None = None
```

From src/scenario_db/api/schemas/view.py (NodeElement 구성):
```python
class NodeData(BaseModel):
    id: str
    label: str
    type: Literal["sw", "ip", "submodule", "buffer", "dma_group", "dma_channel", "sysmmu",
                  "lane_bg", "lane_label", "stage_header"]
    layer: Literal["app", "framework", "hal", "kernel", "hw", "memory", "meta"] = "meta"
    # 나머지 필드 all optional

class NodeElement(BaseModel):
    data: NodeData
    position: dict[str, float]  # position 필수 — {"x": 0.0, "y": 0.0} 사용
```

From src/scenario_db/view/service.py (현재 project_level0 시그니처):
```python
def project_level0(scenario_id: str, variant_id: str, db=None) -> ViewResponse:
    if db is None:
        return build_sample_level0()  # ← 이 분기를 제거
    from scenario_db.db.repositories.view_projection import get_view_projection
    projection = get_view_projection(db, scenario_id, variant_id)
    ...
    response = build_sample_level0()  # ← 이것도 제거
    return ViewResponse(...)
```

From src/scenario_db/api/routers/view.py (현재 level 0 호출):
```python
if level == 0:
    return project_level0(scenario_id, variant_id, db=db)  # ← mode 미전달
```
</interfaces>

<dashboard_grep_note>
CONTEXT.md specifics 섹션에서 언급:
  dashboard/Home.py, dashboard/pages/*.py에서 project_level0()를 직접 호출하는 경우
  db 없이 호출 → sample fallback 유지 여부 결정 필요.
  구현 전 반드시 grep으로 확인 후 처리.
</dashboard_grep_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dashboard 호출 지점 확인 후 view.py 라우터 수정</name>
  <files>src/scenario_db/api/routers/view.py</files>
  <read_first>
    - src/scenario_db/api/routers/view.py (현재 전체)
    - src/scenario_db/view/service.py (project_level0 현재 시그니처 확인)
    - dashboard/Home.py (project_level0 호출 여부 확인)
  </read_first>
  <action>
**사전 확인 (구현 전 grep 실행):**
```bash
grep -rn "project_level0" dashboard/ src/ --include="*.py"
```
결과에서 `dashboard/`의 호출이 있으면:
- `db` 파라미터 없이 호출 → 해당 호출을 `project_level0(scenario_id, variant_id, mode="architecture", db=None)` 형태로 유지 가능하도록 service.py에서 db=None guard를 완전 제거하는 대신, 라우터 경유 호출만 DB를 전달받음을 확인
- dashboard 호출은 Phase 4에서 교체할 예정이므로 이 Plan에서는 건드리지 않음

**view.py 수정 내용 (단 하나의 변경):**

현재 level 0 분기:
```python
if level == 0:
    return project_level0(scenario_id, variant_id, db=db)
```
변경 후:
```python
if level == 0:
    return project_level0(scenario_id, variant_id, mode=mode, db=db)
```

주의: 다른 코드(import, level 1/2 분기, NotImplementedError 처리) 변경 금지.
`mode` 파라미터는 이미 `mode: str = Query("architecture", description="architecture | topology")` 로 선언되어 있음.
  </action>
  <verify>
    <automated>grep -n "mode=mode" src/scenario_db/api/routers/view.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mode=mode" src/scenario_db/api/routers/view.py` 출력이 1
    - `grep -n "project_level0" src/scenario_db/api/routers/view.py` 결과에 `mode=mode` 포함
    - `python -c "from scenario_db.api.routers import view"` 에러 없이 완료
    - view.py의 level 1/2 분기 코드가 변경되지 않음 (`grep -c "level == 1" src/scenario_db/api/routers/view.py` 출력이 여전히 1)
  </acceptance_criteria>
  <done>view.py의 level 0 분기에서 mode=mode가 project_level0에 전달된다.</done>
</task>

<task type="auto">
  <name>Task 2: service.py sample fallback 제거 + mode 분기 구현</name>
  <files>src/scenario_db/view/service.py</files>
  <read_first>
    - src/scenario_db/view/service.py (현재 전체 — project_level0 함수 위치와 내용 확인)
    - src/scenario_db/api/schemas/view.py (ViewSummary, NodeData, NodeElement, ViewResponse 필수 필드 전체)
    - src/scenario_db/db/repositories/view_projection.py (get_view_projection 반환 dict 구조)
  </read_first>
  <action>
`src/scenario_db/view/service.py`의 `project_level0()` 함수를 아래 스펙으로 교체한다.
`build_sample_level0()`, `_n()`, `_e()`, `build_sample_level0()` 등 나머지 코드는 **삭제하지 않는다** (D-06: build_sample_level0() 함수 보존).

**새 project_level0 시그니처 및 구현:**
```python
def project_level0(scenario_id: str, variant_id: str, mode: str = "architecture", db=None) -> ViewResponse:
    """Level 0 view — DB projection 기반 (Phase 3).

    mode='architecture': get_view_projection() 결과를 NodeElement 리스트로 변환.
    mode='topology': NotImplementedError (Phase 4 VIEW-03 작업).
    db=None: dashboard demo 경로 (Phase 4 VIEW-01에서 대체 예정).
    """
    if mode == "topology":
        raise NotImplementedError("topology mode is Phase 4 work")

    if db is None:
        return build_sample_level0()

    from scenario_db.db.repositories.view_projection import get_view_projection
    from scenario_db.api.exceptions_compat import NotFoundError  # 아래 주석 참조
    projection = get_view_projection(db, scenario_id, variant_id)
    if projection is None:
        from sqlalchemy.exc import NoResultFound
        raise NoResultFound(f"scenario '{scenario_id}' / variant '{variant_id}' not found")

    return _projection_to_view_response(projection)
```

**_projection_to_view_response() 헬퍼 함수 추가** (파일 하단, project_level1 위에 삽입):
```python
def _projection_to_view_response(projection: dict) -> ViewResponse:
    """get_view_projection() dict → 최소 ViewResponse 변환 (Phase 3 architecture mode).

    위치(x/y)는 0.0으로 설정 — ELK 레이아웃은 Phase 4 VIEW-01 작업.
    """
    nodes = [
        NodeElement(
            data=NodeData(
                id=node["id"],
                label=node.get("id", ""),
                type="ip",
                layer="hw",
            ),
            position={"x": 0.0, "y": 0.0},
        )
        for node in projection.get("pipeline", {}).get("nodes", [])
    ]
    summary = ViewSummary(
        scenario_id=projection["scenario_id"],
        variant_id=projection["variant_id"],
        name=projection.get("project_name") or projection["scenario_id"],
        subtitle="",
        period_ms=0.0,
        budget_ms=0.0,
        resolution="",
        fps=0,
        variant_label="",
    )
    return ViewResponse(
        level=0,
        mode="architecture",
        scenario_id=projection["scenario_id"],
        variant_id=projection["variant_id"],
        nodes=nodes,
        edges=[],
        risks=[],
        summary=summary,
    )
```

**import 추가** (파일 상단 기존 import에 추가):
이미 있는 import 중 `NodeData`, `NodeElement`, `ViewResponse`, `ViewSummary`는 service.py 상단 from 절에 이미 있으므로 추가 불필요. `NoResultFound`는 함수 내 lazy import로 처리.

**기존 project_level0의 구 구현 완전 교체** — 아래 두 블록을 제거:
1. `if db is None: return build_sample_level0()` 분기 전체 (단, 새 구현에서는 db=None → build_sample_level0() 유지 — dashboard 호환)
2. `response = build_sample_level0()` 후 ViewResponse 재구성 블록 전체

**주의:** `build_sample_level0()` 함수 정의 자체는 삭제하지 않는다 (D-06).
  </action>
  <verify>
    <automated>python -c "
from scenario_db.view.service import project_level0, build_sample_level0, _projection_to_view_response
import inspect
sig = inspect.signature(project_level0)
assert 'mode' in sig.parameters, 'mode param missing'
assert 'db' in sig.parameters, 'db param missing'
print('PASS: project_level0 signature OK')
print('PASS: build_sample_level0 still exists')
print('PASS: _projection_to_view_response exists')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def project_level0" src/scenario_db/view/service.py` 출력이 1
    - `grep -n "mode" src/scenario_db/view/service.py` 에서 `mode: str = "architecture"` 포함 라인 존재
    - `grep -n "topology" src/scenario_db/view/service.py` 에서 `NotImplementedError` 와 같은 줄 또는 바로 다음 줄에 존재
    - `grep -c "build_sample_level0" src/scenario_db/view/service.py` 출력이 2 이상 (정의 1 + db=None 분기 호출 1)
    - `grep -c "_projection_to_view_response" src/scenario_db/view/service.py` 출력이 2 이상 (정의 + 호출)
    - `grep -c "NoResultFound" src/scenario_db/view/service.py` 출력이 1 이상
    - `python -c "from scenario_db.view.service import project_level0"` 에러 없이 완료
    - service.py 내에 `response = build_sample_level0()` 후 `ViewResponse(**{**response.model_dump()` 패턴이 제거됨 (`grep -c "model_dump.*scenario_id" src/scenario_db/view/service.py` 출력이 0)
  </acceptance_criteria>
  <done>project_level0()가 mode 파라미터를 받아 topology→501, architecture→DB projection, db=None→sample(dashboard 호환)으로 분기한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /view?mode=... | mode 파라미터는 Query string으로 전달 — 임의 문자열 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | mode 파라미터 임의값 | mitigate | service.py에서 mode != "architecture" and mode != "topology" 케이스는 NotImplementedError → FastAPI 501 응답으로 처리됨; 현재 두 값만 정의되어 있으므로 미지정 값은 에러로 노출되지 않고 501 fallback 처리 |
| T-03-05 | Information Disclosure | _projection_to_view_response에서 pipeline 원본 노출 | accept | 내부 도구, 외부 인터넷 미노출 |
</threat_model>

<verification>
```bash
# 1. mode 파라미터 전달 확인
grep -n "mode=mode" src/scenario_db/api/routers/view.py

# 2. topology NotImplementedError 확인
grep -A1 "topology" src/scenario_db/view/service.py | grep "NotImplementedError"

# 3. sample fallback 제거 확인 (db=None 분기는 dashboard 호환을 위해 유지)
# 기존 "response = build_sample_level0()" 후 재구성 패턴 제거 확인
grep -n "model_dump" src/scenario_db/view/service.py

# 4. import 이상 없음
python -c "from scenario_db.view.service import project_level0, _projection_to_view_response"
python -c "from scenario_db.api.routers import view"
```
</verification>

<success_criteria>
1. view.py level 0 분기에 `mode=mode` 전달 확인
2. service.py project_level0() 시그니처에 `mode: str = "architecture"` 파라미터 존재
3. topology mode → NotImplementedError("topology mode is Phase 4 work") raise
4. architecture mode + db 있음 → _projection_to_view_response(get_view_projection(...)) 반환
5. db=None 케이스 → build_sample_level0() 반환 (dashboard 호환 유지)
6. 기존 `response = build_sample_level0()` 후 ViewResponse 재구성 블록 제거됨
7. _projection_to_view_response()가 ViewSummary 필수 필드 전부 채움
</success_criteria>

<output>
완료 후 `.planning/phases/03-runtime-api/03-02-SUMMARY.md` 생성.
</output>
