---
phase: 06-sim-package
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scenario_db/sim/bw_calc.py
  - src/scenario_db/sim/perf_calc.py
  - src/scenario_db/sim/power_calc.py
  - tests/sim/test_bw_calc.py
  - tests/sim/test_perf_calc.py
  - tests/sim/test_power_calc.py
autonomous: true
requirements:
  - SIM-02
  - SIM-03
  - SIM-09

must_haves:
  truths:
    - "calc_port_bw()가 FHD30 ISP WDMA_BE(NV12, disable) → 93.312 MB/s를 반환한다 (±1%)"
    - "calc_port_bw()가 FHD30 ISP RDMA_FE(BAYER, SBWC comp_ratio=0.5) → 202.68 MB/s를 반환한다 (±1%)"
    - "calc_port_bw()가 OTF 포트 타입(OTF_IN/OTF_OUT)에서 bw_mbs=0.0을 반환한다"
    - "calc_processing_time()이 ISP FHD30(set_clock=533MHz, ppc=4, h_blank=0.05) → ~1.021 ms를 반환한다"
    - "calc_active_power()가 ISP FHD30(unit=10.5, 1920x1080, voltage=780mV, fps=30) → ~26.28 mW를 반환한다"
  artifacts:
    - path: "src/scenario_db/sim/bw_calc.py"
      provides: "calc_port_bw(port, ip_name, port_type, fps, bw_power_coeff) -> PortBWResult"
      exports: ["calc_port_bw"]
    - path: "src/scenario_db/sim/perf_calc.py"
      provides: "calc_processing_time(pixels, set_clock_mhz, ppc, h_blank_margin) -> float"
      exports: ["calc_processing_time"]
    - path: "src/scenario_db/sim/power_calc.py"
      provides: "calc_active_power(unit_power_mw_mp, width, height, set_voltage_mv, fps) -> float"
      exports: ["calc_active_power"]
    - path: "tests/sim/test_bw_calc.py"
      provides: "Golden 값 assert (93.312, 202.68 MB/s) + OTF zero 테스트"
    - path: "tests/sim/test_perf_calc.py"
      provides: "처리시간 Golden 값 assert (~1.021 ms)"
    - path: "tests/sim/test_power_calc.py"
      provides: "V² 스케일링 Golden 값 assert (~26.28 mW)"
  key_links:
    - from: "src/scenario_db/sim/bw_calc.py"
      to: "src/scenario_db/sim/constants.py"
      via: "BPP_MAP import"
      pattern: "from scenario_db.sim.constants import BPP_MAP"
    - from: "src/scenario_db/sim/bw_calc.py"
      to: "src/scenario_db/models/capability/hw.py"
      via: "PortType enum import (OTF 판별)"
      pattern: "from scenario_db.models.capability.hw import PortType"
    - from: "src/scenario_db/sim/power_calc.py"
      to: "src/scenario_db/sim/constants.py"
      via: "REFERENCE_VOLTAGE_MV, REFERENCE_FPS import"
      pattern: "from scenario_db.sim.constants import REFERENCE"
---

<objective>
Wave 1 계산 함수: bw_calc / perf_calc / power_calc 순수 함수 구현 + Golden 값 단위 테스트

Purpose: PLAN-01 인프라(constants.py, models.py, conftest.py)를 기반으로 핵심 계산 함수 3개를 구현한다.
 이 함수들은 PLAN-03의 dvfs_resolver + runner가 직접 호출하는 계산 엔진이다.
Output: 3개 순수 계산 함수 (DB/ORM 의존 없음) + Golden 값 검증 단위 테스트 (PLAN-01과 병렬 실행)

Note: PLAN-02는 PLAN-01과 Wave 1에서 병렬 실행된다. 단, PLAN-02 실행 전에 PLAN-01 Task 1이
완료되어야 한다 (tests/sim/__init__.py, conftest.py가 존재해야 테스트 실행 가능).
실제 실행 순서: PLAN-01 완료 후 PLAN-02 실행 권장 (또는 독립 에이전트가 병렬 실행).
</objective>

<execution_context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.claude\worktrees\agent-ae815226d1f9ffda2
</execution_context>

<context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\ROADMAP.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\06-sim-package\06-CONTEXT.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\06-sim-package\06-RESEARCH.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\06-sim-package\06-01-SUMMARY.md

<interfaces>
<!-- PLAN-01에서 생성된 인터페이스. 이 PLAN 실행 전 06-01-SUMMARY.md로 확인. -->

From src/scenario_db/sim/constants.py (PLAN-01 생성):
```python
BPP_MAP: dict[str, float] = {
    "NV12": 1.5, "YUV420": 1.5, "RAW10": 1.25, "ARGB": 4.0, "BAYER": 1.0,
}
BW_POWER_COEFF_DEFAULT: float = 80.0
REFERENCE_VOLTAGE_MV: float = 710.0
REFERENCE_FPS: float = 30.0
```

From src/scenario_db/models/evidence/simulation.py (Phase 5 완성):
```python
class PortBWResult(BaseScenarioModel):
    ip: str
    port: str
    direction: Literal["read", "write"]
    bw_mbs: float
    bw_mbs_worst: float | None = None
    bw_power_mw: float
    format: str | None = None
    compression: str | None = None
    llc_enabled: bool = False
```

From src/scenario_db/models/capability/hw.py (Phase 5 완성):
```python
class PortType(StrEnum):
    DMA_READ  = "DMA_READ"
    DMA_WRITE = "DMA_WRITE"
    OTF_IN    = "OTF_IN"
    OTF_OUT   = "OTF_OUT"
```

From src/scenario_db/models/definition/usecase.py (Phase 5 완성):
```python
class PortInputConfig(BaseScenarioModel):
    port: str
    format: str
    bitwidth: int = 8
    width: int
    height: int
    compression: Literal["SBWC", "AFBC", "disable"] = "disable"
    comp_ratio: float = 1.0
    comp_ratio_min: float | None = None
    comp_ratio_max: float | None = None
    llc_enabled: bool = False
    llc_weight: float = 1.0
    r_w_rate: float = 1.0
```

<!-- Golden 값 계산 (설계 문서 §2.2 + planning_context 확인값) -->
<!-- FHD30 ISP WDMA_BE: 1.0 * 30 * 1920 * 1080 * (8/8) * 1.5 / 1e6 = 93.312 MB/s -->
<!-- FHD30 ISP RDMA_FE: 0.5 * 30 * 4000 * 2252 * (12/8) * 1.0 / 1e6 = 202.68 MB/s -->
<!-- ISP processing: 2073600 / (533e6 * 4) * 1.05 * 1000 = 1.021 ms -->
<!-- ISP power: 10.5 * (1920*1080/1e6) * (780/710)^2 * (30/30) = 26.28 mW -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bw_calc.py 구현 + Golden 값 단위 테스트</name>
  <read_first>
    - E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\src\scenario_db\models\evidence\simulation.py (PortBWResult 필드 확인)
    - E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\src\scenario_db\models\capability\hw.py (PortType enum)
    - E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\src\scenario_db\models\definition\usecase.py (PortInputConfig 필드)
  </read_first>
  <files>
    src/scenario_db/sim/bw_calc.py,
    tests/sim/test_bw_calc.py
  </files>
  <behavior>
    - test_fhd30_wdma_be_golden: fhd30_wdma_port(NV12, disable, 1920x1080, bitwidth=8, fps=30) → bw_mbs ≈ 93.312 (abs diff < 1.0)
    - test_fhd30_rdma_fe_golden: fhd30_rdma_port(BAYER, SBWC, comp_ratio=0.5, 4000x2252, bitwidth=12, fps=30) → bw_mbs ≈ 202.68 (abs diff < 2.0)
    - test_otf_in_returns_zero: OTF_IN 포트 → bw_mbs == 0.0, bw_power_mw == 0.0
    - test_otf_out_returns_zero: OTF_OUT 포트 → bw_mbs == 0.0
    - test_direction_dma_read: DMA_READ 포트 → direction == "read"
    - test_direction_dma_write: DMA_WRITE 포트 → direction == "write"
    - test_compression_disable_uses_ratio_1: compression="disable"이면 port.comp_ratio 무시하고 1.0 사용
    - test_llc_disabled_uses_weight_1: llc_enabled=False이면 llc_weight 무시하고 1.0 사용 (bw_power_mw에 영향)
    - test_bw_mbs_worst_when_comp_ratio_max_set: comp_ratio_max=0.8이면 bw_mbs_worst 반환
  </behavior>
  <action>
**테스트 먼저 작성 (TDD RED 단계):**

`tests/sim/test_bw_calc.py`:
```python
from __future__ import annotations

import pytest

from scenario_db.models.capability.hw import PortType
from scenario_db.models.definition.usecase import PortInputConfig
from scenario_db.sim.bw_calc import calc_port_bw


def test_fhd30_wdma_be_golden(fhd30_wdma_port: PortInputConfig) -> None:
    """Golden: 1.0 * 30 * 1920 * 1080 * (8/8) * 1.5 / 1e6 = 93.312 MB/s."""
    result = calc_port_bw(
        port=fhd30_wdma_port,
        ip_name="ISP",
        port_type=PortType.DMA_WRITE,
        fps=30.0,
    )
    assert abs(result.bw_mbs - 93.312) / 93.312 < 0.01, (
        f"Expected ~93.312 MB/s, got {result.bw_mbs}"
    )
    assert result.direction == "write"
    assert result.ip == "ISP"
    assert result.port == "WDMA_BE"
    assert result.format == "NV12"
    assert result.compression == "disable"
    assert result.llc_enabled is False


def test_fhd30_rdma_fe_golden(fhd30_rdma_port: PortInputConfig) -> None:
    """Golden: 0.5 * 30 * 4000 * 2252 * (12/8) * 1.0 / 1e6 = 202.68 MB/s."""
    result = calc_port_bw(
        port=fhd30_rdma_port,
        ip_name="ISP",
        port_type=PortType.DMA_READ,
        fps=30.0,
    )
    assert abs(result.bw_mbs - 202.68) / 202.68 < 0.01, (
        f"Expected ~202.68 MB/s, got {result.bw_mbs}"
    )
    assert result.direction == "read"
    assert result.compression == "SBWC"


def test_otf_in_returns_zero(fhd30_wdma_port: PortInputConfig) -> None:
    """OTF_IN 포트는 DRAM 액세스 없음 — bw_mbs=0."""
    result = calc_port_bw(
        port=fhd30_wdma_port,
        ip_name="ISP",
        port_type=PortType.OTF_IN,
        fps=30.0,
    )
    assert result.bw_mbs == 0.0
    assert result.bw_power_mw == 0.0


def test_otf_out_returns_zero(fhd30_wdma_port: PortInputConfig) -> None:
    result = calc_port_bw(
        port=fhd30_wdma_port,
        ip_name="ISP",
        port_type=PortType.OTF_OUT,
        fps=30.0,
    )
    assert result.bw_mbs == 0.0
    assert result.bw_power_mw == 0.0


def test_direction_dma_read() -> None:
    port = PortInputConfig(port="RDMA", format="NV12", width=1920, height=1080)
    result = calc_port_bw(port=port, ip_name="ISP", port_type=PortType.DMA_READ, fps=30.0)
    assert result.direction == "read"


def test_direction_dma_write() -> None:
    port = PortInputConfig(port="WDMA", format="NV12", width=1920, height=1080)
    result = calc_port_bw(port=port, ip_name="ISP", port_type=PortType.DMA_WRITE, fps=30.0)
    assert result.direction == "write"


def test_compression_disable_uses_ratio_1() -> None:
    """compression=disable이면 comp_ratio=0.5 설정돼도 1.0 사용."""
    port_compress = PortInputConfig(
        port="WDMA", format="NV12", width=1920, height=1080,
        compression="disable", comp_ratio=0.5,
    )
    port_no_compress = PortInputConfig(
        port="WDMA", format="NV12", width=1920, height=1080,
        compression="disable", comp_ratio=1.0,
    )
    r1 = calc_port_bw(port=port_compress, ip_name="ISP", port_type=PortType.DMA_WRITE, fps=30.0)
    r2 = calc_port_bw(port=port_no_compress, ip_name="ISP", port_type=PortType.DMA_WRITE, fps=30.0)
    assert abs(r1.bw_mbs - r2.bw_mbs) < 0.001, "disable compression이면 comp_ratio 무시해야 함"


def test_llc_disabled_uses_weight_1() -> None:
    """llc_enabled=False이면 llc_weight 무시 (bw_power에 영향 없음)."""
    port = PortInputConfig(
        port="WDMA", format="NV12", width=1920, height=1080,
        llc_enabled=False, llc_weight=0.3,
    )
    result = calc_port_bw(port=port, ip_name="ISP", port_type=PortType.DMA_WRITE, fps=30.0)
    # llc_enabled=False → weight=1.0 적용, weight=0.3 아님
    expected_power = result.bw_mbs * 80.0 / 1000.0 * 1.0
    assert abs(result.bw_power_mw - expected_power) < 0.001


def test_bw_mbs_worst_when_comp_ratio_max_set() -> None:
    """comp_ratio_max 설정 시 bw_mbs_worst 계산."""
    port = PortInputConfig(
        port="RDMA", format="NV12", width=1920, height=1080,
        compression="SBWC", comp_ratio=0.5, comp_ratio_max=0.8,
    )
    result = calc_port_bw(port=port, ip_name="ISP", port_type=PortType.DMA_READ, fps=30.0)
    assert result.bw_mbs_worst is not None
    assert result.bw_mbs_worst > result.bw_mbs  # worst > nominal
```

**구현 (TDD GREEN 단계):**

`src/scenario_db/sim/bw_calc.py`:
```python
from __future__ import annotations

from scenario_db.models.capability.hw import PortType
from scenario_db.models.definition.usecase import PortInputConfig
from scenario_db.models.evidence.simulation import PortBWResult
from scenario_db.sim.constants import BPP_MAP, BW_POWER_COEFF_DEFAULT


def calc_port_bw(
    port: PortInputConfig,
    ip_name: str,
    port_type: PortType,
    fps: float,
    bw_power_coeff: float = BW_POWER_COEFF_DEFAULT,
) -> PortBWResult:
    """DMA 포트 BW(MB/s) + 전력(mW) 계산.

    OTF 포트(OTF_IN/OTF_OUT)는 DRAM 액세스 없으므로 bw_mbs=0 반환.

    BW 공식 (설계 문서 §2.2):
        bw_mbs = comp_ratio * fps * width * height * (bitwidth/8) * BPP / 1e6
        bw_power_mw = bw_mbs * bw_power_coeff / 1000 * llc_weight

    주의사항:
    - compression="disable"이면 port.comp_ratio 무시하고 1.0 사용 (Pitfall 4)
    - llc_enabled=False이면 port.llc_weight 무시하고 1.0 사용
    - PortType enum으로 판별 (문자열 비교 금지 — RESEARCH.md Anti-Patterns)
    """
    # OTF 포트는 DRAM 액세스 없음
    if port_type in (PortType.OTF_IN, PortType.OTF_OUT):
        return PortBWResult(
            ip=ip_name,
            port=port.port,
            direction="read",   # OTF는 방향 무관 — Literal 제약 충족용
            bw_mbs=0.0,
            bw_power_mw=0.0,
        )

    bpp = BPP_MAP.get(port.format, 1.0)
    # compression="disable"이면 comp_ratio 무시 (Pitfall 4)
    comp_ratio = port.comp_ratio if port.compression != "disable" else 1.0
    # llc_enabled=False이면 llc_weight 무시
    llc_weight = port.llc_weight if port.llc_enabled else 1.0

    bw_mbs: float = (
        comp_ratio * fps * port.width * port.height * (port.bitwidth / 8) * bpp / 1e6
    )
    bw_power_mw: float = bw_mbs * bw_power_coeff / 1000.0 * llc_weight

    # worst-case BW (comp_ratio_max 설정 시)
    bw_mbs_worst: float | None = None
    if port.comp_ratio_max is not None and port.compression != "disable":
        bw_mbs_worst = (
            port.comp_ratio_max
            * fps * port.width * port.height * (port.bitwidth / 8) * bpp / 1e6
        )

    direction = "read" if port_type == PortType.DMA_READ else "write"

    return PortBWResult(
        ip=ip_name,
        port=port.port,
        direction=direction,
        bw_mbs=bw_mbs,
        bw_mbs_worst=bw_mbs_worst,
        bw_power_mw=bw_power_mw,
        format=port.format,
        compression=port.compression,
        llc_enabled=port.llc_enabled,
    )
```
  </action>
  <verify>
    <automated>cd E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB && uv run pytest tests/sim/test_bw_calc.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/sim/test_bw_calc.py -v` → 전체 PASSED (실패 0개)
    - test_fhd30_wdma_be_golden 통과: abs(result.bw_mbs - 93.312) / 93.312 < 0.01
    - test_fhd30_rdma_fe_golden 통과: abs(result.bw_mbs - 202.68) / 202.68 < 0.01
    - test_otf_in_returns_zero + test_otf_out_returns_zero 통과
    - test_compression_disable_uses_ratio_1 통과 (Pitfall 4 방지 확인)
    - `grep -n "port_type in (PortType.OTF_IN, PortType.OTF_OUT)" src/scenario_db/sim/bw_calc.py` → 매칭 존재 (문자열 비교 금지 확인)
    - `grep -n "compression != .disable." src/scenario_db/sim/bw_calc.py` → 매칭 존재
  </acceptance_criteria>
  <done>bw_calc.py 구현 완료, tests/sim/test_bw_calc.py 전체 PASSED</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: perf_calc.py + power_calc.py 구현 + Golden 값 단위 테스트</name>
  <read_first>
    - E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\src\scenario_db\sim\bw_calc.py (동일 순수 함수 패턴 확인)
  </read_first>
  <files>
    src/scenario_db/sim/perf_calc.py,
    src/scenario_db/sim/power_calc.py,
    tests/sim/test_perf_calc.py,
    tests/sim/test_power_calc.py
  </files>
  <behavior>
    - test_isp_fhd30_processing_time: pixels=2073600, set_clock=533MHz, ppc=4, h_blank=0.05 → abs(result - 1.021) < 0.05
    - test_processing_time_formula: h_blank_margin=0 이면 pixels/(set_clock_mhz*1e6*ppc)*1000
    - test_feasible_within_frame_interval: hw_time_ms=1.021 < 33.33 (1000/30) → feasible True
    - test_isp_fhd30_active_power: unit=10.5, width=1920, height=1080, voltage=780mV, fps=30 → abs(result - 26.28) < 0.5
    - test_power_voltage_scaling: voltage=710mV (기준전압)이면 스케일링 팩터 1.0
    - test_power_fps_scaling: fps=15일 때 결과가 fps=30의 절반
  </behavior>
  <action>
**테스트 먼저 작성 (TDD RED 단계):**

`tests/sim/test_perf_calc.py`:
```python
from __future__ import annotations

from scenario_db.sim.perf_calc import calc_processing_time


def test_isp_fhd30_processing_time() -> None:
    """Golden: 2073600 / (533e6 * 4) * 1.05 * 1000 = 1.021 ms."""
    pixels = 1920 * 1080  # = 2073600
    result = calc_processing_time(
        pixels=pixels,
        set_clock_mhz=533.0,
        ppc=4.0,
        h_blank_margin=0.05,
    )
    assert abs(result - 1.021) < 0.05, f"Expected ~1.021 ms, got {result}"


def test_processing_time_no_blank() -> None:
    """h_blank_margin=0 이면 순수 픽셀/클럭 시간."""
    pixels = 1000000
    clock_mhz = 500.0
    ppc = 4.0
    result = calc_processing_time(pixels=pixels, set_clock_mhz=clock_mhz, ppc=ppc, h_blank_margin=0.0)
    expected = pixels / (clock_mhz * 1e6 * ppc) * 1000.0  # ms
    assert abs(result - expected) < 1e-9


def test_processing_time_feasibility() -> None:
    """FHD30: hw_time=1.021ms < frame_interval=33.33ms → feasible."""
    hw_time_ms = calc_processing_time(
        pixels=1920 * 1080, set_clock_mhz=533.0, ppc=4.0, h_blank_margin=0.05
    )
    frame_interval_ms = 1000.0 / 30.0  # 33.33 ms
    assert hw_time_ms < frame_interval_ms, f"hw_time={hw_time_ms} should be < {frame_interval_ms}"
```

`tests/sim/test_power_calc.py`:
```python
from __future__ import annotations

from scenario_db.sim.power_calc import calc_active_power


def test_isp_fhd30_active_power() -> None:
    """Golden: 10.5 * (1920*1080/1e6) * (780/710)^2 * (30/30) = 26.28 mW."""
    result = calc_active_power(
        unit_power_mw_mp=10.5,
        width=1920,
        height=1080,
        set_voltage_mv=780.0,
        fps=30.0,
    )
    assert abs(result - 26.28) < 0.5, f"Expected ~26.28 mW, got {result}"


def test_power_reference_voltage_scaling() -> None:
    """set_voltage=710mV (REFERENCE_VOLTAGE_MV) → voltage_scale=1.0."""
    result_ref = calc_active_power(
        unit_power_mw_mp=10.0, width=1000, height=1000,
        set_voltage_mv=710.0, fps=30.0,
    )
    expected = 10.0 * (1000 * 1000 / 1e6) * 1.0 * 1.0  # = 10.0 mW
    assert abs(result_ref - expected) < 0.001


def test_power_fps_scaling_half() -> None:
    """fps를 절반으로 줄이면 power도 절반."""
    result_30 = calc_active_power(
        unit_power_mw_mp=10.0, width=1920, height=1080,
        set_voltage_mv=710.0, fps=30.0,
    )
    result_15 = calc_active_power(
        unit_power_mw_mp=10.0, width=1920, height=1080,
        set_voltage_mv=710.0, fps=15.0,
    )
    assert abs(result_15 - result_30 / 2.0) < 0.001


def test_power_voltage_squared_scaling() -> None:
    """전압 2배 → power 4배 (V² 스케일링)."""
    result_710 = calc_active_power(
        unit_power_mw_mp=1.0, width=1000, height=1000,
        set_voltage_mv=710.0, fps=30.0,
    )
    result_1420 = calc_active_power(
        unit_power_mw_mp=1.0, width=1000, height=1000,
        set_voltage_mv=1420.0, fps=30.0,
    )
    # (1420/710)^2 = 4.0
    assert abs(result_1420 / result_710 - 4.0) < 0.001
```

**구현 (TDD GREEN 단계):**

`src/scenario_db/sim/perf_calc.py`:
```python
from __future__ import annotations


def calc_processing_time(
    pixels: int,
    set_clock_mhz: float,
    ppc: float,
    h_blank_margin: float = 0.05,
) -> float:
    """IP 처리시간(ms) 계산.

    공식 (설계 문서 §2.2):
        hw_time_ms = pixels / (set_clock_mhz * 1e6 * ppc) * (1 + h_blank_margin) * 1000

    Args:
        pixels: 처리할 픽셀 수 (width * height), int 타입
        set_clock_mhz: DVFS로 결정된 IP 클럭 (MHz), float
        ppc: pixels-per-clock (IP 처리 폭), float
        h_blank_margin: 수평 블랭킹 여유 비율 (default 0.05 = 5%)
                        SimGlobalConfig.h_blank_margin 값을 전달

    Returns:
        처리시간 (ms), float. feasible 판정: hw_time_ms <= (1000.0 / fps)
    """
    return pixels / (set_clock_mhz * 1e6 * ppc) * (1.0 + h_blank_margin) * 1000.0
```

`src/scenario_db/sim/power_calc.py`:
```python
from __future__ import annotations

from scenario_db.sim.constants import REFERENCE_FPS, REFERENCE_VOLTAGE_MV


def calc_active_power(
    unit_power_mw_mp: float,
    width: int,
    height: int,
    set_voltage_mv: float,
    fps: float,
) -> float:
    """IP Active Power(mW) 계산.

    공식 (설계 문서 §2.2):
        power = unit_power_mw_mp * resolution_MP * (set_voltage_mv / 710.0)^2 * (fps / 30.0)

    Args:
        unit_power_mw_mp: IP 단위 전력 [mW/MP] (IPSimParams.unit_power_mw_mp)
        width: 처리 해상도 너비 (픽셀)
        height: 처리 해상도 높이 (픽셀)
        set_voltage_mv: DVFS 결정 전압 (mV)
        fps: 목표 프레임레이트

    Returns:
        Active power (mW), float
    """
    resolution_mp: float = (width * height) / 1e6
    voltage_scale: float = (set_voltage_mv / REFERENCE_VOLTAGE_MV) ** 2
    fps_scale: float = fps / REFERENCE_FPS
    return unit_power_mw_mp * resolution_mp * voltage_scale * fps_scale
```
  </action>
  <verify>
    <automated>cd E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB && uv run pytest tests/sim/test_perf_calc.py tests/sim/test_power_calc.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/sim/test_perf_calc.py tests/sim/test_power_calc.py -v` → 전체 PASSED
    - test_isp_fhd30_processing_time 통과: abs(result - 1.021) < 0.05
    - test_isp_fhd30_active_power 통과: abs(result - 26.28) < 0.5
    - test_power_voltage_squared_scaling 통과: (1420/710)^2 = 4.0 배율 확인
    - test_power_fps_scaling_half 통과: fps=15 → fps=30의 절반
    - `grep -n "REFERENCE_VOLTAGE_MV" src/scenario_db/sim/power_calc.py` → 매칭 존재 (하드코딩 금지)
    - `grep -n "REFERENCE_FPS" src/scenario_db/sim/power_calc.py` → 매칭 존재
  </acceptance_criteria>
  <done>perf_calc.py + power_calc.py 구현 완료, 6개 단위 테스트 전체 PASSED</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 함수 입력 → 계산 엔진 | PortInputConfig는 Pydantic이 검증하지만 계산 함수 자체는 type hint만 사용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | calc_port_bw() 입력 | accept | Pydantic PortInputConfig의 Literal["SBWC","AFBC","disable"] 제약으로 compression 값 통제, 계산 함수는 순수 함수 |
| T-06-05 | Tampering | BPP_MAP.get() fallback | mitigate | 알 수 없는 format → bpp=1.0 fallback. 계산 결과는 보수적으로 처리됨. 로그 없이 silent이므로 주요 format은 BPP_MAP에 사전 등록 권장 |
</threat_model>

<verification>
```bash
# Wave 1 전체 sim 테스트
cd E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB
uv run pytest tests/sim/ -v

# 기존 테스트 회귀 없음 (SIM-09)
uv run pytest tests/unit/ -q

# 계산 함수 직접 검증
uv run python -c "
from scenario_db.models.definition.usecase import PortInputConfig
from scenario_db.models.capability.hw import PortType
from scenario_db.sim.bw_calc import calc_port_bw
from scenario_db.sim.perf_calc import calc_processing_time
from scenario_db.sim.power_calc import calc_active_power

# Golden 값 검증
port = PortInputConfig(port='WDMA_BE', format='NV12', bitwidth=8, width=1920, height=1080, compression='disable')
r = calc_port_bw(port=port, ip_name='ISP', port_type=PortType.DMA_WRITE, fps=30.0)
print(f'WDMA_BE BW: {r.bw_mbs:.3f} MB/s (golden=93.312)')

hw_time = calc_processing_time(pixels=1920*1080, set_clock_mhz=533.0, ppc=4.0, h_blank_margin=0.05)
print(f'ISP hw_time: {hw_time:.3f} ms (golden=1.021)')

power = calc_active_power(unit_power_mw_mp=10.5, width=1920, height=1080, set_voltage_mv=780.0, fps=30.0)
print(f'ISP power: {power:.2f} mW (golden=26.28)')
"
```
</verification>

<success_criteria>
1. `uv run pytest tests/sim/test_bw_calc.py tests/sim/test_perf_calc.py tests/sim/test_power_calc.py -v` → 전체 PASSED
2. FHD30 ISP WDMA_BE BW = 93.312 MB/s (±1%) 검증 통과
3. FHD30 ISP RDMA_FE BW = 202.68 MB/s (±1%) 검증 통과
4. OTF 포트 bw_mbs=0 검증 통과
5. ISP 처리시간 = 1.021 ms (±0.05 ms) 검증 통과
6. ISP Active Power = 26.28 mW (±0.5 mW) 검증 통과
7. `uv run pytest tests/unit/ -q` 기존 테스트 회귀 없음
</success_criteria>

<output>
After completion, create `.planning/phases/06-sim-package/06-02-SUMMARY.md`

Summary must include:
- Files created (exact paths)
- Golden 값 실제 계산 결과 (BW/처리시간/Power — 테스트 출력값)
- Test results (test count, all passed)
- Any edge cases discovered during implementation
</output>
