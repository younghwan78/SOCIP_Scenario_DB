---
phase: 06-sim-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scenario_db/config.py
  - hw_config/dvfs-projectA.yaml
  - src/scenario_db/sim/__init__.py
  - src/scenario_db/sim/constants.py
  - src/scenario_db/sim/models.py
  - tests/sim/__init__.py
  - tests/sim/conftest.py
  - pyproject.toml
autonomous: true
requirements:
  - SIM-01
  - SIM-06
  - SIM-07

must_haves:
  truths:
    - "sim/ 패키지가 import 가능하다 (`from scenario_db.sim.models import SimRunResult`)"
    - "constants.py의 BPP_MAP에 NV12/RAW10/ARGB/BAYER/YUV420 5개 키가 정의된다"
    - "sim/models.py의 4개 신규 모델(ResolvedIPConfig, DVFSLevel, DVFSTable, SimRunResult)이 round-trip 직렬화를 통과한다"
    - "config.py에 DVFS_CONFIG_PATH = Path('hw_config/dvfs-projectA.yaml') 상수가 추가된다"
    - "hw_config/dvfs-projectA.yaml이 CAM/INT/MIF 3개 도메인을 정의한다"
    - "tests/sim/conftest.py가 YAML 파일 의존 없이 인라인 픽스처를 제공한다"
    - "pyproject.toml testpaths에 'tests/sim'이 추가된다"
  artifacts:
    - path: "src/scenario_db/sim/__init__.py"
      provides: "sim 패키지 마커"
    - path: "src/scenario_db/sim/constants.py"
      provides: "BPP_MAP, BW_POWER_COEFF_DEFAULT, REFERENCE_VOLTAGE_MV, REFERENCE_FPS"
      contains: "BPP_MAP"
    - path: "src/scenario_db/sim/models.py"
      provides: "ResolvedIPConfig, DVFSLevel, DVFSTable, SimRunResult (신규 4개) + PortBWResult, IPTimingResult (re-import)"
      exports: ["ResolvedIPConfig", "DVFSLevel", "DVFSTable", "SimRunResult"]
    - path: "src/scenario_db/config.py"
      provides: "DVFS_CONFIG_PATH Path 상수"
      contains: "DVFS_CONFIG_PATH"
    - path: "hw_config/dvfs-projectA.yaml"
      provides: "CAM/INT/MIF DVFS 도메인 테이블"
      contains: "dvfs_tables"
    - path: "tests/sim/conftest.py"
      provides: "isp_sim_params, cam_dvfs_table, fhd30_wdma_port, fhd30_rdma_port 픽스처"
    - path: "pyproject.toml"
      provides: "tests/sim testpaths 등록"
      contains: "tests/sim"
  key_links:
    - from: "src/scenario_db/sim/models.py"
      to: "src/scenario_db/models/evidence/simulation.py"
      via: "re-import (D-01 단일 정의 원칙)"
      pattern: "from scenario_db.models.evidence.simulation import"
    - from: "src/scenario_db/sim/models.py"
      to: "src/scenario_db/models/common.py"
      via: "BaseScenarioModel 상속"
      pattern: "BaseScenarioModel"
---

<objective>
Wave 1 인프라: sim/ 패키지 뼈대 + 상수 + Pydantic 모델 + 픽스처 설정

Purpose: Wave 2 계산 모듈(bw_calc/perf_calc/power_calc)과 Wave 3 통합 모듈이 의존하는 기반을 확립한다.
 모든 import 경로, 상수 값, Pydantic 모델 계약이 이 플랜에서 확정된다.
Output: sim/ 패키지 import 가능 상태 + constants.py + models.py (round-trip 테스트 통과) + 픽스처 + DVFS YAML
</objective>

<execution_context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.claude\worktrees\agent-ae815226d1f9ffda2
</execution_context>

<context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\ROADMAP.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\06-sim-package\06-CONTEXT.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\06-sim-package\06-RESEARCH.md

<interfaces>
<!-- Phase 5에서 완성된 소비 대상 모델. 재정의 금지 — re-import만 허용. -->

From src/scenario_db/models/evidence/simulation.py:
```python
class PortBWResult(BaseScenarioModel):
    ip: str
    port: str
    direction: Literal["read", "write"]
    bw_mbs: float
    bw_mbs_worst: float | None = None
    bw_power_mw: float
    format: str | None = None
    compression: str | None = None
    llc_enabled: bool = False

class IPTimingResult(BaseScenarioModel):
    ip: str
    hw_time_ms: float
    required_clock_mhz: float
    set_clock_mhz: float
    set_voltage_mv: float
    feasible: bool
```

From src/scenario_db/models/common.py:
```python
class BaseScenarioModel(BaseModel):
    model_config = ConfigDict(extra="forbid")
```

From src/scenario_db/models/capability/hw.py:
```python
class PortType(StrEnum):
    DMA_READ  = "DMA_READ"
    DMA_WRITE = "DMA_WRITE"
    OTF_IN    = "OTF_IN"
    OTF_OUT   = "OTF_OUT"

class PortSpec(BaseScenarioModel):
    name: str
    type: PortType
    max_bw_gbps: float | None = None

class IPSimParams(BaseScenarioModel):
    hw_name_in_sim: str
    ppc: float
    unit_power_mw_mp: float
    idc: float = 0.0
    vdd: str
    dvfs_group: str
    latency_us: float = 0.0
    ports: list[PortSpec] = Field(default_factory=list)
```

From src/scenario_db/models/definition/usecase.py:
```python
class PortInputConfig(BaseScenarioModel):
    port: str
    format: str
    bitwidth: int = 8
    width: int
    height: int
    compression: Literal["SBWC", "AFBC", "disable"] = "disable"
    comp_ratio: float = 1.0
    comp_ratio_min: float | None = None
    comp_ratio_max: float | None = None
    llc_enabled: bool = False
    llc_weight: float = 1.0
    r_w_rate: float = 1.0

class SimGlobalConfig(BaseScenarioModel):
    asv_group: int = 4
    sw_margin: float = 0.25
    bw_power_coeff: float = 80.0
    vbat: float = 4.0
    pmic_eff: float = 0.85
    h_blank_margin: float = 0.05
    dvfs_overrides: dict[str, int] = Field(default_factory=dict)

class SensorSpec(BaseScenarioModel):  # usecase.py에 이미 존재
    # v_valid_ratio: OTF 그룹 타이밍 제약에 사용
    ...
```

From src/scenario_db/config.py (현재 파일 끝):
```python
@lru_cache(maxsize=None)
def get_settings() -> Settings:
    """Settings 싱글톤 — lru_cache로 캐싱."""
    return Settings()
# 파일 끝 — 여기에 DVFS_CONFIG_PATH 추가
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 인프라 파일 생성 (config.py 수정, hw_config/ 디렉토리, sim/ 패키지 마커, pyproject.toml)</name>
  <read_first>
    - E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\src\scenario_db\config.py (전체 — DVFS_CONFIG_PATH 추가 위치 확인)
    - E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\pyproject.toml (전체 — testpaths 수정 위치 확인)
  </read_first>
  <files>
    src/scenario_db/config.py,
    hw_config/dvfs-projectA.yaml,
    src/scenario_db/sim/__init__.py,
    tests/sim/__init__.py,
    pyproject.toml
  </files>
  <action>
**1. config.py 수정** — 기존 파일 끝에 2줄 추가:
```python
from pathlib import Path

# DVFS 설정 파일 경로 — production 로드 경로. 테스트는 dvfs_tables dict 직접 주입.
DVFS_CONFIG_PATH: Path = Path("hw_config/dvfs-projectA.yaml")
```
Settings 클래스 내부에 추가하지 말 것 (환경변수 불필요).

**2. hw_config/ 디렉토리 생성 + dvfs-projectA.yaml 작성:**
```yaml
# hw_config/dvfs-projectA.yaml
# ProjectA DVFS 테이블 — CAM/INT/MIF 도메인
# 설계 문서 §7.3 기준
dvfs_tables:
  CAM:
    - level: 0
      speed_mhz: 600
      voltages: {0: 820, 4: 780, 8: 750}
    - level: 1
      speed_mhz: 533
      voltages: {0: 760, 4: 720, 8: 700}
    - level: 2
      speed_mhz: 400
      voltages: {0: 700, 4: 660, 8: 630}
  INT:
    - level: 0
      speed_mhz: 533
      voltages: {0: 800, 4: 760, 8: 730}
    - level: 1
      speed_mhz: 333
      voltages: {0: 730, 4: 690, 8: 660}
  MIF:
    - level: 0
      speed_mhz: 3200
      voltages: {0: 750, 4: 730, 8: 710}
    - level: 1
      speed_mhz: 2133
      voltages: {0: 700, 4: 680, 8: 660}
```

**3. sim/__init__.py 생성** — 빈 파일 (패키지 마커):
```python
# src/scenario_db/sim/__init__.py
```

**4. tests/sim/__init__.py 생성** — 빈 파일:
```python
# tests/sim/__init__.py
```

**5. pyproject.toml 수정** — testpaths에 "tests/sim" 추가:
```toml
[tool.pytest.ini_options]
testpaths = ["tests/unit", "tests/sim"]
markers = [
    "unit: pure Python, no external deps",
    "integration: requires Docker + PostgreSQL testcontainer",
]
```
  </action>
  <verify>
    <automated>cd E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB && python -c "from scenario_db.config import DVFS_CONFIG_PATH; print(DVFS_CONFIG_PATH)"</automated>
  </verify>
  <acceptance_criteria>
    - config.py 마지막 줄에 `DVFS_CONFIG_PATH: Path = Path("hw_config/dvfs-projectA.yaml")` 존재: `grep -n "DVFS_CONFIG_PATH" src/scenario_db/config.py`
    - pyproject.toml testpaths에 `"tests/sim"` 포함: `grep "tests/sim" pyproject.toml`
    - hw_config/dvfs-projectA.yaml에 CAM/INT/MIF 3개 도메인 존재: `grep "CAM:\|INT:\|MIF:" hw_config/dvfs-projectA.yaml | wc -l` → 3
    - `from scenario_db.config import DVFS_CONFIG_PATH` import 성공
    - `from scenario_db.sim import *` import 성공 (빈 패키지)
  </acceptance_criteria>
  <done>config.py에 DVFS_CONFIG_PATH 추가 완료, dvfs-projectA.yaml 생성, sim/__init__.py + tests/sim/__init__.py 생성, pyproject.toml testpaths 업데이트 완료</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sim/constants.py + sim/models.py 구현 + round-trip 테스트</name>
  <read_first>
    - E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\src\scenario_db\models\common.py (BaseScenarioModel 패턴)
    - E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\src\scenario_db\models\evidence\simulation.py (PortBWResult, IPTimingResult — re-import 대상)
  </read_first>
  <files>
    src/scenario_db/sim/constants.py,
    src/scenario_db/sim/models.py,
    tests/sim/conftest.py,
    tests/sim/test_constants.py,
    tests/sim/test_models.py
  </files>
  <behavior>
    - test_bpp_map_values: BPP_MAP["NV12"] == 1.5, BPP_MAP["YUV420"] == 1.5, BPP_MAP["RAW10"] == 1.25, BPP_MAP["ARGB"] == 4.0, BPP_MAP["BAYER"] == 1.0
    - test_reference_constants: REFERENCE_VOLTAGE_MV == 710.0, REFERENCE_FPS == 30.0, BW_POWER_COEFF_DEFAULT == 80.0
    - test_dvfs_level_roundtrip: DVFSLevel(level=0, speed_mhz=600, voltages={0: 820, 4: 780}) → model_dump → model_validate → 동일 객체
    - test_dvfs_table_roundtrip: DVFSTable(domain="CAM", levels=[...]) round-trip 통과
    - test_resolved_ip_config_roundtrip: ResolvedIPConfig 필드 전체 round-trip
    - test_sim_run_result_roundtrip: SimRunResult 생성 + model_dump + model_validate 동일
    - test_port_bw_result_reimport: `from scenario_db.sim.models import PortBWResult` 가 evidence.simulation.PortBWResult와 동일 클래스
  </behavior>
  <action>
**constants.py 생성:**
```python
from __future__ import annotations

# ---------------------------------------------------------------------------
# BPP Map: samples-per-pixel (bitwidth는 별도 인수로 bit count 담당)
# 출처: docs/simulation-engine-integration.md §6.2 + SimEngine constants.py 검증
# ---------------------------------------------------------------------------
BPP_MAP: dict[str, float] = {
    "NV12":   1.5,    # YUV420 semi-planar (1 Y + 0.5 UV per pixel)
    "YUV420": 1.5,    # NV12 동일
    "RAW10":  1.25,   # Bayer 10-bit packed (10/8 samples per byte)
    "ARGB":   4.0,    # 32-bit RGBA (4 bytes per pixel)
    "BAYER":  1.0,    # 1 sample/pixel; bitwidth 인수가 bit count 담당 (예: bitwidth=12)
}

BW_POWER_COEFF_DEFAULT: float = 80.0   # mW/(GB/s) — DRAM BW당 전력 계수
REFERENCE_VOLTAGE_MV: float = 710.0    # 0.71V — power V² 스케일링 기준 전압
REFERENCE_FPS: float = 30.0            # 30fps — power fps 스케일링 기준
```

**models.py 생성 (D-01: PortBWResult/IPTimingResult는 re-import, 재정의 없음):**
```python
from __future__ import annotations

from pydantic import Field

from scenario_db.models.common import BaseScenarioModel

# D-01: 단일 정의 원칙 — evidence layer 모델을 복제하면 Phase 7 타입 불일치
from scenario_db.models.evidence.simulation import IPTimingResult, PortBWResult

__all__ = [
    "PortBWResult",       # re-export
    "IPTimingResult",     # re-export
    "ResolvedIPConfig",
    "DVFSLevel",
    "DVFSTable",
    "SimRunResult",
]


class ResolvedIPConfig(BaseScenarioModel):
    """DVFS resolve 결과 — 단일 IP의 최종 클럭/전압 설정."""
    ip_name: str
    required_clock_mhz: float
    set_clock_mhz: float
    set_voltage_mv: float
    dvfs_group: str
    vdd: str


class DVFSLevel(BaseScenarioModel):
    """DVFS 테이블의 단일 레벨 엔트리."""
    level: int
    speed_mhz: float
    voltages: dict[int, float]   # asv_group -> voltage_mv (예: {0: 820, 4: 780, 8: 750})


class DVFSTable(BaseScenarioModel):
    """단일 VDD 도메인의 DVFS 레벨 목록."""
    domain: str
    levels: list[DVFSLevel] = Field(default_factory=list)

    def find_min_level(self, required_clock_mhz: float, asv_group: int = 4) -> DVFSLevel | None:
        """required_clock_mhz 이상을 처리할 수 있는 최소(가장 낮은 speed) 레벨 반환.
        levels는 speed_mhz 내림차순 정렬 가정 (level 0 = 최고속).
        required를 충족하는 가장 낮은 speed 레벨 = 가장 높은 level 번호.
        """
        eligible = [lv for lv in self.levels if lv.speed_mhz >= required_clock_mhz]
        if not eligible:
            return None
        # speed가 가장 작은 것 (level 번호가 가장 큰 것) 선택 → 전력 최소화
        return min(eligible, key=lambda lv: lv.speed_mhz)


class SimRunResult(BaseScenarioModel):
    """전체 시뮬레이션 파이프라인 실행 결과."""
    scenario_id: str
    variant_id: str
    total_power_mw: float
    total_power_ma: float        # mA = total_power_mw / (vbat * pmic_eff * 1000) * 1000
    bw_total_mbs: float          # sum(dma_breakdown[].bw_mbs)
    hw_time_max_ms: float        # max(timing_breakdown[].hw_time_ms)
    feasible: bool               # hw_time_max_ms <= (1000.0 / fps)
    infeasible_reason: str | None = None
    resolved: dict[str, ResolvedIPConfig] = Field(default_factory=dict)   # node_id -> ResolvedIPConfig
    dma_breakdown: list[PortBWResult] = Field(default_factory=list)
    timing_breakdown: list[IPTimingResult] = Field(default_factory=list)
    vdd_power: dict[str, float] = Field(default_factory=dict)             # VDD domain -> total mW
```

**tests/sim/conftest.py 생성 (YAML 파일 의존 없음 — 인라인 하드코딩):**
```python
from __future__ import annotations

import pytest

from scenario_db.models.capability.hw import IPSimParams, PortSpec, PortType
from scenario_db.models.definition.usecase import (
    IPPortConfig,
    PortInputConfig,
    SimGlobalConfig,
    SensorSpec,
)
from scenario_db.sim.models import DVFSLevel, DVFSTable


@pytest.fixture
def isp_sim_params() -> IPSimParams:
    return IPSimParams(
        hw_name_in_sim="ISP",
        ppc=4.0,
        unit_power_mw_mp=10.5,
        idc=0.5,
        vdd="VDD_INTCAM",
        dvfs_group="CAM",
        ports=[
            PortSpec(name="RDMA_FE", type=PortType.DMA_READ, max_bw_gbps=25.6),
            PortSpec(name="WDMA_BE", type=PortType.DMA_WRITE, max_bw_gbps=12.8),
            PortSpec(name="CINFIFO", type=PortType.OTF_IN),
            PortSpec(name="COUTFIFO", type=PortType.OTF_OUT),
        ],
    )


@pytest.fixture
def csis_sim_params() -> IPSimParams:
    return IPSimParams(
        hw_name_in_sim="CSIS",
        ppc=4.0,
        unit_power_mw_mp=3.2,
        idc=0.1,
        vdd="VDD_CAM",
        dvfs_group="CAM",
        ports=[
            PortSpec(name="WDMA", type=PortType.DMA_WRITE, max_bw_gbps=12.8),
            PortSpec(name="COUTFIFO", type=PortType.OTF_OUT),
        ],
    )


@pytest.fixture
def cam_dvfs_table() -> DVFSTable:
    return DVFSTable(
        domain="CAM",
        levels=[
            DVFSLevel(level=0, speed_mhz=600, voltages={0: 820, 4: 780, 8: 750}),
            DVFSLevel(level=1, speed_mhz=533, voltages={0: 760, 4: 720, 8: 700}),
            DVFSLevel(level=2, speed_mhz=400, voltages={0: 700, 4: 660, 8: 630}),
        ],
    )


@pytest.fixture
def fhd30_wdma_port() -> PortInputConfig:
    """FHD30 ISP WDMA_BE NV12 — Golden BW = 93.312 MB/s.
    계산: 1.0 * 30 * 1920 * 1080 * (8/8) * 1.5 / 1e6 = 93.312
    """
    return PortInputConfig(
        port="WDMA_BE",
        format="NV12",
        bitwidth=8,
        width=1920,
        height=1080,
        compression="disable",
    )


@pytest.fixture
def fhd30_rdma_port() -> PortInputConfig:
    """FHD30 ISP RDMA_FE BAYER SBWC — Golden BW = 202.68 MB/s.
    계산: 0.5 * 30 * 4000 * 2252 * (12/8) * 1.0 / 1e6 = 202.68
    """
    return PortInputConfig(
        port="RDMA_FE",
        format="BAYER",
        bitwidth=12,
        width=4000,
        height=2252,
        compression="SBWC",
        comp_ratio=0.5,
    )


@pytest.fixture
def default_sim_config() -> SimGlobalConfig:
    return SimGlobalConfig(
        asv_group=4,
        sw_margin=0.25,
        bw_power_coeff=80.0,
        vbat=4.0,
        pmic_eff=0.85,
        h_blank_margin=0.05,
    )


@pytest.fixture
def sensor_fhd30() -> SensorSpec:
    """FHD30 센서 — OTF 그룹 DVFS 제약용."""
    return SensorSpec(
        ip_ref="ip-csis-v3",
        frame_width=4000,
        frame_height=3000,
        fps=30.0,
        v_valid_ratio=0.85,
    )
```

**tests/sim/test_constants.py 생성:**
```python
from __future__ import annotations

import pytest

from scenario_db.sim.constants import (
    BPP_MAP,
    BW_POWER_COEFF_DEFAULT,
    REFERENCE_FPS,
    REFERENCE_VOLTAGE_MV,
)


@pytest.mark.parametrize("fmt,expected", [
    ("NV12", 1.5),
    ("YUV420", 1.5),
    ("RAW10", 1.25),
    ("ARGB", 4.0),
    ("BAYER", 1.0),
])
def test_bpp_map_values(fmt: str, expected: float) -> None:
    assert BPP_MAP[fmt] == expected, f"BPP_MAP[{fmt!r}] should be {expected}, got {BPP_MAP[fmt]}"


def test_reference_constants() -> None:
    assert REFERENCE_VOLTAGE_MV == 710.0
    assert REFERENCE_FPS == 30.0
    assert BW_POWER_COEFF_DEFAULT == 80.0
```

**tests/sim/test_models.py 생성:**
```python
from __future__ import annotations

from scenario_db.models.evidence.simulation import (
    IPTimingResult as EvidenceIPTimingResult,
)
from scenario_db.models.evidence.simulation import (
    PortBWResult as EvidencePortBWResult,
)
from scenario_db.sim.models import (
    DVFSLevel,
    DVFSTable,
    IPTimingResult,
    PortBWResult,
    ResolvedIPConfig,
    SimRunResult,
)


def test_port_bw_result_is_reimport() -> None:
    """D-01: sim.models.PortBWResult는 evidence.simulation.PortBWResult와 동일 클래스."""
    assert PortBWResult is EvidencePortBWResult

def test_ip_timing_result_is_reimport() -> None:
    assert IPTimingResult is EvidenceIPTimingResult


def test_dvfs_level_roundtrip() -> None:
    obj = DVFSLevel(level=0, speed_mhz=600.0, voltages={0: 820, 4: 780, 8: 750})
    dumped = obj.model_dump(exclude_none=True)
    obj2 = DVFSLevel.model_validate(dumped)
    assert obj == obj2
    assert obj.speed_mhz == 600.0


def test_dvfs_table_roundtrip() -> None:
    obj = DVFSTable(
        domain="CAM",
        levels=[
            DVFSLevel(level=0, speed_mhz=600.0, voltages={0: 820, 4: 780}),
            DVFSLevel(level=1, speed_mhz=533.0, voltages={0: 760, 4: 720}),
        ],
    )
    dumped = obj.model_dump(exclude_none=True)
    obj2 = DVFSTable.model_validate(dumped)
    assert obj == obj2


def test_dvfs_table_find_min_level() -> None:
    table = DVFSTable(
        domain="CAM",
        levels=[
            DVFSLevel(level=0, speed_mhz=600.0, voltages={4: 780}),
            DVFSLevel(level=1, speed_mhz=533.0, voltages={4: 720}),
            DVFSLevel(level=2, speed_mhz=400.0, voltages={4: 660}),
        ],
    )
    # 20.74 MHz required -> level 2 (400 MHz) 충분 — 가장 낮은 speed 선택
    lv = table.find_min_level(required_clock_mhz=20.74, asv_group=4)
    assert lv is not None
    assert lv.level == 2
    assert lv.speed_mhz == 400.0


def test_dvfs_table_find_min_level_none_when_insufficient() -> None:
    table = DVFSTable(
        domain="CAM",
        levels=[DVFSLevel(level=0, speed_mhz=100.0, voltages={4: 700})],
    )
    # 200 MHz required → 모든 레벨 insufficient
    lv = table.find_min_level(required_clock_mhz=200.0, asv_group=4)
    assert lv is None


def test_resolved_ip_config_roundtrip() -> None:
    obj = ResolvedIPConfig(
        ip_name="ISP",
        required_clock_mhz=20.74,
        set_clock_mhz=400.0,
        set_voltage_mv=660.0,
        dvfs_group="CAM",
        vdd="VDD_INTCAM",
    )
    dumped = obj.model_dump(exclude_none=True)
    obj2 = ResolvedIPConfig.model_validate(dumped)
    assert obj == obj2


def test_sim_run_result_roundtrip() -> None:
    bw = PortBWResult(ip="ISP", port="WDMA_BE", direction="write", bw_mbs=93.31, bw_power_mw=7.46)
    timing = IPTimingResult(
        ip="ISP", hw_time_ms=1.021,
        required_clock_mhz=20.74, set_clock_mhz=400.0,
        set_voltage_mv=660.0, feasible=True,
    )
    resolved = ResolvedIPConfig(
        ip_name="ISP", required_clock_mhz=20.74, set_clock_mhz=400.0,
        set_voltage_mv=660.0, dvfs_group="CAM", vdd="VDD_INTCAM",
    )
    obj = SimRunResult(
        scenario_id="uc-camera-recording",
        variant_id="FHD30-normal",
        total_power_mw=26.28,
        total_power_ma=7.73,
        bw_total_mbs=93.31,
        hw_time_max_ms=1.021,
        feasible=True,
        resolved={"isp0": resolved},
        dma_breakdown=[bw],
        timing_breakdown=[timing],
        vdd_power={"VDD_INTCAM": 26.28},
    )
    dumped = obj.model_dump(exclude_none=True)
    obj2 = SimRunResult.model_validate(dumped)
    assert obj2.scenario_id == "uc-camera-recording"
    assert obj2.feasible is True
    assert len(obj2.dma_breakdown) == 1
    assert len(obj2.timing_breakdown) == 1
```

**TDD 순서:** 테스트 파일 먼저 작성 → `uv run pytest tests/sim/test_constants.py tests/sim/test_models.py -x` 실행 (RED) → constants.py + models.py 구현 → 재실행 (GREEN)
  </action>
  <verify>
    <automated>cd E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB && uv run pytest tests/sim/test_constants.py tests/sim/test_models.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/sim/test_constants.py tests/sim/test_models.py -v` → 모두 PASSED (실패 0개)
    - BPP_MAP["BAYER"] == 1.0 테스트 통과
    - PortBWResult is EvidencePortBWResult 동일성 테스트 통과 (re-import 확인)
    - DVFSTable.find_min_level(20.74) → level=2 (speed=400) 반환 테스트 통과
    - SimRunResult round-trip 테스트 통과
    - `grep -n "BAYER.*1\.0" src/scenario_db/sim/constants.py` → 매칭 존재
    - `grep -n "from scenario_db.models.evidence.simulation import" src/scenario_db/sim/models.py` → 매칭 존재
    - `grep -rn "class PortBWResult" src/scenario_db/sim/models.py` → 결과 없음 (재정의 금지 확인)
  </acceptance_criteria>
  <done>constants.py + models.py 구현 완료, tests/sim/conftest.py + test_constants.py + test_models.py 모두 PASSED</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML 파일 → DVFSTable | dvfs-projectA.yaml은 개발자가 작성한 설정 파일 — yaml.safe_load()로만 파싱 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | hw_config/dvfs-projectA.yaml | accept | 전압/주파수 HW 파라미터 — 민감 정보 아님, 소스 제어로 버전 관리 충분 |
| T-06-02 | Tampering | dvfs-projectA.yaml YAML 파싱 | mitigate | yaml.safe_load() 사용 — 임의 Python 객체 역직렬화 방지 |
| T-06-03 | Information Disclosure | BPP_MAP 상수 | accept | 공개 HW 스펙 — 민감 정보 없음 |
</threat_model>

<verification>
```bash
# 전체 Wave 1 검증
cd E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB
uv run pytest tests/sim/test_constants.py tests/sim/test_models.py -v

# 기존 테스트 회귀 없음 확인 (SIM-09)
uv run pytest tests/unit/ -q

# import 경로 검증
uv run python -c "
from scenario_db.config import DVFS_CONFIG_PATH
from scenario_db.sim.constants import BPP_MAP, REFERENCE_VOLTAGE_MV
from scenario_db.sim.models import SimRunResult, DVFSTable, ResolvedIPConfig
print('All imports OK')
print(f'DVFS_CONFIG_PATH = {DVFS_CONFIG_PATH}')
print(f'BPP_MAP[BAYER] = {BPP_MAP[\"BAYER\"]}')
"
```
</verification>

<success_criteria>
1. `uv run pytest tests/sim/test_constants.py tests/sim/test_models.py -v` → 전체 통과
2. `from scenario_db.sim.models import SimRunResult, DVFSTable, DVFSLevel, ResolvedIPConfig` import 성공
3. `from scenario_db.config import DVFS_CONFIG_PATH` import 성공
4. BPP_MAP 5개 키(NV12/YUV420/RAW10/ARGB/BAYER) 모두 정확한 값 보유
5. PortBWResult is EvidencePortBWResult (동일 클래스, 재정의 없음)
6. `uv run pytest tests/unit/ -q` 기존 테스트 회귀 없음
</success_criteria>

<output>
After completion, create `.planning/phases/06-sim-package/06-01-SUMMARY.md`

Summary must include:
- Files created/modified (exact paths)
- Constants defined (BPP_MAP values, REFERENCE_VOLTAGE_MV, REFERENCE_FPS)
- Models defined (ResolvedIPConfig fields, DVFSLevel fields, DVFSTable fields, SimRunResult fields)
- Test results (test count, all passed)
- Any deviations from plan (e.g., SensorSpec field names if different from conftest assumptions)
</output>
