---
phase: 1
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scenario_db/db/repositories/scenario_graph.py
  - tests/unit/test_scenario_graph_models.py
autonomous: true
requirements:
  - DB-02

must_haves:
  truths:
    - "get_canonical_graph(db, 'uc-camera-recording', 'UHD60-HDR10-H265') 호출 시 CanonicalScenarioGraph DTO가 반환된다"
    - "존재하지 않는 scenario_id 또는 variant_id 요청 시 None이 반환된다"
    - "반환된 graph에 scenario, variant, project, evidence, issues, waivers, reviews 필드가 모두 포함된다"
    - "CanonicalScenarioGraph는 model_config = ConfigDict(extra='forbid')를 준수한다"
    - "ORM → Pydantic 변환 시 _sa_instance_state 키로 인한 ValidationError가 발생하지 않는다"
  artifacts:
    - path: "src/scenario_db/db/repositories/scenario_graph.py"
      provides: "Record DTO들 + CanonicalScenarioGraph DTO + get_canonical_graph() 함수"
      exports:
        - "ScenarioRecord"
        - "VariantRecord"
        - "ProjectRecord"
        - "EvidenceRecord"
        - "IssueRecord"
        - "WaiverRecord"
        - "ReviewRecord"
        - "IpRecord"
        - "SwProfileRecord"
        - "CanonicalScenarioGraph"
        - "get_canonical_graph"
    - path: "tests/unit/test_scenario_graph_models.py"
      provides: "CanonicalScenarioGraph Pydantic round-trip 단위 테스트"
  key_links:
    - from: "get_canonical_graph()"
      to: "ScenarioVariant ORM"
      via: "db.query(ScenarioVariant).filter_by(scenario_id=x, id=y).one_or_none()"
      pattern: "filter_by\\(scenario_id=.*id="
    - from: "ScenarioRecord"
      to: "Scenario ORM"
      via: "ConfigDict(from_attributes=True) + model_validate(orm_obj)"
      pattern: "from_attributes=True"
---

<objective>
CanonicalScenarioGraph DTO + DB 조회 서비스 구현 (DB-02).

Purpose: DB에서 scenario + variant + project + evidence + issues + waivers + reviews를
단일 Pydantic DTO로 반환하는 get_canonical_graph() 서비스를 구현한다.
이 DTO는 Phase 2 Resolver, Phase 3 API /graph 엔드포인트, Phase 4 Viewer의 공통 입력 계약이다.
ORM relationship 없이 수동 다중 쿼리(최대 6회)로 N+1을 방지한다 (D-05~D-09 per CONTEXT.md).

Output:
- src/scenario_db/db/repositories/scenario_graph.py (신규 — DTO + 서비스)
- tests/unit/test_scenario_graph_models.py (신규 — Pydantic round-trip 단위 테스트)
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db-foundation/01-CONTEXT.md
@.planning/phases/01-db-foundation/01-RESEARCH.md
@.planning/phases/01-db-foundation/01-PATTERNS.md

<interfaces>
<!-- 기존 ORM 모델 — executor가 Record DTO 필드를 정의할 때 직접 참조 -->

From src/scenario_db/db/models/definition.py:
```python
class Project(Base):
    __tablename__ = "projects"
    id = Column(Text, primary_key=True)
    schema_version = Column(Text, nullable=False)
    metadata_ = Column("metadata", JSONB, nullable=False)  # Python attr: metadata_
    globals_ = Column("globals", JSONB)                    # Python attr: globals_
    yaml_sha256 = Column(Text, nullable=False)

class Scenario(Base):
    __tablename__ = "scenarios"
    id = Column(Text, primary_key=True)
    schema_version = Column(Text, nullable=False)
    project_ref = Column(Text, ForeignKey("projects.id"), nullable=False)
    metadata_ = Column("metadata", JSONB, nullable=False)  # Python attr: metadata_
    pipeline = Column(JSONB, nullable=False)
    size_profile = Column(JSONB)
    design_axes = Column(JSONB)
    yaml_sha256 = Column(Text, nullable=False)

class ScenarioVariant(Base):
    __tablename__ = "scenario_variants"
    scenario_id = Column(Text, ForeignKey("scenarios.id"), primary_key=True)  # 복합 PK!
    id = Column(Text, primary_key=True)                                        # 복합 PK!
    severity = Column(Text)
    design_conditions = Column(JSONB)
    ip_requirements = Column(JSONB)
    sw_requirements = Column(JSONB)
    violation_policy = Column(JSONB)
    tags = Column(JSONB)
    derived_from_variant = Column(Text)
```

From src/scenario_db/db/models/decision.py:
```python
class Issue(Base):
    __tablename__ = "issues"
    id = Column(Text, primary_key=True)
    schema_version = Column(Text, nullable=False)
    metadata_ = Column("metadata", JSONB, nullable=False)
    affects = Column(JSONB)
    affects_ip = Column(JSONB)
    pmu_signature = Column(JSONB)
    resolution = Column(JSONB)
    yaml_sha256 = Column(Text, nullable=False)

class Waiver(Base):
    __tablename__ = "waivers"
    id = Column(Text, primary_key=True)
    yaml_sha256 = Column(Text, nullable=False)
    title = Column(Text, nullable=False)
    issue_ref = Column(Text, ForeignKey("issues.id"))
    scope = Column(JSONB, nullable=False)
    justification = Column(Text)
    status = Column(Text, nullable=False)
    approver_claim = Column(Text, nullable=False)
    # ... 날짜/audit 컬럼 다수 (Optional)

class Review(Base):
    __tablename__ = "reviews"
    id = Column(Text, primary_key=True)
    yaml_sha256 = Column(Text, nullable=False)
    scenario_ref = Column(Text, ForeignKey("scenarios.id"), nullable=False)
    variant_ref = Column(Text, nullable=False)
    evidence_refs = Column(JSONB)
    gate_result = Column(Text)
    auto_checks = Column(JSONB)
    decision = Column(Text)
    waiver_ref = Column(Text, ForeignKey("waivers.id"))
    rationale = Column(Text)
    review_scope = Column(JSONB)
    validation_ = Column("validation", JSONB)  # Python attr: validation_
    status = Column(Text, nullable=False)
    approver_claim = Column(Text, nullable=False)
    # ... audit 컬럼 다수 (Optional)
```

From src/scenario_db/db/models/capability.py:
```python
class IpCatalog(Base):
    __tablename__ = "ip_catalog"
    id = Column(Text, primary_key=True)
    schema_version = Column(Text, nullable=False)
    category = Column(Text)
    hierarchy = Column(JSONB)
    capabilities = Column(JSONB)
    rtl_version = Column(Text)
    compatible_soc = Column(JSONB)
    yaml_sha256 = Column(Text, nullable=False)

class SwProfile(Base):
    __tablename__ = "sw_profiles"
    id = Column(Text, primary_key=True)
    schema_version = Column(Text, nullable=False)
    metadata_ = Column("metadata", JSONB, nullable=False)  # Python attr: metadata_
    components = Column(JSONB, nullable=False)
    feature_flags = Column(JSONB, nullable=False)
    compatibility = Column(JSONB)
    yaml_sha256 = Column(Text, nullable=False)
```

From src/scenario_db/db/models/evidence.py:
```python
class Evidence(Base):
    __tablename__ = "evidence"
    id = Column(Text, primary_key=True)
    schema_version = Column(Text, nullable=False)
    kind = Column(Text, nullable=False)
    scenario_ref = Column(Text, ForeignKey("scenarios.id"), nullable=False)
    variant_ref = Column(Text, nullable=False)
    sw_baseline_ref = Column(Text, ForeignKey("sw_profiles.id"))
    sweep_job_id = Column(Text, ForeignKey("sweep_jobs.id"))
    execution_context = Column(JSONB, nullable=False)
    sweep_context = Column(JSONB)
    resolution_result = Column(JSONB)
    overall_feasibility = Column(Text)
    aggregation = Column(JSONB, nullable=False)
    kpi = Column(JSONB, nullable=False)
    run_info = Column(JSONB)
    ip_breakdown = Column(JSONB)
    provenance = Column(JSONB)
    artifacts = Column(JSONB)
    yaml_sha256 = Column(Text, nullable=False)
    sw_version_hint = Column(Text, Computed(...))  # Generated column
    sweep_value_hint = Column(Text, Computed(...)) # Generated column
```

From src/scenario_db/db/repositories/definition.py (패턴 참조):
```python
def get_variant(db: Session, scenario_id: str, variant_id: str) -> ScenarioVariant | None:
    return (
        db.query(ScenarioVariant)
        .filter_by(scenario_id=scenario_id, id=variant_id)
        .one_or_none()
    )
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Record DTO 정의 + 단위 테스트</name>
  <read_first>
    - src/scenario_db/db/models/definition.py (ScenarioRecord, VariantRecord, ProjectRecord 필드 추출)
    - src/scenario_db/db/models/decision.py (IssueRecord, WaiverRecord, ReviewRecord 필드 추출)
    - src/scenario_db/db/models/capability.py (IpRecord, SwProfileRecord 필드 추출)
    - src/scenario_db/db/models/evidence.py (EvidenceRecord 필드 추출 — sw_version_hint 처리 주의)
    - tests/unit/test_definition_models.py (Pydantic round-trip 패턴 확인)
  </read_first>
  <files>
    src/scenario_db/db/repositories/scenario_graph.py,
    tests/unit/test_scenario_graph_models.py
  </files>
  <behavior>
    - ScenarioRecord.model_validate(scenario_orm_obj) succeeds (from_attributes=True)
    - CanonicalScenarioGraph(scenario_id="x", variant_id="y", ...) with minimal fields succeeds
    - CanonicalScenarioGraph(unexpected_field="z", ...) raises ValidationError
    - ScenarioRecord(unexpected_field="z") raises ValidationError
    - VariantRecord with all None optional fields succeeds
  </behavior>
  <action>
**Step 1: src/scenario_db/db/repositories/scenario_graph.py — DTO 정의 부분 작성**

(get_canonical_graph 함수는 Task 2에서 추가. 이 task에서는 DTO 클래스만 정의)

파일 상단 imports:
```python
from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field
from sqlalchemy.orm import Session

from scenario_db.db.models.capability import IpCatalog, SwProfile
from scenario_db.db.models.decision import Issue, Review, Waiver
from scenario_db.db.models.definition import Project, Scenario, ScenarioVariant
from scenario_db.db.models.evidence import Evidence
```

Record DTO 정의 규칙:
- 모든 Record 클래스: `model_config = ConfigDict(extra='forbid', from_attributes=True)`
- ORM Python alias 컬럼(`metadata_`, `globals_`, `validation_`)은 Pydantic 필드명도 동일하게(`metadata_`, `globals_`, `validation_`) 정의
- nullable ORM 컬럼 → Pydantic `field: type | None = None`
- Evidence의 `sw_version_hint`, `sweep_value_hint`는 Generated column — `sw_version_hint: str | None = None` 으로 Optional 처리

다음 Record DTO를 정의한다:

```python
class ProjectRecord(BaseModel):
    model_config = ConfigDict(extra='forbid', from_attributes=True)
    id: str
    schema_version: str
    metadata_: dict
    globals_: dict | None = None
    yaml_sha256: str


class ScenarioRecord(BaseModel):
    model_config = ConfigDict(extra='forbid', from_attributes=True)
    id: str
    schema_version: str
    project_ref: str
    metadata_: dict
    pipeline: dict
    size_profile: dict | None = None
    design_axes: list | None = None
    yaml_sha256: str


class VariantRecord(BaseModel):
    model_config = ConfigDict(extra='forbid', from_attributes=True)
    scenario_id: str
    id: str
    severity: str | None = None
    design_conditions: dict | None = None
    ip_requirements: dict | None = None
    sw_requirements: dict | None = None
    violation_policy: dict | None = None
    tags: list | None = None
    derived_from_variant: str | None = None


class IpRecord(BaseModel):
    model_config = ConfigDict(extra='forbid', from_attributes=True)
    id: str
    schema_version: str
    category: str | None = None
    hierarchy: dict | None = None
    capabilities: dict | None = None
    rtl_version: str | None = None
    compatible_soc: list | None = None
    yaml_sha256: str


class SwProfileRecord(BaseModel):
    model_config = ConfigDict(extra='forbid', from_attributes=True)
    id: str
    schema_version: str
    metadata_: dict
    components: dict
    feature_flags: dict
    compatibility: dict | None = None
    yaml_sha256: str


class EvidenceRecord(BaseModel):
    model_config = ConfigDict(extra='forbid', from_attributes=True)
    id: str
    schema_version: str
    kind: str
    scenario_ref: str
    variant_ref: str
    sw_baseline_ref: str | None = None
    sweep_job_id: str | None = None
    execution_context: dict
    sweep_context: dict | None = None
    resolution_result: dict | None = None
    overall_feasibility: str | None = None
    aggregation: dict
    kpi: dict
    run_info: dict | None = None
    ip_breakdown: dict | None = None
    provenance: dict | None = None
    artifacts: dict | None = None
    yaml_sha256: str
    sw_version_hint: str | None = None
    sweep_value_hint: str | None = None


class IssueRecord(BaseModel):
    model_config = ConfigDict(extra='forbid', from_attributes=True)
    id: str
    schema_version: str
    metadata_: dict
    affects: list | None = None
    affects_ip: list | None = None
    pmu_signature: dict | None = None
    resolution: dict | None = None
    yaml_sha256: str


class WaiverRecord(BaseModel):
    model_config = ConfigDict(extra='forbid', from_attributes=True)
    id: str
    yaml_sha256: str
    title: str
    issue_ref: str | None = None
    scope: dict
    justification: str | None = None
    status: str
    approver_claim: str
    claim_at: object | None = None
    git_commit_sha: str | None = None
    git_commit_author_email: str | None = None
    git_signed: bool | None = None
    approved_by_auth: str | None = None
    auth_method: str | None = None
    auth_timestamp: object | None = None
    auth_session_id: str | None = None
    approved_at: object | None = None
    expires_on: object | None = None


class ReviewRecord(BaseModel):
    model_config = ConfigDict(extra='forbid', from_attributes=True)
    id: str
    yaml_sha256: str
    scenario_ref: str
    variant_ref: str
    evidence_refs: list | None = None
    gate_result: str | None = None
    auto_checks: dict | None = None
    decision: str | None = None
    waiver_ref: str | None = None
    rationale: str | None = None
    review_scope: dict | None = None
    validation_: dict | None = None
    status: str
    approver_claim: str
    claim_at: object | None = None
    git_commit_sha: str | None = None
    git_commit_author_email: str | None = None
    git_signed: bool | None = None
    approved_by_auth: str | None = None
    auth_method: str | None = None
    auth_timestamp: object | None = None
    auth_session_id: str | None = None


class CanonicalScenarioGraph(BaseModel):
    model_config = ConfigDict(extra='forbid')
    scenario_id: str
    variant_id: str
    scenario: ScenarioRecord
    variant: VariantRecord
    project: ProjectRecord | None = None
    pipeline: dict
    ip_catalog: dict[str, IpRecord]
    sw_profiles: dict[str, SwProfileRecord]
    evidence: list[EvidenceRecord]
    issues: list[IssueRecord]
    waivers: list[WaiverRecord]
    reviews: list[ReviewRecord]
```

**Step 2: tests/unit/test_scenario_graph_models.py 작성**

```python
from __future__ import annotations

import pytest
from pydantic import ValidationError

from scenario_db.db.repositories.scenario_graph import (
    CanonicalScenarioGraph,
    EvidenceRecord,
    IpRecord,
    IssueRecord,
    ProjectRecord,
    ReviewRecord,
    ScenarioRecord,
    SwProfileRecord,
    VariantRecord,
    WaiverRecord,
)


def _minimal_scenario_record() -> dict:
    return {
        "id": "uc-camera-recording",
        "schema_version": "2.2",
        "project_ref": "projectA",
        "metadata_": {"name": "test"},
        "pipeline": {"nodes": []},
        "yaml_sha256": "abc123",
    }


def _minimal_variant_record() -> dict:
    return {
        "scenario_id": "uc-camera-recording",
        "id": "UHD60-HDR10-H265",
    }


def test_scenario_record_minimal():
    rec = ScenarioRecord.model_validate(_minimal_scenario_record())
    assert rec.id == "uc-camera-recording"
    assert rec.size_profile is None


def test_scenario_record_extra_forbidden():
    data = {**_minimal_scenario_record(), "unexpected": "oops"}
    with pytest.raises(ValidationError):
        ScenarioRecord.model_validate(data)


def test_variant_record_all_optional_none():
    rec = VariantRecord.model_validate(_minimal_variant_record())
    assert rec.severity is None
    assert rec.ip_requirements is None


def test_canonical_graph_construct():
    graph = CanonicalScenarioGraph(
        scenario_id="uc-camera-recording",
        variant_id="UHD60-HDR10-H265",
        scenario=ScenarioRecord.model_validate(_minimal_scenario_record()),
        variant=VariantRecord.model_validate(_minimal_variant_record()),
        project=None,
        pipeline={"nodes": []},
        ip_catalog={},
        sw_profiles={},
        evidence=[],
        issues=[],
        waivers=[],
        reviews=[],
    )
    assert graph.scenario_id == "uc-camera-recording"
    assert graph.variant_id == "UHD60-HDR10-H265"
    assert graph.project is None
    assert graph.evidence == []


def test_canonical_graph_extra_forbidden():
    with pytest.raises(ValidationError):
        CanonicalScenarioGraph(
            scenario_id="x",
            variant_id="y",
            scenario=ScenarioRecord.model_validate(_minimal_scenario_record()),
            variant=VariantRecord.model_validate(_minimal_variant_record()),
            pipeline={},
            ip_catalog={},
            sw_profiles={},
            evidence=[],
            issues=[],
            waivers=[],
            reviews=[],
            unexpected_field="forbidden",
        )
```
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_scenario_graph_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/scenario_db/db/repositories/scenario_graph.py` 가 존재한다
    - `grep -c "class CanonicalScenarioGraph" src/scenario_db/db/repositories/scenario_graph.py` == 1
    - `grep -c "from_attributes=True" src/scenario_db/db/repositories/scenario_graph.py` >= 7 (모든 Record 클래스)
    - `grep -c "extra='forbid'" src/scenario_db/db/repositories/scenario_graph.py` >= 9 (CanonicalScenarioGraph 포함)
    - `grep -c "class ScenarioRecord" src/scenario_db/db/repositories/scenario_graph.py` == 1
    - `grep -c "class VariantRecord" src/scenario_db/db/repositories/scenario_graph.py` == 1
    - `uv run pytest tests/unit/test_scenario_graph_models.py -x -q` exits 0
  </acceptance_criteria>
  <done>9개 Record/Graph DTO 클래스 정의 완료, unit tests 5개 모두 pass</done>
</task>

<task type="auto">
  <name>Task 2: get_canonical_graph() 구현</name>
  <read_first>
    - src/scenario_db/db/repositories/scenario_graph.py (Task 1에서 생성한 DTO 코드 확인)
    - src/scenario_db/db/repositories/definition.py (session.query().filter_by() 패턴 확인)
    - .planning/phases/01-db-foundation/01-RESEARCH.md §Pattern 4 (수동 다중 쿼리 전략)
    - .planning/phases/01-db-foundation/01-RESEARCH.md §Finding 1 (ORM relationship 없음)
    - .planning/phases/01-db-foundation/01-RESEARCH.md §Finding 3 (_issue_affects_scenario)
  </read_first>
  <files>
    src/scenario_db/db/repositories/scenario_graph.py
  </files>
  <action>
`scenario_graph.py` 하단에 다음 헬퍼 함수와 `get_canonical_graph()` 함수를 추가한다.

**헬퍼 함수:**
```python
def _issue_affects_scenario(affects: list[dict] | None, scenario_id: str) -> bool:
    if not affects:
        return False
    return any(
        entry.get("scenario_ref") in ("*", scenario_id)
        for entry in affects
    )
```

**get_canonical_graph() 구현:**

ORM에 `relationship()` 없으므로 `joinedload`/`selectinload` 사용 금지 (AttributeError 발생).
기존 `session.query()` 스타일로 작성 (definition.py 패턴 일관성).

```python
def get_canonical_graph(
    db: Session,
    scenario_id: str,
    variant_id: str,
) -> CanonicalScenarioGraph | None:
    """
    DB에서 scenario 전체 그래프를 단일 DTO로 반환.
    존재하지 않는 scenario_id 또는 variant_id → None 반환.
    """
    # Query 1a: Scenario 조회
    scenario = db.query(Scenario).filter_by(id=scenario_id).one_or_none()
    if scenario is None:
        return None

    # Query 1b: ScenarioVariant 조회 (복합 PK: scenario_id + id 모두 필요)
    variant = (
        db.query(ScenarioVariant)
        .filter_by(scenario_id=scenario_id, id=variant_id)
        .one_or_none()
    )
    if variant is None:
        return None

    # Query 1c: Project 조회 (LEFT OUTER — project_ref가 없을 수도 있음)
    project = db.query(Project).filter_by(id=scenario.project_ref).one_or_none()

    # Query 2a: Evidence (scenario_ref + variant_ref 필터)
    evidence_rows = (
        db.query(Evidence)
        .filter(Evidence.scenario_ref == scenario_id, Evidence.variant_ref == variant_id)
        .all()
    )

    # Query 2b: Issues (Python-level filter — affects JSONB, 소규모 fixture)
    all_issues = db.query(Issue).all()
    issues = [iss for iss in all_issues if _issue_affects_scenario(iss.affects, scenario_id)]

    # Query 2c: Waivers (matched issue_refs — issue_ref IN (matched ids))
    matched_issue_ids = {iss.id for iss in issues}
    if matched_issue_ids:
        waivers = (
            db.query(Waiver)
            .filter(Waiver.issue_ref.in_(matched_issue_ids))
            .all()
        )
    else:
        waivers = []

    # Query 2d: Reviews (scenario_ref + variant_ref)
    reviews = (
        db.query(Review)
        .filter(Review.scenario_ref == scenario_id, Review.variant_ref == variant_id)
        .all()
    )

    # Query 3: IP Catalog (pipeline nodes의 ip_ref 목록)
    pipeline_ip_refs = {
        node["ip_ref"]
        for node in (scenario.pipeline or {}).get("nodes", [])
        if "ip_ref" in node
    }
    if pipeline_ip_refs:
        ip_catalog_rows = db.query(IpCatalog).filter(IpCatalog.id.in_(pipeline_ip_refs)).all()
    else:
        ip_catalog_rows = []

    # Query 4: SW Profiles (variant.sw_requirements의 profile_ref 목록)
    sw_req = variant.sw_requirements or {}
    sw_profile_refs: set[str] = set()
    for item in sw_req.get("profile_constraints", []) if isinstance(sw_req, dict) else []:
        if isinstance(item, dict) and "profile_ref" in item:
            sw_profile_refs.add(item["profile_ref"])
    if sw_profile_refs:
        sw_profile_rows = db.query(SwProfile).filter(SwProfile.id.in_(sw_profile_refs)).all()
    else:
        sw_profile_rows = []

    # ORM → Pydantic 변환 (from_attributes=True — _sa_instance_state 자동 무시)
    return CanonicalScenarioGraph(
        scenario_id=scenario_id,
        variant_id=variant_id,
        scenario=ScenarioRecord.model_validate(scenario),
        variant=VariantRecord.model_validate(variant),
        project=ProjectRecord.model_validate(project) if project else None,
        pipeline=scenario.pipeline or {},
        ip_catalog={ip.id: IpRecord.model_validate(ip) for ip in ip_catalog_rows},
        sw_profiles={sw.id: SwProfileRecord.model_validate(sw) for sw in sw_profile_rows},
        evidence=[EvidenceRecord.model_validate(e) for e in evidence_rows],
        issues=[IssueRecord.model_validate(i) for i in issues],
        waivers=[WaiverRecord.model_validate(w) for w in waivers],
        reviews=[ReviewRecord.model_validate(r) for r in reviews],
    )
```

**중요 주의사항:**
- `model_validate(orm_obj)` 패턴 사용 (Record DTO의 `from_attributes=True` 덕분에 `_sa_instance_state` 자동 무시됨)
- `model_validate(row.__dict__)` 패턴 사용 금지 (`_sa_instance_state` 포함으로 `extra='forbid'` 위반)
- ScenarioVariant는 반드시 `filter_by(scenario_id=scenario_id, id=variant_id)` — 단일 PK 조회 금지
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_scenario_graph_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def get_canonical_graph" src/scenario_db/db/repositories/scenario_graph.py` == 1
    - `grep -c "def _issue_affects_scenario" src/scenario_db/db/repositories/scenario_graph.py` == 1
    - `grep -c "filter_by(scenario_id=scenario_id, id=variant_id)" src/scenario_db/db/repositories/scenario_graph.py` == 1
    - `grep -c "model_validate(orm_obj)\|model_validate(scenario)\|model_validate(variant)\|model_validate(project)\|model_validate(ip)\|model_validate(sw)\|model_validate(e)\|model_validate(i)\|model_validate(w)\|model_validate(r)" src/scenario_db/db/repositories/scenario_graph.py` >= 1
    - `grep -c "row.__dict__" src/scenario_db/db/repositories/scenario_graph.py` == 0 (금지 패턴 없음)
    - `grep -c "joinedload\|selectinload" src/scenario_db/db/repositories/scenario_graph.py` == 0 (금지 패턴 없음)
    - `uv run pytest tests/unit/test_scenario_graph_models.py -x -q` exits 0 (regression 없음)
  </acceptance_criteria>
  <done>get_canonical_graph() 구현 완료. 반환값: CanonicalScenarioGraph | None. 수동 6-쿼리 전략으로 N+1 방지.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → get_canonical_graph() | Phase 3에서 scenario_id, variant_id가 HTTP 경로 파라미터로 입력됨 |
| DB → CanonicalScenarioGraph DTO | ORM 객체가 Pydantic DTO로 변환됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Injection | get_canonical_graph() — scenario_id, variant_id | accept | `db.query(Model).filter_by(id=scenario_id)` — SQLAlchemy parameterized query로 모든 입력이 bind parameter 처리됨. SQL injection 불가 |
| T-02-02 | Information Disclosure | CanonicalScenarioGraph — 전체 그래프 반환 | accept | Phase 1은 내부 서비스 계층. Phase 3 API 노출 시 인증/인가 레이어 추가 필요 (Phase 3 scope) |
| T-02-03 | Tampering | ORM → Pydantic 변환 | mitigate | `ConfigDict(extra='forbid')` 적용으로 예상치 못한 필드 포함 시 ValidationError 발생 — 데이터 무결성 보장 |
| T-02-04 | Denial of Service | all_issues 전체 로드 | accept | 현재 fixture는 소규모 (수십 레코드). 대용량 데이터 시 JSONB DB-side 필터로 교체 필요. Phase 1 scope에서는 허용 |
</threat_model>

<verification>
**Unit test (Task 1 완료 후):**
```bash
uv run pytest tests/unit/test_scenario_graph_models.py -x -q
```

**Integration test (Plan 03 Wave 2에서 통합 실행):**
```bash
uv run pytest tests/integration/test_scenario_graph.py -x -q -m integration
```

**Regression check:**
```bash
uv run pytest tests/ -q
```
기존 209개 테스트 + 신규 테스트 모두 green.
</verification>

<success_criteria>
1. `uv run pytest tests/unit/test_scenario_graph_models.py -x -q` exits 0
2. `grep -c "class CanonicalScenarioGraph" src/scenario_db/db/repositories/scenario_graph.py` == 1
3. `grep -c "def get_canonical_graph" src/scenario_db/db/repositories/scenario_graph.py` == 1
4. `grep -c "row.__dict__" src/scenario_db/db/repositories/scenario_graph.py` == 0
5. `grep -c "joinedload\|selectinload" src/scenario_db/db/repositories/scenario_graph.py` == 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-db-foundation/01-2-SUMMARY.md`
</output>
