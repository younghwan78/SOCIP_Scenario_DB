---
phase: 1
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/fixtures/02_definition/uc-camera-recording.yaml
  - src/scenario_db/etl/validate_loaded.py
  - src/scenario_db/etl/loader.py
  - tests/unit/test_validate_loaded.py
  - tests/integration/test_validate_loaded.py
autonomous: true
requirements:
  - DB-01

must_haves:
  truths:
    - "validate_loaded(session) 호출 시 ValidationReport(errors, warnings) Pydantic 모델이 반환된다"
    - "demo fixtures 로드 후 validate_loaded() 실행 결과 errors == [] (FHD30-SDR-H265 포함)"
    - "load_yaml_dir() 완료 후 validate_loaded()가 자동 실행되어 오류가 logger.warning으로 출력된다"
    - "존재하지 않는 project_ref, scenario_id, issue_ref를 가진 레코드는 errors에 수집된다"
    - "gate_rule trigger/condition/action 형식 위반도 errors에 수집된다"
  artifacts:
    - path: "src/scenario_db/etl/validate_loaded.py"
      provides: "ValidationReport 모델 + validate_loaded() 함수 (8가지 FK-like 규칙)"
      exports: ["ValidationReport", "validate_loaded"]
    - path: "tests/unit/test_validate_loaded.py"
      provides: "ValidationReport 단위 테스트"
    - path: "tests/integration/test_validate_loaded.py"
      provides: "demo fixtures 기반 통합 테스트"
  key_links:
    - from: "src/scenario_db/etl/loader.py"
      to: "src/scenario_db/etl/validate_loaded.validate_loaded"
      via: "session.commit() 직후 지역 import + 호출"
      pattern: "validate_loaded\\(session\\)"
    - from: "tests/integration/test_validate_loaded.py"
      to: "engine fixture (conftest.py)"
      via: "pytestmark = pytest.mark.integration"
      pattern: "def test_.*\\(engine\\)"
---

<objective>
ETL post-load semantic validation 구현 (DB-01).

Purpose: ETL 완료 후 DB 상태에서 FK-like 참조 무결성을 소프트하게 검증한다.
8가지 규칙을 검사하여 오류 목록을 수집하고 ValidationReport로 반환함으로써
참조 무결성 위반을 탐지하되 DB 상태를 유지한다 (D-01, D-02, D-03, D-04 per CONTEXT.md).

Output:
- src/scenario_db/etl/validate_loaded.py (신규)
- src/scenario_db/etl/loader.py (수정 — validate_loaded() 자동 호출 삽입)
- tests/unit/test_validate_loaded.py (신규)
- tests/integration/test_validate_loaded.py (신규)
- demo/fixtures/02_definition/uc-camera-recording.yaml (수정 — FHD30-SDR-H265 variant 추가)
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db-foundation/01-CONTEXT.md
@.planning/phases/01-db-foundation/01-RESEARCH.md
@.planning/phases/01-db-foundation/01-PATTERNS.md

<interfaces>
<!-- 기존 코드 계약 — executor가 직접 참조할 것 -->

From src/scenario_db/etl/loader.py (수정 대상):
```python
def load_yaml_dir(directory: Path, session: Session) -> dict[str, int]:
    # ...
    session.commit()         # line 98 — 이 라인 이후에 validate_loaded() 삽입

    total = sum(counts.values())
    logger.info("ETL complete — %d loaded, %d skipped", total, len(skipped))
    return counts            # 반환값 dict[str, int] 변경 없음
```

From src/scenario_db/db/models/definition.py:
```python
class Project(Base):
    id = Column(Text, primary_key=True)
    metadata_ = Column("metadata", JSONB, nullable=False)  # Python attr: metadata_
    globals_ = Column("globals", JSONB)                    # Python attr: globals_

class Scenario(Base):
    id = Column(Text, primary_key=True)
    project_ref = Column(Text, ForeignKey("projects.id"), nullable=False)
    pipeline = Column(JSONB, nullable=False)  # {nodes: [{ip_ref: str, ...}]}

class ScenarioVariant(Base):
    scenario_id = Column(Text, ForeignKey("scenarios.id"), primary_key=True)  # 복합 PK 1
    id = Column(Text, primary_key=True)                                        # 복합 PK 2
```

From src/scenario_db/db/models/decision.py:
```python
class GateRule(Base):
    id = Column(Text, primary_key=True)
    trigger = Column(JSONB, nullable=False)    # {"events": [...]}
    condition = Column(JSONB, nullable=False)  # {"match": {...}}
    action = Column(JSONB, nullable=False)     # {"gate_result": "BLOCK"|"WARN"}

class Issue(Base):
    id = Column(Text, primary_key=True)
    affects = Column(JSONB)  # list[{"scenario_ref": str, "match_rule": dict}]

class Waiver(Base):
    id = Column(Text, primary_key=True)
    issue_ref = Column(Text, ForeignKey("issues.id"))

class Review(Base):
    id = Column(Text, primary_key=True)
    scenario_ref = Column(Text, ForeignKey("scenarios.id"), nullable=False)
    variant_ref = Column(Text, nullable=False)
    evidence_refs = Column(JSONB)   # list[str]
    waiver_ref = Column(Text, ForeignKey("waivers.id"))
```

From src/scenario_db/db/models/evidence.py:
```python
class Evidence(Base):
    id = Column(Text, primary_key=True)
    scenario_ref = Column(Text, ForeignKey("scenarios.id"), nullable=False)
    variant_ref = Column(Text, nullable=False)
```

From tests/integration/conftest.py:
```python
# engine fixture: session-scoped, PostgreSQL testcontainers, ETL 로드 완료 상태
# 사용법: def test_xxx(engine): with Session(engine) as session: ...
DEMO_FIXTURES = Path(__file__).parent.parent.parent / "demo" / "fixtures"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FHD30-SDR-H265 fixture 추가 + 테스트 스캐폴드</name>
  <read_first>
    - demo/fixtures/02_definition/uc-camera-recording.yaml (현재 variant 목록 확인)
    - tests/unit/test_decision_models.py (unit test 패턴 확인)
    - tests/integration/test_api_definition.py (integration test marker/fixture 패턴 확인)
    - tests/integration/conftest.py (engine fixture 구조 확인)
  </read_first>
  <files>
    demo/fixtures/02_definition/uc-camera-recording.yaml,
    tests/unit/test_validate_loaded.py,
    tests/integration/test_validate_loaded.py
  </files>
  <action>
**Step 1: FHD30-SDR-H265 variant를 uc-camera-recording.yaml에 추가**

`demo/fixtures/02_definition/uc-camera-recording.yaml` 의 `variants:` 섹션에
FHD30-SDR-H265 variant를 추가한다. 기존 UHD60-HDR10-H265 variant를 기반으로
해상도/코덱만 변경한 최소 variant로 작성한다:

```yaml
  - id: FHD30-SDR-H265
    severity: normal
    design_conditions:
      resolution: "1920x1080"
      fps: 30
      color_format: SDR
      codec: H265
    ip_requirements: {}
    sw_requirements: {}
    tags: [fhd, sdr, h265]
```

**Step 2: tests/unit/test_validate_loaded.py 작성**

파일 헤더에 `from __future__ import annotations` 포함.
`pytestmark` 없음 (unit test는 DB 불필요).

다음 테스트 함수를 포함한다:
- `test_validation_report_empty_is_valid()`: `ValidationReport()` 생성 → `report.is_valid is True`, `report.errors == []`, `report.warnings == []`
- `test_validation_report_with_errors_is_invalid()`: `ValidationReport(errors=["e"])` → `report.is_valid is False`
- `test_validation_report_extra_fields_forbidden()`: `ValidationReport(errors=[], unexpected="x")` → `pytest.raises(ValidationError)`
- `test_validation_report_warnings_do_not_affect_validity()`: `ValidationReport(warnings=["w"])` → `report.is_valid is True`

import: `from scenario_db.etl.validate_loaded import ValidationReport`

**Step 3: tests/integration/test_validate_loaded.py 작성**

파일 헤더:
```python
from __future__ import annotations
import pytest
from sqlalchemy.orm import Session
from scenario_db.etl.validate_loaded import validate_loaded

pytestmark = pytest.mark.integration

SCENARIO_ID = "uc-camera-recording"
VARIANT_ID = "UHD60-HDR10-H265"
FHD_VARIANT_ID = "FHD30-SDR-H265"
```

다음 테스트 함수를 포함한다:
- `test_validate_loaded_no_errors(engine)`: `with Session(engine) as session: report = validate_loaded(session)` → `assert report.errors == []`
- `test_validate_loaded_is_valid(engine)`: `report.is_valid is True`
- `test_validate_loaded_fhd_variant_exists(engine)`: `from scenario_db.db.models.definition import ScenarioVariant` → `db.query(ScenarioVariant).filter_by(scenario_id=SCENARIO_ID, id=FHD_VARIANT_ID).one_or_none()` → `assert variant is not None`
- `test_load_triggers_validation(engine)`: `validate_loaded` 임포트 후 `load_yaml_dir` 내부에서 자동 호출된다는 것을 확인 — 이 테스트는 `load_yaml_dir` 완료 후 DB에 데이터가 있음을 확인하는 방식으로 간접 검증 (conftest.py의 engine fixture가 이미 load_yaml_dir를 실행하므로, `engine`을 인자로 받아 `Session(engine)` 으로 validate_loaded 실행 → errors == [])
  </action>
  <verify>
    <automated>
      python -c "from pathlib import Path; assert Path('tests/unit/test_validate_loaded.py').exists(), 'unit test scaffold missing'; assert Path('tests/integration/test_validate_loaded.py').exists(), 'integration test scaffold missing'; print('scaffold OK')"
    </automated>
    <automated>
      grep -q "FHD30-SDR-H265" demo/fixtures/02_definition/uc-camera-recording.yaml && echo "FHD30 fixture OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - demo/fixtures/02_definition/uc-camera-recording.yaml 에 `id: FHD30-SDR-H265` 항목이 존재한다: `grep -c "FHD30-SDR-H265" demo/fixtures/02_definition/uc-camera-recording.yaml` 결과 >= 1
    - tests/unit/test_validate_loaded.py 가 존재하고 `from scenario_db.etl.validate_loaded import ValidationReport` 라인이 있다
    - tests/integration/test_validate_loaded.py 에 `pytestmark = pytest.mark.integration` 이 있다
    - tests/integration/test_validate_loaded.py 에 `def test_validate_loaded_no_errors(engine):` 가 있다
    - `uv run pytest tests/unit/test_validate_loaded.py -x -q` 가 exit 0
  </acceptance_criteria>
  <done>FHD30-SDR-H265 variant가 fixture에 존재하고, unit/integration test 스캐폴드 파일 2개가 생성됨</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: validate_loaded.py 구현 + loader.py 통합</name>
  <read_first>
    - src/scenario_db/etl/validate_loaded.py (존재하지 않음 — 신규 생성)
    - src/scenario_db/etl/loader.py (수정 대상 — session.commit() 이후 삽입 위치 확인)
    - src/scenario_db/etl/mappers/decision.py (ETL layer import 패턴 참조)
    - .planning/phases/01-db-foundation/01-RESEARCH.md §Pattern 3 (8가지 규칙 구현 코드)
  </read_first>
  <files>
    src/scenario_db/etl/validate_loaded.py,
    src/scenario_db/etl/loader.py
  </files>
  <behavior>
    - ValidationReport() → is_valid=True, errors=[], warnings=[]
    - ValidationReport(errors=["e"]) → is_valid=False
    - validate_loaded(session) with demo fixtures → errors == []
    - validate_loaded(session) with missing project_ref → errors contains "scenario '...': project_ref '...' not found in projects"
    - validate_loaded(session) with invalid gate_rule (no 'events' key) → errors contains "gate_rule '...': trigger missing 'events' key"
    - validate_loaded(session) with issue affects wildcard "*" → no error (wildcard is valid)
  </behavior>
  <action>
**Step 1: src/scenario_db/etl/validate_loaded.py 신규 생성**

D-01, D-02, D-04 per CONTEXT.md 구현.

```python
from __future__ import annotations

import logging
from pydantic import BaseModel, ConfigDict
from sqlalchemy.orm import Session
from sqlalchemy import select

from scenario_db.db.models.capability import IpCatalog
from scenario_db.db.models.decision import GateRule, Issue, Waiver
from scenario_db.db.models.definition import Project, Scenario, ScenarioVariant
from scenario_db.db.models.evidence import Evidence
from scenario_db.db.models.decision import Review

logger = logging.getLogger(__name__)


class ValidationReport(BaseModel):
    model_config = ConfigDict(extra='forbid')

    errors: list[str] = []
    warnings: list[str] = []

    @property
    def is_valid(self) -> bool:
        return len(self.errors) == 0


def _issue_affects_scenario(affects: list[dict] | None, scenario_id: str) -> bool:
    if not affects:
        return False
    return any(
        entry.get("scenario_ref") in ("*", scenario_id)
        for entry in affects
    )


def validate_loaded(session: Session) -> ValidationReport:
    errors: list[str] = []
    warnings: list[str] = []

    # Rule 1: scenario.project_ref → projects.id
    project_ids = {r[0] for r in session.execute(select(Project.id))}
    for scenario_id, project_ref in session.execute(
        select(Scenario.id, Scenario.project_ref)
    ):
        if project_ref not in project_ids:
            errors.append(
                f"scenario '{scenario_id}': project_ref '{project_ref}' not found in projects"
            )

    # Rule 2: scenario_variant.scenario_id → scenarios.id
    scenario_ids = {r[0] for r in session.execute(select(Scenario.id))}
    for sv_scenario_id, sv_id in session.execute(
        select(ScenarioVariant.scenario_id, ScenarioVariant.id)
    ):
        if sv_scenario_id not in scenario_ids:
            errors.append(
                f"variant '{sv_id}': scenario_id '{sv_scenario_id}' not found in scenarios"
            )

    # Rule 3: evidence.scenario_ref + evidence.variant_ref → 대상 존재 여부
    variant_keys = {
        (r[0], r[1])
        for r in session.execute(
            select(ScenarioVariant.scenario_id, ScenarioVariant.id)
        )
    }
    for ev_id, ev_scenario_ref, ev_variant_ref in session.execute(
        select(Evidence.id, Evidence.scenario_ref, Evidence.variant_ref)
    ):
        if ev_scenario_ref not in scenario_ids:
            errors.append(
                f"evidence '{ev_id}': scenario_ref '{ev_scenario_ref}' not found in scenarios"
            )
        elif (ev_scenario_ref, ev_variant_ref) not in variant_keys:
            errors.append(
                f"evidence '{ev_id}': variant_ref '{ev_variant_ref}' not found under scenario '{ev_scenario_ref}'"
            )

    # Rule 4: review.scenario_ref + review.variant_ref + review.evidence_refs + review.waiver_ref
    evidence_ids = {r[0] for r in session.execute(select(Evidence.id))}
    waiver_ids = {r[0] for r in session.execute(select(Waiver.id))}
    for rev_id, rev_scenario_ref, rev_variant_ref, rev_evidence_refs, rev_waiver_ref in session.execute(
        select(Review.id, Review.scenario_ref, Review.variant_ref, Review.evidence_refs, Review.waiver_ref)
    ):
        if rev_scenario_ref not in scenario_ids:
            errors.append(
                f"review '{rev_id}': scenario_ref '{rev_scenario_ref}' not found in scenarios"
            )
        elif (rev_scenario_ref, rev_variant_ref) not in variant_keys:
            errors.append(
                f"review '{rev_id}': variant_ref '{rev_variant_ref}' not found under scenario '{rev_scenario_ref}'"
            )
        if rev_evidence_refs:
            for eref in rev_evidence_refs:
                if eref not in evidence_ids:
                    errors.append(
                        f"review '{rev_id}': evidence_ref '{eref}' not found in evidence"
                    )
        if rev_waiver_ref and rev_waiver_ref not in waiver_ids:
            errors.append(
                f"review '{rev_id}': waiver_ref '{rev_waiver_ref}' not found in waivers"
            )

    # Rule 5: issue.affects[*].scenario_ref → '*' 또는 존재하는 scenario_id
    for issue_id, affects in session.execute(select(Issue.id, Issue.affects)):
        if affects:
            for entry in affects:
                ref = entry.get("scenario_ref", "")
                if ref != "*" and ref not in scenario_ids:
                    warnings.append(
                        f"issue '{issue_id}': affects.scenario_ref '{ref}' not found"
                    )

    # Rule 6: waiver.issue_ref → issues.id
    issue_ids = {r[0] for r in session.execute(select(Issue.id))}
    for waiver_id, issue_ref in session.execute(select(Waiver.id, Waiver.issue_ref)):
        if issue_ref and issue_ref not in issue_ids:
            errors.append(
                f"waiver '{waiver_id}': issue_ref '{issue_ref}' not found in issues"
            )

    # Rule 7: gate_rule trigger/condition/action 필수 키 존재
    for rule_id, trigger, condition, action in session.execute(
        select(GateRule.id, GateRule.trigger, GateRule.condition, GateRule.action)
    ):
        if not trigger or "events" not in trigger:
            errors.append(f"gate_rule '{rule_id}': trigger missing 'events' key")
        if not condition or "match" not in condition:
            errors.append(f"gate_rule '{rule_id}': condition missing 'match' key")
        if not action or "gate_result" not in action:
            errors.append(f"gate_rule '{rule_id}': action missing 'gate_result' key")

    # Rule 8: scenario.pipeline 노드 ip_ref → ip_catalog.id
    ip_ids = {r[0] for r in session.execute(select(IpCatalog.id))}
    for scen_id, pipeline in session.execute(select(Scenario.id, Scenario.pipeline)):
        for node in (pipeline or {}).get("nodes", []):
            ip_ref = node.get("ip_ref")
            if ip_ref and ip_ref not in ip_ids:
                errors.append(
                    f"scenario '{scen_id}': pipeline node ip_ref '{ip_ref}' not in ip_catalog"
                )

    return ValidationReport(errors=errors, warnings=warnings)
```

**Step 2: src/scenario_db/etl/loader.py 수정 (D-03)**

`session.commit()` 이후, `total = sum(counts.values())` 이전에 다음 블록을 삽입:

```python
    # semantic validation — commit 이후 DB 상태 검증 (D-03)
    from scenario_db.etl.validate_loaded import validate_loaded
    _report = validate_loaded(session)
    if _report.errors:
        for _err in _report.errors:
            logger.warning("Validation: %s", _err)
    if _report.warnings:
        for _warn in _report.warnings:
            logger.debug("Validation warning: %s", _warn)
```

import는 circular import 방지를 위해 함수 내부 지역 import로 배치한다.
반환값 `dict[str, int]`는 변경하지 않는다.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_validate_loaded.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/scenario_db/etl/validate_loaded.py` 가 존재한다
    - `grep -c "class ValidationReport" src/scenario_db/etl/validate_loaded.py` == 1
    - `grep -c "def validate_loaded" src/scenario_db/etl/validate_loaded.py` == 1
    - `grep -c "def _issue_affects_scenario" src/scenario_db/etl/validate_loaded.py` == 1
    - `grep -c "from_scenario_db.etl.validate_loaded import validate_loaded" src/scenario_db/etl/loader.py` — 또는 `grep -c "validate_loaded" src/scenario_db/etl/loader.py` >= 1
    - `grep -c "session.commit()" src/scenario_db/etl/loader.py` == 1 (기존 commit 라인 유지)
    - `uv run pytest tests/unit/test_validate_loaded.py -x -q` exits 0
    - `grep -c "model_config = ConfigDict(extra='forbid')" src/scenario_db/etl/validate_loaded.py` == 1
  </acceptance_criteria>
  <done>validate_loaded.py 완성 (8가지 규칙), loader.py에 자동 호출 삽입, unit tests all pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ETL → DB | YAML 파일에서 파싱된 데이터가 SQLAlchemy 세션을 통해 PostgreSQL에 적재됨 |
| validate_loaded → DB | SELECT only — 읽기 전용 쿼리, 사용자 입력 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | validate_loaded() SELECT 쿼리 | accept | SQLAlchemy parameterized query 사용 — 모든 값이 bind parameter로 처리됨. YAML 파싱 시점에 이미 Python native type으로 변환됨 |
| T-01-02 | Information Disclosure | ValidationReport.errors | accept | 내부 DB 상태를 에러 메시지에 포함 — 현재 Phase는 내부 ETL 도구, 외부 노출 없음. Phase 3 API 노출 시 재평가 필요 |
| T-01-03 | Denial of Service | validate_loaded() 전체 테이블 스캔 | accept | 소규모 fixture (수십~수백 레코드). 대용량 데이터 시 인덱스 쿼리로 개선 필요하나 현재 Phase scope 외 |
</threat_model>

<verification>
**Wave 1 완료 후 실행:**
```bash
uv run pytest tests/unit/test_validate_loaded.py -x -q
uv run pytest tests/integration/test_validate_loaded.py -x -q -m integration
```

**Phase gate (모든 plan 완료 후):**
```bash
uv run pytest tests/ -q
```
기존 209개 테스트 + 신규 테스트 모두 green.
</verification>

<success_criteria>
1. `uv run pytest tests/unit/test_validate_loaded.py -x -q` exits 0
2. `uv run pytest tests/integration/test_validate_loaded.py -x -q -m integration` exits 0
3. `grep -c "FHD30-SDR-H265" demo/fixtures/02_definition/uc-camera-recording.yaml` >= 1
4. `grep -c "validate_loaded" src/scenario_db/etl/loader.py` >= 1
5. `uv run pytest tests/ -q` 기존 209개 테스트 regression 없음
</success_criteria>

<output>
After completion, create `.planning/phases/01-db-foundation/01-1-SUMMARY.md`
</output>
