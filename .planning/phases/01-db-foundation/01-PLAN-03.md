---
phase: 1
plan: 3
type: execute
wave: 2
depends_on: ["01-PLAN-01", "01-PLAN-02"]
files_modified:
  - src/scenario_db/db/repositories/view_projection.py
  - tests/integration/test_scenario_graph.py
  - tests/integration/test_view_projection.py
autonomous: true
requirements:
  - DB-03

must_haves:
  truths:
    - "view_projection 쿼리가 Repository 메서드로 캡슐화되어 서비스 레이어에서 ORM 직접 사용 불필요"
    - "get_view_projection(db, scenario_id, variant_id) 호출 시 Level 0 lane data dict를 반환한다"
    - "get_canonical_graph(db, 'uc-camera-recording', 'UHD60-HDR10-H265') 통합 테스트가 green이다"
    - "존재하지 않는 scenario_id 요청 시 None이 반환된다"
    - "기존 209개 테스트가 모두 green이다 (regression 없음)"
  artifacts:
    - path: "src/scenario_db/db/repositories/view_projection.py"
      provides: "get_view_projection() 함수 — Level 0 lane data 조회"
      exports: ["get_view_projection"]
    - path: "tests/integration/test_scenario_graph.py"
      provides: "get_canonical_graph() 통합 테스트"
    - path: "tests/integration/test_view_projection.py"
      provides: "get_view_projection() 통합 테스트"
  key_links:
    - from: "src/scenario_db/db/repositories/view_projection.py"
      to: "Scenario, ScenarioVariant, IpCatalog ORM"
      via: "db.query() 필터 기반 조회"
      pattern: "def get_view_projection"
    - from: "tests/integration/test_scenario_graph.py"
      to: "engine fixture (conftest.py)"
      via: "pytestmark = pytest.mark.integration"
      pattern: "def test_.*\\(engine\\)"
---

<objective>
view_projection Repository 구현 + Phase 1 통합 테스트 완료 (DB-03).

Purpose: Phase 3 view router가 소비할 view_projection 쿼리를 Repository 메서드로 캡슐화한다.
동시에 Plan 01/02에서 생성된 신규 모듈(validate_loaded, scenario_graph)의 통합 테스트를 완성하여
Phase 1 전체 요구사항(DB-01~DB-03)의 integration test coverage를 달성한다.

Output:
- src/scenario_db/db/repositories/view_projection.py (신규)
- tests/integration/test_scenario_graph.py (신규)
- tests/integration/test_view_projection.py (신규)
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db-foundation/01-CONTEXT.md
@.planning/phases/01-db-foundation/01-RESEARCH.md
@.planning/phases/01-db-foundation/01-PATTERNS.md
@.planning/phases/01-db-foundation/01-1-SUMMARY.md
@.planning/phases/01-db-foundation/01-2-SUMMARY.md

<interfaces>
<!-- Plan 01/02 완료 후 존재하는 인터페이스 -->

From src/scenario_db/etl/validate_loaded.py (Plan 01 산출물):
```python
class ValidationReport(BaseModel):
    model_config = ConfigDict(extra='forbid')
    errors: list[str] = []
    warnings: list[str] = []

    @property
    def is_valid(self) -> bool: ...

def validate_loaded(session: Session) -> ValidationReport: ...
```

From src/scenario_db/db/repositories/scenario_graph.py (Plan 02 산출물):
```python
class CanonicalScenarioGraph(BaseModel):
    scenario_id: str
    variant_id: str
    scenario: ScenarioRecord
    variant: VariantRecord
    project: ProjectRecord | None
    pipeline: dict
    ip_catalog: dict[str, IpRecord]
    sw_profiles: dict[str, SwProfileRecord]
    evidence: list[EvidenceRecord]
    issues: list[IssueRecord]
    waivers: list[WaiverRecord]
    reviews: list[ReviewRecord]

def get_canonical_graph(
    db: Session,
    scenario_id: str,
    variant_id: str,
) -> CanonicalScenarioGraph | None: ...
```

From src/scenario_db/db/repositories/evidence.py (패턴 참조):
```python
def list_evidence(db: Session, *, scenario_ref: str | None = None, ...) -> tuple[list[Evidence], int]:
    q = db.query(Evidence)
    if scenario_ref is not None:
        q = q.filter(Evidence.scenario_ref == scenario_ref)
    return q.all(), q.count()
```

From src/scenario_db/db/models/definition.py (view_projection 조회 대상):
```python
class Scenario(Base):
    id: str
    pipeline: JSONB  # {nodes: [{ip_ref, instance_id, lane_id, ...}]}
    metadata_: JSONB  # {name: str, ...}

class ScenarioVariant(Base):
    scenario_id: str (PK1)
    id: str (PK2)
    ip_requirements: JSONB
    sw_requirements: JSONB
```

From tests/integration/conftest.py:
```python
# engine: session-scoped, PostgreSQL (testcontainers), ETL + validate_loaded 완료 상태
# FHD30-SDR-H265 variant는 Plan 01에서 fixture에 추가되었으므로 존재함
DEMO_FIXTURES = Path(__file__).parent.parent.parent / "demo" / "fixtures"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: view_projection.py 구현</name>
  <read_first>
    - src/scenario_db/db/repositories/view_projection.py (존재하지 않음 — 신규 생성)
    - src/scenario_db/db/repositories/evidence.py (query 패턴 참조)
    - src/scenario_db/view/service.py (project_level0() stub 확인 — 반환 구조 참조)
    - src/scenario_db/db/models/definition.py (Scenario.pipeline JSONB 구조 확인)
    - src/scenario_db/db/models/capability.py (IpCatalog 필드 확인)
  </read_first>
  <files>
    src/scenario_db/db/repositories/view_projection.py
  </files>
  <action>
**D-10 per CONTEXT.md: view_projection 쿼리 캡슐화**

`src/scenario_db/db/repositories/view_projection.py` 신규 생성.

이 Repository는 Phase 3 view router (`mode=architecture|topology` 파라미터)가 소비할
Level 0 lane data를 조회한다. Phase 4에서 Streamlit viewer에 연동된다.

파일 구조:
```python
from __future__ import annotations

from sqlalchemy.orm import Session

from scenario_db.db.models.capability import IpCatalog
from scenario_db.db.models.definition import Project, Scenario, ScenarioVariant


def get_view_projection(
    db: Session,
    scenario_id: str,
    variant_id: str,
) -> dict | None:
    """
    Level 0 lane data 조회 — Phase 3 view router가 소비.

    반환 구조:
    {
        "scenario_id": str,
        "variant_id": str,
        "project_name": str | None,
        "pipeline": dict,          # scenario.pipeline JSONB 그대로
        "ip_catalog": list[dict],  # pipeline 노드가 참조하는 IpCatalog 목록
        "lanes": list[dict],       # pipeline.nodes에서 lane_id 기준으로 그룹화
    }
    존재하지 않는 scenario_id 또는 variant_id → None 반환.
    """
    # Scenario 조회
    scenario = db.query(Scenario).filter_by(id=scenario_id).one_or_none()
    if scenario is None:
        return None

    # Variant 조회 (복합 PK: scenario_id + id)
    variant = (
        db.query(ScenarioVariant)
        .filter_by(scenario_id=scenario_id, id=variant_id)
        .one_or_none()
    )
    if variant is None:
        return None

    # Project name 조회 (optional)
    project = db.query(Project).filter_by(id=scenario.project_ref).one_or_none()
    project_name = (project.metadata_ or {}).get("name") if project else None

    # Pipeline nodes에서 ip_ref 수집 → IpCatalog 조회
    pipeline = scenario.pipeline or {}
    nodes = pipeline.get("nodes", [])
    ip_refs = {node["ip_ref"] for node in nodes if "ip_ref" in node}
    if ip_refs:
        ip_rows = db.query(IpCatalog).filter(IpCatalog.id.in_(ip_refs)).all()
    else:
        ip_rows = []
    ip_catalog = [
        {
            "id": ip.id,
            "category": ip.category,
            "hierarchy": ip.hierarchy,
            "capabilities": ip.capabilities,
        }
        for ip in ip_rows
    ]

    # Lane 그룹화: pipeline.nodes를 lane_id 기준으로 그룹화
    # lane_id가 없는 노드는 "default" lane에 배치
    lanes_map: dict[str, list[dict]] = {}
    for node in nodes:
        lane_id = node.get("lane_id", "default")
        lanes_map.setdefault(lane_id, []).append(node)
    lanes = [
        {"lane_id": lid, "nodes": lane_nodes}
        for lid, lane_nodes in lanes_map.items()
    ]

    return {
        "scenario_id": scenario_id,
        "variant_id": variant_id,
        "project_name": project_name,
        "pipeline": pipeline,
        "ip_catalog": ip_catalog,
        "lanes": lanes,
    }
```
  </action>
  <verify>
    <automated>uv run python -c "from scenario_db.db.repositories.view_projection import get_view_projection; print('import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/scenario_db/db/repositories/view_projection.py` 가 존재한다
    - `grep -c "def get_view_projection" src/scenario_db/db/repositories/view_projection.py` == 1
    - `grep -c "filter_by(scenario_id=scenario_id, id=variant_id)" src/scenario_db/db/repositories/view_projection.py` == 1
    - `grep -c "def get_view_projection" src/scenario_db/db/repositories/view_projection.py` == 1 (서비스 레이어에 ORM 직접 노출 없이 캡슐화됨)
    - `uv run python -c "from scenario_db.db.repositories.view_projection import get_view_projection; print('OK')"` exits 0
  </acceptance_criteria>
  <done>get_view_projection() 구현 완료. 반환값: Level 0 lane data dict | None</done>
</task>

<task type="auto">
  <name>Task 2: 통합 테스트 완성 + Phase 1 최종 검증</name>
  <read_first>
    - tests/integration/conftest.py (engine fixture 구조 재확인)
    - tests/integration/test_validate_loaded.py (Plan 01 통합 테스트 파일 — marker 패턴 참조)
    - src/scenario_db/db/repositories/scenario_graph.py (get_canonical_graph 시그니처 확인)
    - src/scenario_db/db/repositories/view_projection.py (Task 1 산출물)
    - .planning/phases/01-db-foundation/01-RESEARCH.md §Code Examples (test_canonical_graph 패턴)
  </read_first>
  <files>
    tests/integration/test_scenario_graph.py,
    tests/integration/test_view_projection.py
  </files>
  <action>
**Step 1: tests/integration/test_scenario_graph.py 작성**

```python
from __future__ import annotations

import pytest
from sqlalchemy.orm import Session

from scenario_db.db.repositories.scenario_graph import get_canonical_graph

pytestmark = pytest.mark.integration

SCENARIO_ID = "uc-camera-recording"
VARIANT_ID = "UHD60-HDR10-H265"
FHD_VARIANT_ID = "FHD30-SDR-H265"


def test_canonical_graph_demo_scenario(engine):
    """uc-camera-recording + UHD60-HDR10-H265 canonical graph 로드."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, VARIANT_ID)

    assert graph is not None
    assert graph.scenario_id == SCENARIO_ID
    assert graph.variant_id == VARIANT_ID
    assert graph.project is not None
    assert graph.pipeline is not None
    assert isinstance(graph.ip_catalog, dict)
    assert isinstance(graph.evidence, list)
    assert isinstance(graph.issues, list)
    assert isinstance(graph.waivers, list)
    assert isinstance(graph.reviews, list)


def test_canonical_graph_fhd_variant(engine):
    """FHD30-SDR-H265 variant canonical graph 로드 (Plan 01에서 fixture 추가됨)."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, FHD_VARIANT_ID)

    assert graph is not None
    assert graph.scenario_id == SCENARIO_ID
    assert graph.variant_id == FHD_VARIANT_ID


def test_canonical_graph_not_found_scenario(engine):
    """존재하지 않는 scenario_id → None 반환."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, "no-such-scenario", "no-variant")

    assert graph is None


def test_canonical_graph_not_found_variant(engine):
    """존재하는 scenario + 존재하지 않는 variant_id → None 반환."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, "no-such-variant")

    assert graph is None


def test_canonical_graph_scenario_fields(engine):
    """반환된 graph의 scenario 필드가 ScenarioRecord 구조를 갖춘다."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, VARIANT_ID)

    assert graph is not None
    assert graph.scenario.id == SCENARIO_ID
    assert graph.scenario.project_ref is not None
    assert isinstance(graph.scenario.pipeline, dict)


def test_canonical_graph_no_sa_instance_state(engine):
    """CanonicalScenarioGraph에 _sa_instance_state 필드가 없다 (ORM 오염 없음)."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, VARIANT_ID)

    assert graph is not None
    # model_dump()에 _sa_instance_state가 포함되면 from_attributes=True 미적용 증거
    dumped = graph.model_dump()
    assert "_sa_instance_state" not in dumped
    assert "_sa_instance_state" not in dumped.get("scenario", {})
```

**Step 2: tests/integration/test_view_projection.py 작성**

```python
from __future__ import annotations

import pytest
from sqlalchemy.orm import Session

from scenario_db.db.repositories.view_projection import get_view_projection

pytestmark = pytest.mark.integration

SCENARIO_ID = "uc-camera-recording"
VARIANT_ID = "UHD60-HDR10-H265"


def test_view_projection_demo_scenario(engine):
    """uc-camera-recording + UHD60-HDR10-H265 Level 0 lane data 조회."""
    with Session(engine) as session:
        result = get_view_projection(session, SCENARIO_ID, VARIANT_ID)

    assert result is not None
    assert result["scenario_id"] == SCENARIO_ID
    assert result["variant_id"] == VARIANT_ID
    assert "pipeline" in result
    assert "lanes" in result
    assert "ip_catalog" in result
    assert isinstance(result["lanes"], list)
    assert isinstance(result["ip_catalog"], list)


def test_view_projection_not_found(engine):
    """존재하지 않는 scenario_id → None 반환."""
    with Session(engine) as session:
        result = get_view_projection(session, "no-such-scenario", "no-variant")

    assert result is None


def test_view_projection_not_found_variant(engine):
    """존재하는 scenario + 존재하지 않는 variant_id → None 반환."""
    with Session(engine) as session:
        result = get_view_projection(session, SCENARIO_ID, "no-such-variant")

    assert result is None


def test_view_projection_has_project_name(engine):
    """project_name 필드가 존재한다 (None 또는 str)."""
    with Session(engine) as session:
        result = get_view_projection(session, SCENARIO_ID, VARIANT_ID)

    assert result is not None
    assert "project_name" in result
```

**Step 3: Phase 1 최종 회귀 검증**

모든 테스트 실행:
```bash
uv run pytest tests/ -q
```
기존 209개 테스트 + 신규 테스트 (unit + integration) 모두 green이어야 한다.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_scenario_graph.py tests/integration/test_view_projection.py tests/integration/test_validate_loaded.py -x -q -m integration</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/test_scenario_graph.py` 가 존재하고 `pytestmark = pytest.mark.integration` 이 있다
    - `tests/integration/test_view_projection.py` 가 존재하고 `pytestmark = pytest.mark.integration` 이 있다
    - `uv run pytest tests/integration/test_scenario_graph.py -x -q -m integration` exits 0
    - `uv run pytest tests/integration/test_view_projection.py -x -q -m integration` exits 0
    - `uv run pytest tests/integration/test_validate_loaded.py -x -q -m integration` exits 0
    - `uv run pytest tests/ -q` exits 0 (기존 209개 + 신규 테스트 모두 green)
    - `grep -c "test_canonical_graph_no_sa_instance_state" tests/integration/test_scenario_graph.py` == 1
  </acceptance_criteria>
  <done>Phase 1 전체 통합 테스트 green. DB-01/DB-02/DB-03 요구사항 모두 검증 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 3 API → get_view_projection() | Phase 3에서 scenario_id, variant_id가 HTTP 경로 파라미터로 입력됨 |
| view_projection → DB | SELECT only — 읽기 전용 쿼리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Injection | get_view_projection() — scenario_id, variant_id | accept | `db.query(Model).filter_by(id=scenario_id)` — SQLAlchemy parameterized query. Phase 3에서 API 경로 파라미터로 노출될 때 FastAPI path parameter validation이 추가됨 |
| T-03-02 | Information Disclosure | get_view_projection() 반환값 | accept | pipeline JSONB + ip_catalog 전체 반환 — Phase 1은 내부 서비스. Phase 3 API 노출 시 필드 필터링 검토 필요 |
| T-03-03 | Tampering | IpCatalog.in_() 쿼리 | accept | `IpCatalog.id.in_(ip_refs)` — ip_refs는 YAML fixture에서 파싱된 Python set, 사용자 입력 직접 주입 경로 없음 |
</threat_model>

<verification>
**Wave 2 완료 후 전체 Phase 1 검증:**
```bash
# 통합 테스트만 실행 (느림 — testcontainers 포함)
uv run pytest tests/integration/ -q -m integration

# 전체 suite (기존 209 + 신규)
uv run pytest tests/ -q
```

**개별 확인:**
```bash
# DB-01 검증
uv run pytest tests/unit/test_validate_loaded.py tests/integration/test_validate_loaded.py -q

# DB-02 검증
uv run pytest tests/unit/test_scenario_graph_models.py tests/integration/test_scenario_graph.py -q -m integration

# DB-03 검증
uv run pytest tests/integration/test_view_projection.py -q -m integration
```
</verification>

<success_criteria>
1. `uv run pytest tests/ -q` exits 0 (기존 209개 + 신규 테스트 모두 green)
2. `grep -c "def get_view_projection" src/scenario_db/db/repositories/view_projection.py` == 1
3. `uv run pytest tests/integration/ -q -m integration` exits 0
4. Phase 1 goal 달성: "DB에서 scenario 전체 그래프를 안전하게 조회할 수 있다"
   - ETL 로드 후 semantic validation 자동 실행 (DB-01 ✓)
   - get_canonical_graph()로 단일 DTO 조회 (DB-02 ✓)
   - view_projection + scenario_graph Repository 캡슐화 (DB-03 ✓)
</success_criteria>

<output>
After completion, create `.planning/phases/01-db-foundation/01-3-SUMMARY.md`
</output>
