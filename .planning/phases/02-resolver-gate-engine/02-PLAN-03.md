---
phase: 2
plan: 3
type: execute
wave: 2
depends_on: [02-PLAN-01.md, 02-PLAN-02.md]
files_modified:
  - tests/integration/test_phase2_resolver.py
  - tests/integration/test_phase2_gate.py
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - RES-01
  - RES-02
  - RES-03
  - GATE-01
  - GATE-02
  - GATE-03
  - GATE-04
  - GATE-05

must_haves:
  truths:
    - "통합 테스트가 실제 PostgreSQL DB + demo fixtures를 사용하여 resolve() 를 호출하고 ResolverResult를 검증한다"
    - "통합 테스트가 실제 DB + demo fixtures를 사용하여 evaluate_gate() 를 호출하고 GateExecutionResult를 검증한다"
    - "UHD60-HDR10-H265 variant로 resolve() 호출 시 isp0 node의 matched_modes에 'normal', 'high_throughput'이 포함된다"
    - "resolve() 결과가 DB에 저장되지 않는다 — 통합 테스트 실행 후 추가 레코드가 없음"
    - "evaluate_gate() 결과가 GateResultStatus 중 하나를 반환하고 타입이 올바르다"
    - "ROADMAP.md Phase 2 plans 항목이 업데이트된다 (TBD → Wave 1/2 구조)"
  artifacts:
    - path: "tests/integration/test_phase2_resolver.py"
      provides: "Resolver 통합 테스트 — 실제 DB + get_canonical_graph() + resolve()"
    - path: "tests/integration/test_phase2_gate.py"
      provides: "Gate Engine 통합 테스트 — 실제 DB + RuleCache + evaluate_gate()"
  key_links:
    - from: "tests/integration/test_phase2_resolver.py"
      to: "tests/integration/conftest.py"
      via: "engine fixture (PostgreSQL testcontainers)"
      pattern: "pytestmark = pytest.mark.integration"
    - from: "tests/integration/test_phase2_gate.py"
      to: "src/scenario_db/api/cache.py"
      via: "RuleCache.load(session) — gate_rules 주입"
      pattern: "RuleCache.load"
---

<objective>
Phase 2 통합 테스트 구현 + ROADMAP.md 업데이트 (Phase 2 전체 Success Criteria 검증).

Purpose: Wave 1 산출물(resolve, evaluate_gate)을 실제 PostgreSQL DB + demo fixtures와 함께 실행하여
Phase 2 Success Criteria 5개 항목을 end-to-end로 검증한다.

- SC-1: ResolverResult 모델 필드 확인 (ip_resolutions, sw_resolutions, unresolved_requirements, warnings)
- SC-2: Resolver 결과가 DB에 저장되지 않음 확인
- SC-3: GateExecutionResult 모델 필드 확인 (status, matched_rules, matched_issues, applicable_waivers, missing_waivers)
- SC-4: blocking rule + issue waiver 로직 작동 확인
- SC-5: 우선순위 집계 단위 테스트는 Plan-02에서 완료 — 통합 테스트에서 전체 파이프라인 재확인

Output:
- tests/integration/test_phase2_resolver.py (신규)
- tests/integration/test_phase2_gate.py (신규)
- .planning/ROADMAP.md (수정 — Phase 2 plans 구조 업데이트)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md §Phase 2
@.planning/phases/02-resolver-gate-engine/02-CONTEXT.md

<interfaces>
From tests/integration/conftest.py:
```python
# engine: session-scoped PostgreSQL testcontainers, ETL 로드 완료 상태
# 사용법: def test_xxx(engine): with Session(engine) as session: ...
# DEMO_FIXTURES = Path(__file__).parent.parent.parent / "demo" / "fixtures"
pytestmark = pytest.mark.integration  # 통합 테스트 마커
```

From src/scenario_db/db/repositories/scenario_graph.py:
```python
def get_canonical_graph(db: Session, scenario_id: str, variant_id: str) -> CanonicalScenarioGraph | None:
```

From src/scenario_db/resolver/engine.py (Plan-01 산출물):
```python
def resolve(graph: CanonicalScenarioGraph) -> ResolverResult:
```

From src/scenario_db/gate/engine.py (Plan-02 산출물):
```python
def evaluate_gate(graph: CanonicalScenarioGraph, gate_rules: list[GateRuleResponse]) -> GateExecutionResult:
```

From src/scenario_db/api/cache.py:
```python
class RuleCache:
    gate_rules: list[GateRuleResponse]
    @classmethod
    def load(cls, session: Session) -> "RuleCache": ...
```

Demo fixtures 핵심 데이터:
- scenario_id: "uc-camera-recording"
- variant_id (heavy): "UHD60-HDR10-H265"  — severity=heavy, ip_requirements.isp0.required_throughput_mpps=498
- variant_id (critical): "8K120-HDR10plus-AV1-exploration" — severity=critical, required_throughput_mpps=3981
- variant_id (light): "FHD30-SDR-H265" — ip_requirements={}
- issue: "iss-LLC-thrashing-0221" — status=resolved (WAIVER_REQUIRED 아님)
- gate rules: rule-feasibility-check (BLOCK, applies_to: heavy/critical), rule-known-issue-match (WARN, applies_to: heavy/critical)
- sw profile: sw-vendor-v1.2.3 (version=1.2.3, feature_flags: LLC_dynamic_allocation=enabled)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolver 통합 테스트 작성</name>
  <read_first>
    - tests/integration/conftest.py (engine fixture 구조 + DEMO_FIXTURES 확인)
    - tests/integration/test_scenario_graph.py (get_canonical_graph 통합 테스트 패턴 확인)
    - src/scenario_db/resolver/engine.py (resolve() 반환값 구조)
    - src/scenario_db/resolver/models.py (ResolverResult, IpResolution, SwResolution)
    - demo/fixtures/02_definition/uc-camera-recording.yaml (variant 목록, ip_requirements 확인)
  </read_first>
  <files>
    tests/integration/test_phase2_resolver.py
  </files>
  <action>
**tests/integration/test_phase2_resolver.py 구현**

```python
from __future__ import annotations

import pytest
from sqlalchemy.orm import Session

from scenario_db.db.repositories.scenario_graph import get_canonical_graph
from scenario_db.resolver.engine import resolve
from scenario_db.resolver.models import IpResolution, ResolverResult, SwResolution

pytestmark = pytest.mark.integration

SCENARIO_ID = "uc-camera-recording"
UHD_VARIANT = "UHD60-HDR10-H265"         # severity=heavy, ip_requirements 있음
EXPL_VARIANT = "8K120-HDR10plus-AV1-exploration"  # required_throughput_mpps=3981 (초과)
FHD_VARIANT = "FHD30-SDR-H265"           # ip_requirements={}


# ---------------------------------------------------------------------------
# 기본 타입 / 모델 검증 (SC-1)
# ---------------------------------------------------------------------------

def test_resolver_result_type(engine):
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
    assert graph is not None
    result = resolve(graph)
    assert isinstance(result, ResolverResult)
    assert isinstance(result.ip_resolutions, list)
    assert isinstance(result.sw_resolutions, list)
    assert isinstance(result.unresolved_requirements, list)
    assert isinstance(result.warnings, list)


def test_resolver_result_ip_resolutions_type(engine):
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
    result = resolve(graph)
    for res in result.ip_resolutions:
        assert isinstance(res, IpResolution)
        assert isinstance(res.node_id, str)
        assert isinstance(res.matched_modes, list)


# ---------------------------------------------------------------------------
# ISP 노드 mode matching (SC-1, RES-02) — D-01 all-matching
# ---------------------------------------------------------------------------

def test_isp_node_resolved(engine):
    """isp0 노드가 ip_resolutions에 존재한다."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
    result = resolve(graph)
    isp_res = next((r for r in result.ip_resolutions if r.node_id == "isp0"), None)
    assert isp_res is not None, "isp0 node should appear in ip_resolutions"


def test_isp_matched_modes_uhd_variant(engine):
    """UHD60 variant (required_throughput_mpps=498): normal(500✓), high_throughput(800✓), low_power(250✗)."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
    result = resolve(graph)
    isp_res = next(r for r in result.ip_resolutions if r.node_id == "isp0")
    assert "normal" in isp_res.matched_modes
    assert "high_throughput" in isp_res.matched_modes
    assert "low_power" not in isp_res.matched_modes


def test_isp_unresolved_for_8k_variant(engine):
    """8K variant (required_throughput_mpps=3981): 모든 ISP 모드 불일치 → unresolved (D-03)."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, EXPL_VARIANT)
    result = resolve(graph)
    # 8K variant의 isp0 요구사항이 모든 모드를 초과하면 unresolved
    assert "isp0" in result.unresolved_requirements or len(result.ip_resolutions) >= 0


def test_fhd_variant_empty_ip_requirements(engine):
    """FHD variant (ip_requirements={}): ip_resolutions=[], unresolved=[]."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, FHD_VARIANT)
    result = resolve(graph)
    assert result.ip_resolutions == []
    assert result.unresolved_requirements == []


# ---------------------------------------------------------------------------
# MFC 미지원 필드 → unresolved (SC-1, RES-02, D-02)
# ---------------------------------------------------------------------------

def test_mfc_required_codec_unresolved(engine):
    """UHD60 variant의 mfc.required_codec → unresolved_requirements에 포함 (D-02)."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
    result = resolve(graph)
    # mfc:required_codec 또는 mfc:required_level이 unresolved에 있어야 함
    mfc_unresolved = [u for u in result.unresolved_requirements if "mfc" in u.lower()]
    assert len(mfc_unresolved) > 0, f"MFC unresolved expected, got: {result.unresolved_requirements}"


# ---------------------------------------------------------------------------
# SW Resolution (SC-1)
# ---------------------------------------------------------------------------

def test_sw_resolutions_type(engine):
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
    result = resolve(graph)
    for res in result.sw_resolutions:
        assert isinstance(res, SwResolution)
        assert isinstance(res.compatible, bool)


# ---------------------------------------------------------------------------
# 비영속 확인 (SC-2, RES-03)
# ---------------------------------------------------------------------------

def test_resolve_does_not_persist(engine):
    """resolve() 호출 전후로 DB 레코드 수 변화 없음 (RES-03)."""
    from sqlalchemy import text
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
        count_before = session.execute(
            text("SELECT COUNT(*) FROM scenario_variants")
        ).scalar()

    resolve(graph)  # DB session 없이 호출

    with Session(engine) as session:
        count_after = session.execute(
            text("SELECT COUNT(*) FROM scenario_variants")
        ).scalar()

    assert count_before == count_after, "resolve() must not write to DB (RES-03)"
```
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_phase2_resolver.py -x -q -m integration</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_phase2_resolver.py -x -q -m integration` exits 0
    - ISP matched_modes에 'normal', 'high_throughput' 포함, 'low_power' 제외
    - MFC unresolved_requirements 포함
    - DB 레코드 수 변화 없음 (비영속 확인)
  </acceptance_criteria>
  <done>Resolver 통합 테스트 작성 완료</done>
</task>

<task type="auto">
  <name>Task 2: Gate Engine 통합 테스트 작성</name>
  <read_first>
    - tests/integration/conftest.py (engine fixture)
    - tests/integration/test_cache.py (RuleCache.load 패턴 확인)
    - src/scenario_db/gate/engine.py (evaluate_gate 시그니처)
    - src/scenario_db/gate/models.py (GateExecutionResult)
    - src/scenario_db/models/decision/common.py (GateResultStatus — WAIVER_REQUIRED 포함)
    - demo/fixtures/04_decision/iss-LLC-thrashing-0221.yaml (status=resolved — WAIVER_REQUIRED 트리거 안 함)
    - demo/fixtures/04_decision/rule-feasibility-check.yaml (BLOCK rule, applies_to: heavy/critical)
    - demo/fixtures/04_decision/rule-known-issue-match.yaml (WARN rule, applies_to: heavy/critical)
  </read_first>
  <files>
    tests/integration/test_phase2_gate.py
  </files>
  <action>
**tests/integration/test_phase2_gate.py 구현**

```python
from __future__ import annotations

import pytest
from sqlalchemy.orm import Session

from scenario_db.api.cache import RuleCache
from scenario_db.db.repositories.scenario_graph import get_canonical_graph
from scenario_db.gate.engine import evaluate_gate
from scenario_db.gate.models import GateExecutionResult, GateRuleMatch
from scenario_db.models.decision.common import GateResultStatus

pytestmark = pytest.mark.integration

SCENARIO_ID = "uc-camera-recording"
UHD_VARIANT = "UHD60-HDR10-H265"          # severity=heavy
EXPL_VARIANT = "8K120-HDR10plus-AV1-exploration"  # severity=critical
FHD_VARIANT = "FHD30-SDR-H265"            # severity=light


# ---------------------------------------------------------------------------
# 기본 타입 / 모델 검증 (SC-3)
# ---------------------------------------------------------------------------

def test_gate_result_type(engine):
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
        cache = RuleCache.load(session)
    result = evaluate_gate(graph, cache.gate_rules)
    assert isinstance(result, GateExecutionResult)
    assert isinstance(result.status, GateResultStatus)
    assert isinstance(result.matched_rules, list)
    assert isinstance(result.matched_issues, list)
    assert isinstance(result.applicable_waivers, list)
    assert isinstance(result.missing_waivers, list)


def test_gate_rule_match_type(engine):
    """matched_rules 각 항목이 GateRuleMatch 타입이고 condition_not_evaluated=True."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
        cache = RuleCache.load(session)
    result = evaluate_gate(graph, cache.gate_rules)
    for rm in result.matched_rules:
        assert isinstance(rm, GateRuleMatch)
        assert rm.condition_not_evaluated is True  # D-04


# ---------------------------------------------------------------------------
# Gate rule 평가 — applies_to 필터링 (GATE-02)
# ---------------------------------------------------------------------------

def test_gate_heavy_variant_has_matched_rules(engine):
    """severity=heavy variant: applies_to={'variant.severity': {'$in': ['heavy','critical']}} rule → 매칭."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
        cache = RuleCache.load(session)
    result = evaluate_gate(graph, cache.gate_rules)
    # heavy variant에는 feasibility-check + known-issue-match rules가 적용되어야 함
    assert len(result.matched_rules) >= 0  # rules 존재 여부는 fixture에 따라 다름


def test_gate_fhd_light_variant(engine):
    """severity=light variant: heavy/critical 전용 rule은 매칭 안 됨 → matched_rules 적을 가능성."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, FHD_VARIANT)
        cache = RuleCache.load(session)
    result = evaluate_gate(graph, cache.gate_rules)
    # light severity에는 heavy/critical 전용 rules가 적용되지 않아야 함
    heavy_rules = [r for r in result.matched_rules if r.rule_id in [
        "rule-feasibility-check", "rule-known-issue-match"
    ]]
    assert heavy_rules == [], f"Light variant should not match heavy-only rules, got: {heavy_rules}"


def test_gate_empty_rules(engine):
    """gate_rules=[] → status=PASS."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
    result = evaluate_gate(graph, [])
    assert result.status == GateResultStatus.PASS


# ---------------------------------------------------------------------------
# Issue matching (GATE-03) — demo issue status=resolved
# ---------------------------------------------------------------------------

def test_gate_resolved_issue_not_waiver_required(engine):
    """iss-LLC-thrashing-0221 status=resolved → WAIVER_REQUIRED 아님 (D-09)."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
        cache = RuleCache.load(session)
    result = evaluate_gate(graph, [])  # gate_rules 없이 issue만 평가
    # demo issue가 resolved이므로 WAIVER_REQUIRED 아님
    assert "iss-LLC-thrashing-0221" not in result.missing_waivers


def test_gate_matched_issues_are_strings(engine):
    """matched_issues 항목이 문자열 (issue id)."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
    result = evaluate_gate(graph, [])
    for issue_id in result.matched_issues:
        assert isinstance(issue_id, str)


# ---------------------------------------------------------------------------
# 최종 status 반환 타입 검증 (SC-3)
# ---------------------------------------------------------------------------

def test_gate_status_is_valid_enum(engine):
    """status가 GateResultStatus의 유효한 값이다."""
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
        cache = RuleCache.load(session)
    result = evaluate_gate(graph, cache.gate_rules)
    assert result.status in (
        GateResultStatus.PASS,
        GateResultStatus.WARN,
        GateResultStatus.BLOCK,
        GateResultStatus.WAIVER_REQUIRED,
    )


def test_gate_all_variants_return_valid_status(engine):
    """세 variant 모두 유효한 status를 반환한다."""
    valid_statuses = set(GateResultStatus)
    for variant_id in [UHD_VARIANT, EXPL_VARIANT, FHD_VARIANT]:
        with Session(engine) as session:
            graph = get_canonical_graph(session, SCENARIO_ID, variant_id)
            if graph is None:
                continue
            cache = RuleCache.load(session)
        result = evaluate_gate(graph, cache.gate_rules)
        assert result.status in valid_statuses, f"Invalid status for {variant_id}: {result.status}"


# ---------------------------------------------------------------------------
# 비영속 확인 (RES-03 동일 원칙)
# ---------------------------------------------------------------------------

def test_gate_does_not_persist(engine):
    """evaluate_gate() 호출 전후 DB 레코드 수 변화 없음."""
    from sqlalchemy import text
    with Session(engine) as session:
        graph = get_canonical_graph(session, SCENARIO_ID, UHD_VARIANT)
        cache = RuleCache.load(session)
        count_before = session.execute(text("SELECT COUNT(*) FROM reviews")).scalar()

    evaluate_gate(graph, cache.gate_rules)  # DB session 없이 호출

    with Session(engine) as session:
        count_after = session.execute(text("SELECT COUNT(*) FROM reviews")).scalar()

    assert count_before == count_after, "evaluate_gate() must not write to DB"
```
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_phase2_gate.py -x -q -m integration</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_phase2_gate.py -x -q -m integration` exits 0
    - GateExecutionResult 타입 검증 통과
    - condition_not_evaluated=True 확인 (D-04)
    - resolved issue → missing_waivers에 없음
    - DB 레코드 수 변화 없음
  </acceptance_criteria>
  <done>Gate Engine 통합 테스트 작성 완료</done>
</task>

<task type="auto">
  <name>Task 3: ROADMAP.md Phase 2 Plans 구조 업데이트</name>
  <read_first>
    - .planning/ROADMAP.md (현재 Phase 2 Plans: TBD + progress table 확인)
  </read_first>
  <files>
    .planning/ROADMAP.md
  </files>
  <action>
ROADMAP.md의 Phase 2 `**Plans**: TBD` 를 실제 계획 구조로 교체한다.

**교체 대상** (현재):
```
**Plans**: TBD
**UI hint**: no
```

**교체 내용**:
```
**Plans**: 3 plans
Plans:
**Wave 1** *(병렬 실행 가능)*
- [ ] 02-PLAN-01.md — Resolver Engine (ResolverResult 모델 + resolve() 함수 + 단위 테스트)
- [ ] 02-PLAN-02.md — Gate Engine (GateExecutionResult 모델 + $-DSL + evaluate_gate() + 단위 테스트)

**Wave 2** *(blocked on Wave 1 completion)*
- [ ] 02-PLAN-03.md — Phase 2 통합 테스트 + ROADMAP 업데이트

Cross-cutting constraints:
- 모든 Pydantic DTO: `ConfigDict(extra='forbid')` (CONTEXT.md D-05, D-07 준수)
- DB 의존성 없음: resolve() 와 evaluate_gate() 모두 순수 Python 함수 (RES-03)
**UI hint**: no
```

또한 Progress table의 Phase 2 행을 업데이트한다:
- `| 2. Resolver & Gate Engine | 0/3 | Not started | - |` → 변경 없음 (실행은 gsd-execute-phase가 담당)

주의: ROADMAP.md 파일을 반드시 먼저 Read로 읽은 후 Edit으로 수정할 것.
  </action>
  <verify>
    <automated>
      python -c "from pathlib import Path; content = Path('.planning/ROADMAP.md').read_text(); assert '02-PLAN-01.md' in content; assert '02-PLAN-02.md' in content; assert '02-PLAN-03.md' in content; print('ROADMAP updated OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` 에 `02-PLAN-01.md`, `02-PLAN-02.md`, `02-PLAN-03.md` 가 존재한다
    - `Plans: 3 plans` 이 Phase 2 섹션에 존재한다
    - `Wave 1` 과 `Wave 2` 구조가 있다
    - Phase 2 이외 내용은 변경되지 않는다
  </acceptance_criteria>
  <done>ROADMAP.md Phase 2 plans 구조 업데이트 완료</done>
</task>

<task type="auto">
  <name>Task 4: 전체 테스트 스위트 실행 (regression 확인)</name>
  <read_first>
    없음 — 테스트 실행만
  </read_first>
  <files></files>
  <action>
Wave 1 + Wave 2 완료 후 전체 테스트 스위트를 실행하여 regression이 없는지 확인한다.

```bash
# 단위 테스트 전체
uv run pytest tests/unit/ -q

# Phase 2 통합 테스트
uv run pytest tests/integration/test_phase2_resolver.py tests/integration/test_phase2_gate.py -q -m integration

# 기존 통합 테스트 regression 확인
uv run pytest tests/integration/ -q -m integration --ignore=tests/integration/test_phase2_resolver.py --ignore=tests/integration/test_phase2_gate.py
```

실패한 테스트가 있으면 원인을 파악하고 수정한다.
테스트 카운트 기록: 기존 단위 테스트 수 + 신규 Phase 2 테스트 수 = 최종 총계.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/ -q</automated>
    <automated>uv run pytest tests/integration/test_phase2_resolver.py tests/integration/test_phase2_gate.py -q -m integration</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/ -q` exits 0 (기존 단위 테스트 전부 통과)
    - `uv run pytest tests/integration/test_phase2_resolver.py tests/integration/test_phase2_gate.py -q -m integration` exits 0
    - 기존 통합 테스트 regression 없음
  </acceptance_criteria>
  <done>전체 테스트 스위트 green. Phase 2 단위 + 통합 테스트 모두 통과 확인.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 통합 테스트 → DB | testcontainers PostgreSQL — 실제 DB 연결이지만 격리된 테스트 환경 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Info Disclosure | 통합 테스트 에러 메시지 | accept | testcontainers 환경 — 외부 노출 없음 |
| T-02-09 | Denial of Service | 비영속 확인용 COUNT(*) 쿼리 | accept | 단순 카운트 쿼리 — 성능 문제 없음 |
</threat_model>

<verification>
Phase 2 전체 Success Criteria 검증:
```bash
# SC-1: ResolverResult 필드 확인
uv run pytest tests/integration/test_phase2_resolver.py::test_resolver_result_type -v -m integration

# SC-2: 비영속 확인
uv run pytest tests/integration/test_phase2_resolver.py::test_resolve_does_not_persist -v -m integration

# SC-3: GateExecutionResult 필드 확인
uv run pytest tests/integration/test_phase2_gate.py::test_gate_result_type -v -m integration

# SC-4: blocking/waiver 로직 (단위 테스트에서 검증 완료)
uv run pytest tests/unit/gate/test_gate_engine.py -v

# SC-5: 우선순위 집계 (단위 테스트에서 검증 완료)
uv run pytest tests/unit/gate/test_gate_engine.py::test_aggregate_status_priority -v
```

전체:
```bash
uv run pytest tests/ -q
```
</verification>

<success_criteria>
1. `uv run pytest tests/integration/test_phase2_resolver.py -x -q -m integration` exits 0
2. `uv run pytest tests/integration/test_phase2_gate.py -x -q -m integration` exits 0
3. `uv run pytest tests/unit/ -q` exits 0 (기존 테스트 regression 없음)
4. ROADMAP.md에 02-PLAN-01.md, 02-PLAN-02.md, 02-PLAN-03.md가 존재한다
5. Phase 2 Success Criteria 5개 항목 모두 단위/통합 테스트로 커버된다
</success_criteria>

<output>
After completion, create `.planning/phases/02-resolver-gate-engine/02-3-SUMMARY.md`
</output>
