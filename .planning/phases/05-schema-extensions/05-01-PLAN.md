---
phase: 05-schema-extensions
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scenario_db/models/capability/hw.py
  - src/scenario_db/models/definition/usecase.py
  - src/scenario_db/models/evidence/simulation.py
  - tests/unit/test_schema_extensions.py
  - tests/unit/fixtures/hw/ip-isp-v12-with-sim.yaml
  - tests/unit/fixtures/evidence/sim-FHD30-with-breakdown.yaml
autonomous: true
requirements:
  - SCH-01
  - SCH-02
  - SCH-03
  - SCH-04

must_haves:
  truths:
    - "IPSimParams | PortSpec | PortType 모델이 hw.py에 존재하고, IpCatalog.sim_params: IPSimParams | None = None 필드를 갖는다"
    - "PortInputConfig | IPPortConfig | SimGlobalConfig | SensorSpec 모델이 usecase.py에 존재하고, Variant.sim_port_config / sim_config와 Usecase.sensor 필드가 추가된다"
    - "PortBWResult | IPTimingResult 모델이 simulation.py에 존재하고, SimulationEvidence.dma_breakdown / timing_breakdown 필드가 추가된다"
    - "기존 fixture(sim_params 없는 ip-isp-v12.yaml, breakdown 없는 sim 파일)가 ValidationError 없이 파싱된다 (backward compat)"
    - "신규 fixture(ip-isp-v12-with-sim.yaml, sim-FHD30-with-breakdown.yaml)가 round-trip 직렬화를 통과한다"
  artifacts:
    - path: "src/scenario_db/models/capability/hw.py"
      provides: "PortType(StrEnum), PortSpec, IPSimParams 신규 모델 + IpCatalog.sim_params 필드"
      contains: "class IPSimParams"
    - path: "src/scenario_db/models/definition/usecase.py"
      provides: "PortInputConfig, IPPortConfig, SimGlobalConfig, SensorSpec 신규 모델 + Variant/Usecase 필드 확장"
      contains: "class SensorSpec"
    - path: "src/scenario_db/models/evidence/simulation.py"
      provides: "PortBWResult, IPTimingResult 신규 모델 + SimulationEvidence 필드 확장"
      contains: "class PortBWResult"
    - path: "tests/unit/test_schema_extensions.py"
      provides: "신규 모델 round-trip + backward compat 단위 테스트"
      exports: ["test_ip_catalog_backward_compat_no_sim_params", "test_ip_catalog_sim_params_roundtrip",
                "test_sensor_spec_roundtrip", "test_variant_sim_config_roundtrip",
                "test_port_bw_result_roundtrip", "test_sim_evidence_backward_compat_no_breakdown"]
    - path: "tests/unit/fixtures/hw/ip-isp-v12-with-sim.yaml"
      provides: "sim_params가 있는 IpCatalog 테스트 fixture"
      contains: "sim_params"
    - path: "tests/unit/fixtures/evidence/sim-FHD30-with-breakdown.yaml"
      provides: "dma_breakdown/timing_breakdown가 있는 SimulationEvidence 테스트 fixture"
      contains: "dma_breakdown"
  key_links:
    - from: "IpCatalog.sim_params"
      to: "IPSimParams"
      via: "타입 어노테이션"
      pattern: "sim_params: IPSimParams \\| None = None"
    - from: "Variant.sim_port_config"
      to: "IPPortConfig"
      via: "dict[str, IPPortConfig]"
      pattern: "sim_port_config: dict\\[str, IPPortConfig\\] \\| None = None"
    - from: "SimulationEvidence.dma_breakdown"
      to: "PortBWResult"
      via: "list[PortBWResult]"
      pattern: "dma_breakdown: list\\[PortBWResult\\]"
---

<objective>
Phase 5 Pydantic 모델 레이어 확장 — sim/ 패키지(Phase 6)가 소비할 7개 신규 모델을 정의하고, 기존 3개 최상위 모델(IpCatalog, Variant/Usecase, SimulationEvidence)에 Optional 필드를 추가한다.

Purpose: 스키마 단일 진실 원천(Pydantic 모델)을 먼저 확립하여 Wave 2 ETL 매퍼가 확정된 타입을 참조할 수 있게 한다. ORM/migration 작업(Plan 05-02)과 파일 충돌 없이 병렬 진행 가능하다.

Output:
- 3개 Pydantic 파일 수정 (hw.py, usecase.py, simulation.py)
- 1개 단위 테스트 파일 신규 생성 (test_schema_extensions.py)
- 2개 YAML fixture 신규 생성 (ip-isp-v12-with-sim.yaml, sim-FHD30-with-breakdown.yaml)
</objective>

<execution_context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\05-schema-extensions\05-RESEARCH.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\05-schema-extensions\05-PATTERNS.md
</execution_context>

<context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\ROADMAP.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\STATE.md

<interfaces>
<!-- 수정 대상 파일의 현재 구조 — 읽기 후 이 내용으로 확장한다 -->

From src/scenario_db/models/capability/hw.py (현재 IpCatalog 끝부분):
```python
class IpCatalog(BaseScenarioModel):
    id: DocumentId
    schema_version: SchemaVersion
    kind: Literal["ip"]
    category: str
    hierarchy: IpHierarchy
    capabilities: IpCapabilities
    rtl_version: str | None = None
    compatible_soc: list[DocumentId] = Field(default_factory=list)
    # sim_params 없음 — 신규 추가 대상
```

From src/scenario_db/models/definition/usecase.py (현재 Variant):
```python
class Variant(BaseScenarioModel):
    id: str
    severity: Severity
    design_conditions: dict[str, str | int | float] = Field(default_factory=dict)
    size_overrides: dict[str, str] = Field(default_factory=dict)
    ip_requirements: dict[str, IpRequirementSpec] = Field(default_factory=dict)
    sw_requirements: SwRequirements | None = None
    violation_policy: ViolationPolicy | None = None
    tags: list[str] = Field(default_factory=list)
    derived_from_variant: str | None = None
    design_conditions_override: dict[str, str | int | float] | None = None
    # sim_port_config, sim_config 없음
```

From src/scenario_db/models/evidence/simulation.py (현재 SimulationEvidence 끝부분):
```python
class SimulationEvidence(BaseScenarioModel):
    ...
    ip_breakdown: list[IpBreakdown] = Field(default_factory=list)
    artifacts: list[Artifact] = Field(default_factory=list)
    # dma_breakdown, timing_breakdown 없음
```

BaseScenarioModel (from common.py):
```python
class BaseScenarioModel(BaseModel):
    model_config = ConfigDict(extra="forbid")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hw.py에 IPSimParams + PortSpec + PortType 모델 추가 및 IpCatalog 확장</name>
  <files>src/scenario_db/models/capability/hw.py</files>

  <read_first>
    - src/scenario_db/models/capability/hw.py (전체 — IpCatalog 위치, import 목록, IpCapabilities 블록 확인)
    - src/scenario_db/models/common.py (BaseScenarioModel, DocumentId 정의 확인)
  </read_first>

  <behavior>
    - hw.py의 기존 import에 `from enum import StrEnum` 추가 (이미 있으면 중복 금지)
    - `PortType(StrEnum)` 신규 클래스: DMA_READ / DMA_WRITE / OTF_IN / OTF_OUT
    - `PortSpec(BaseScenarioModel)` 신규 클래스: name(str), type(PortType), max_bw_gbps(float | None = None)
    - `IPSimParams(BaseScenarioModel)` 신규 클래스: hw_name_in_sim(str), ppc(float), unit_power_mw_mp(float), idc(float = 0.0), vdd(str), dvfs_group(str), latency_us(float = 0.0), ports(list[PortSpec] = Field(default_factory=list))
    - IpCatalog 클래스 끝에 `sim_params: IPSimParams | None = None` 추가
    - 삽입 위치: IpCapabilities 블록 아래, IpCatalog 클래스 위에 PortType → PortSpec → IPSimParams 순서로 삽입
  </behavior>

  <action>
hw.py 파일 전체를 읽은 후:

1. 기존 import 블록에 `from enum import StrEnum`이 없으면 추가. 기존 `from typing import Literal` 라인 근처에 배치.

2. `IpCapabilities` 클래스 정의 **아래**, `IpCatalog` 클래스 정의 **위** 위치에 아래 3개 클래스를 삽입:

```python
class PortType(StrEnum):
    DMA_READ  = "DMA_READ"
    DMA_WRITE = "DMA_WRITE"
    OTF_IN    = "OTF_IN"
    OTF_OUT   = "OTF_OUT"


class PortSpec(BaseScenarioModel):
    name: str
    type: PortType
    max_bw_gbps: float | None = None


class IPSimParams(BaseScenarioModel):
    hw_name_in_sim: str
    ppc: float
    unit_power_mw_mp: float
    idc: float = 0.0
    vdd: str
    dvfs_group: str
    latency_us: float = 0.0
    ports: list[PortSpec] = Field(default_factory=list)
```

3. `IpCatalog` 클래스의 마지막 필드(`compatible_soc`) 바로 아래에:
```python
    sim_params: IPSimParams | None = None  # Phase 5 추가 — backward compat Optional
```

주의:
- `extra='forbid'`는 BaseScenarioModel에서 상속되므로 model_config 재선언 금지
- PortType.OTF_IN / OTF_OUT 항목은 값이 "OTF_IN" / "OTF_OUT" (언더스코어 포함)
  </action>

  <verify>
    <automated>cd /d E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB && uv run python -c "from scenario_db.models.capability.hw import IpCatalog, IPSimParams, PortSpec, PortType; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "class IPSimParams" src/scenario_db/models/capability/hw.py` — 출력 있음
    - `grep -n "class PortSpec" src/scenario_db/models/capability/hw.py` — 출력 있음
    - `grep -n "class PortType" src/scenario_db/models/capability/hw.py` — 출력 있음
    - `grep -n "sim_params: IPSimParams" src/scenario_db/models/capability/hw.py` — 출력 있음
    - `grep -n "= None" src/scenario_db/models/capability/hw.py | grep sim_params` — 기본값 None 확인
  </acceptance_criteria>

  <done>hw.py import 성공, IPSimParams/PortSpec/PortType 클래스 정의됨, IpCatalog.sim_params = None 기본값 존재</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: usecase.py에 SensorSpec / IPPortConfig / SimGlobalConfig 추가 및 Variant + Usecase 확장</name>
  <files>src/scenario_db/models/definition/usecase.py</files>

  <read_first>
    - src/scenario_db/models/definition/usecase.py (전체 — Variant / Usecase 클래스 위치, ViolationPolicy 블록, 기존 import 목록 확인)
    - src/scenario_db/models/common.py (DocumentId 타입 확인)
  </read_first>

  <behavior>
    - `PortInputConfig(BaseScenarioModel)`: port(str), format(str), bitwidth(int=8), width(int), height(int), compression(Literal["SBWC","AFBC","disable"]="disable"), comp_ratio(float=1.0), comp_ratio_min(float|None=None), comp_ratio_max(float|None=None), llc_enabled(bool=False), llc_weight(float=1.0), r_w_rate(float=1.0)
    - `IPPortConfig(BaseScenarioModel)`: mode(str="Normal"), sw_margin_override(float|None=None), inputs(list[PortInputConfig]=Field(default_factory=list)), outputs(list[PortInputConfig]=Field(default_factory=list))
    - `SimGlobalConfig(BaseScenarioModel)`: asv_group(int=4), sw_margin(float=0.25), bw_power_coeff(float=80.0), vbat(float=4.0), pmic_eff(float=0.85), h_blank_margin(float=0.05), dvfs_overrides(dict[str,int]=Field(default_factory=dict))
    - `SensorSpec(BaseScenarioModel)`: ip_ref(DocumentId), frame_width(int), frame_height(int), fps(float), v_valid_ratio(float=0.85)
    - Variant에 `sim_port_config: dict[str, IPPortConfig] | None = None` + `sim_config: SimGlobalConfig | None = None` 추가
    - Usecase에 `sensor: SensorSpec | None = None` 추가
  </behavior>

  <action>
usecase.py 파일 전체를 읽은 후:

1. **삽입 위치 결정**: `ViolationPolicy` 클래스 아래, `Variant` 클래스 위에 `PortInputConfig → IPPortConfig → SimGlobalConfig` 3개 클래스 삽입.

2. 아래 코드 블록을 ViolationPolicy 아래에 삽입:
```python
class PortInputConfig(BaseScenarioModel):
    port: str
    format: str
    bitwidth: int = 8
    width: int
    height: int
    compression: Literal["SBWC", "AFBC", "disable"] = "disable"
    comp_ratio: float = 1.0
    comp_ratio_min: float | None = None
    comp_ratio_max: float | None = None
    llc_enabled: bool = False
    llc_weight: float = 1.0
    r_w_rate: float = 1.0


class IPPortConfig(BaseScenarioModel):
    mode: str = "Normal"
    sw_margin_override: float | None = None
    inputs: list[PortInputConfig] = Field(default_factory=list)
    outputs: list[PortInputConfig] = Field(default_factory=list)


class SimGlobalConfig(BaseScenarioModel):
    asv_group: int = 4
    sw_margin: float = 0.25
    bw_power_coeff: float = 80.0
    vbat: float = 4.0
    pmic_eff: float = 0.85
    h_blank_margin: float = 0.05
    dvfs_overrides: dict[str, int] = Field(default_factory=dict)
```

3. `UsecaseMetadata` 클래스 아래, `Usecase` 클래스 위에 `SensorSpec` 클래스 삽입:
```python
class SensorSpec(BaseScenarioModel):
    """OTF 그룹 타이밍 제약 기준 — v_valid_time 기반 처리량."""
    ip_ref: DocumentId
    frame_width: int
    frame_height: int
    fps: float
    v_valid_ratio: float = 0.85
```

4. `Variant` 클래스의 마지막 필드(`design_conditions_override`) 아래에 2개 필드 추가:
```python
    sim_port_config: dict[str, IPPortConfig] | None = None  # Phase 5 추가
    sim_config: SimGlobalConfig | None = None               # Phase 5 추가
```

5. `Usecase` 클래스에서 `references: UsecaseReferences | None = None` 필드 아래에:
```python
    sensor: SensorSpec | None = None  # Phase 5 추가 — Usecase 레벨 OTF 센서 스펙
```
단, Usecase 클래스에 `@model_validator` 데코레이터가 있으면 그 위에 sensor 필드를 배치한다.

주의:
- `Literal`은 이미 import되어 있을 것이므로 중복 import 금지
- `DocumentId`는 기존 import에서 scenario_db.models.common으로부터 이미 import됨 — 중복 금지
  </action>

  <verify>
    <automated>cd /d E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB && uv run python -c "from scenario_db.models.definition.usecase import Usecase, Variant, SensorSpec, SimGlobalConfig, IPPortConfig, PortInputConfig; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "class SensorSpec" src/scenario_db/models/definition/usecase.py` — 출력 있음
    - `grep -n "class PortInputConfig" src/scenario_db/models/definition/usecase.py` — 출력 있음
    - `grep -n "class SimGlobalConfig" src/scenario_db/models/definition/usecase.py` — 출력 있음
    - `grep -n "sim_port_config" src/scenario_db/models/definition/usecase.py` — 출력 있음 (Variant 필드)
    - `grep -n "sensor: SensorSpec" src/scenario_db/models/definition/usecase.py` — 출력 있음 (Usecase 필드)
  </acceptance_criteria>

  <done>usecase.py import 성공, SensorSpec/PortInputConfig/IPPortConfig/SimGlobalConfig 클래스 정의됨, Variant.sim_port_config/sim_config/Usecase.sensor 필드 존재</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: simulation.py에 PortBWResult + IPTimingResult 추가 및 SimulationEvidence 확장</name>
  <files>src/scenario_db/models/evidence/simulation.py</files>

  <read_first>
    - src/scenario_db/models/evidence/simulation.py (전체 — IpBreakdown 위치, SimulationEvidence 클래스 구조, model_validator 유무 확인)
  </read_first>

  <behavior>
    - `PortBWResult(BaseScenarioModel)`: ip(str), port(str), direction(Literal["read","write"]), bw_mbs(float), bw_mbs_worst(float|None=None), bw_power_mw(float), format(str|None=None), compression(str|None=None), llc_enabled(bool=False)
    - `IPTimingResult(BaseScenarioModel)`: ip(str), hw_time_ms(float), required_clock_mhz(float), set_clock_mhz(float), set_voltage_mv(float), feasible(bool)
    - SimulationEvidence에 `dma_breakdown: list[PortBWResult] = Field(default_factory=list)` + `timing_breakdown: list[IPTimingResult] = Field(default_factory=list)` 추가
    - `direction`은 `Literal["read","write"]`만 사용 — PortType enum import 절대 금지 (순환 import 방지)
  </behavior>

  <action>
simulation.py 파일 전체를 읽은 후:

1. `IpBreakdown` 클래스 아래, `SimulationEvidence` 클래스 위 위치에 아래 2개 클래스를 삽입:

```python
class PortBWResult(BaseScenarioModel):
    ip: str
    port: str
    direction: Literal["read", "write"]
    bw_mbs: float
    bw_mbs_worst: float | None = None
    bw_power_mw: float
    format: str | None = None
    compression: str | None = None
    llc_enabled: bool = False


class IPTimingResult(BaseScenarioModel):
    ip: str
    hw_time_ms: float
    required_clock_mhz: float
    set_clock_mhz: float
    set_voltage_mv: float
    feasible: bool
```

2. `SimulationEvidence` 클래스에서 `artifacts: list[Artifact] = Field(default_factory=list)` 필드 아래에 (model_validator 위에) 2개 필드 추가:

```python
    dma_breakdown: list[PortBWResult] = Field(default_factory=list)      # Phase 5 추가
    timing_breakdown: list[IPTimingResult] = Field(default_factory=list)  # Phase 5 추가
```

주의:
- `from scenario_db.models.capability.hw import PortType` 금지 — 순환 import 발생
- `direction`은 반드시 `Literal["read", "write"]` 사용 (문자열 리터럴)
- 기존 `@model_validator`는 위치 변경 없이 그대로 유지
  </action>

  <verify>
    <automated>cd /d E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB && uv run python -c "from scenario_db.models.evidence.simulation import SimulationEvidence, PortBWResult, IPTimingResult; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "class PortBWResult" src/scenario_db/models/evidence/simulation.py` — 출력 있음
    - `grep -n "class IPTimingResult" src/scenario_db/models/evidence/simulation.py` — 출력 있음
    - `grep -n "dma_breakdown" src/scenario_db/models/evidence/simulation.py` — 출력 있음 (SimulationEvidence 필드)
    - `grep -n "Literal\[.read" src/scenario_db/models/evidence/simulation.py` — PortType 아닌 Literal 사용 확인
    - `grep -rn "from scenario_db.models.capability.hw import" src/scenario_db/models/evidence/simulation.py` — 출력 없음 (순환 import 부재 확인)
  </acceptance_criteria>

  <done>simulation.py import 성공, PortBWResult/IPTimingResult 클래스 정의됨, SimulationEvidence.dma_breakdown/timing_breakdown 필드 존재, hw.py 순환 import 없음</done>
</task>

<task type="auto">
  <name>Task 4: 신규 YAML fixture 2종 생성 + test_schema_extensions.py 작성</name>
  <files>
    tests/unit/fixtures/hw/ip-isp-v12-with-sim.yaml,
    tests/unit/fixtures/evidence/sim-FHD30-with-breakdown.yaml,
    tests/unit/test_schema_extensions.py
  </files>

  <read_first>
    - tests/unit/fixtures/hw/ip-isp-v12.yaml (기존 fixture 구조 확인 — sim_params 추가 기준)
    - tests/unit/fixtures/evidence/sim-camera-recording-UHD60-A0-sw123.yaml (기존 evidence fixture 구조)
    - tests/unit/test_capability_models.py (roundtrip helper 패턴 확인, FIXTURES 경로)
    - tests/unit/test_evidence_models.py (backward compat 테스트 패턴 확인)
  </read_first>

  <action>
**Step 1: ip-isp-v12-with-sim.yaml 작성**

기존 ip-isp-v12.yaml 내용을 기반으로, 끝에 sim_params 섹션 추가:
```yaml
# 기존 ip-isp-v12.yaml 내용 전체 복사 후 아래 섹션 추가
sim_params:
  hw_name_in_sim: "ISP"
  ppc: 4.0
  unit_power_mw_mp: 10.5
  idc: 0.5
  vdd: "VDD_INTCAM"
  dvfs_group: "CAM"
  latency_us: 0.0
  ports:
    - name: "RDMA_FE"
      type: "DMA_READ"
      max_bw_gbps: 25.6
    - name: "WDMA_BE"
      type: "DMA_WRITE"
      max_bw_gbps: 12.8
    - name: "CINFIFO"
      type: "OTF_IN"
    - name: "COUTFIFO"
      type: "OTF_OUT"
```

**Step 2: sim-FHD30-with-breakdown.yaml 작성**

기존 sim-camera-recording-UHD60-A0-sw123.yaml 내용 전체 복사 후:
- id를 `sim-camera-recording-FHD30-A0-sw123`으로 변경
- 파일 끝에 아래 섹션 추가 (ip_breakdown, artifacts 뒤에):
```yaml
dma_breakdown:
  - ip: "isp0"
    port: "RDMA_FE"
    direction: "read"
    bw_mbs: 4800.0
    bw_mbs_worst: 5200.0
    bw_power_mw: 384.0
    format: "BAYER"
    compression: "disable"
    llc_enabled: false
  - ip: "isp0"
    port: "WDMA_BE"
    direction: "write"
    bw_mbs: 1584.0
    bw_power_mw: 126.72
    format: "NV12"
    llc_enabled: true
timing_breakdown:
  - ip: "isp0"
    hw_time_ms: 16.67
    required_clock_mhz: 533.0
    set_clock_mhz: 533.0
    set_voltage_mv: 750
    feasible: true
```

**Step 3: test_schema_extensions.py 작성**

전체 파일:
```python
"""Phase 5 Schema Extensions — Pydantic 모델 round-trip + backward compat 단위 테스트.

FIXTURES 경로:
  - fixtures/hw/ip-isp-v12.yaml           (기존 — sim_params 없음)
  - fixtures/hw/ip-isp-v12-with-sim.yaml  (신규 — sim_params 있음)
  - fixtures/evidence/sim-camera-recording-UHD60-A0-sw123.yaml  (기존 — breakdown 없음)
  - fixtures/evidence/sim-FHD30-with-breakdown.yaml             (신규 — breakdown 있음)
"""
from __future__ import annotations

from pathlib import Path

import pytest
import yaml
from pydantic import ValidationError

from scenario_db.models.capability.hw import (
    IpCatalog,
    IPSimParams,
    PortSpec,
    PortType,
)
from scenario_db.models.definition.usecase import (
    IPPortConfig,
    PortInputConfig,
    SensorSpec,
    SimGlobalConfig,
    Usecase,
    Variant,
)
from scenario_db.models.evidence.simulation import (
    IPTimingResult,
    PortBWResult,
    SimulationEvidence,
)

FIXTURES = Path(__file__).parent / "fixtures"


# ---------------------------------------------------------------------------
# Helpers
# ---------------------------------------------------------------------------

def load_yaml(path: Path) -> dict:
    return yaml.safe_load(path.read_text(encoding="utf-8"))


def roundtrip(model_cls, path: Path, **dump_kwargs):
    raw = load_yaml(path)
    obj = model_cls.model_validate(raw)
    serialised = obj.model_dump(exclude_none=True, **dump_kwargs)
    obj2 = model_cls.model_validate(serialised)
    assert obj == obj2
    return obj


# ---------------------------------------------------------------------------
# SCH-01: IpCatalog.sim_params
# ---------------------------------------------------------------------------

def test_ip_catalog_backward_compat_no_sim_params():
    """기존 ip-isp-v12.yaml (sim_params 없음)이 ValidationError 없이 파싱된다."""
    obj = roundtrip(IpCatalog, FIXTURES / "hw" / "ip-isp-v12.yaml")
    assert obj.sim_params is None


def test_ip_catalog_sim_params_roundtrip():
    """sim_params가 있는 fixture가 round-trip 직렬화를 통과한다."""
    obj = roundtrip(IpCatalog, FIXTURES / "hw" / "ip-isp-v12-with-sim.yaml")
    assert obj.sim_params is not None
    assert obj.sim_params.hw_name_in_sim == "ISP"
    assert obj.sim_params.ppc == 4.0
    assert len(obj.sim_params.ports) == 4


def test_ip_sim_params_port_types():
    """PortSpec의 type이 PortType enum으로 정확히 파싱된다."""
    obj = roundtrip(IpCatalog, FIXTURES / "hw" / "ip-isp-v12-with-sim.yaml")
    types = {p.type for p in obj.sim_params.ports}
    assert PortType.DMA_READ in types
    assert PortType.OTF_IN in types


def test_ip_sim_params_inline():
    """IPSimParams inline dict로 직접 생성 가능하다."""
    params = IPSimParams(
        hw_name_in_sim="MFC",
        ppc=2.0,
        unit_power_mw_mp=5.0,
        vdd="VDD_INT",
        dvfs_group="INT",
        ports=[PortSpec(name="RDMA", type=PortType.DMA_READ)],
    )
    assert params.idc == 0.0         # 기본값 확인
    assert params.latency_us == 0.0  # 기본값 확인


# ---------------------------------------------------------------------------
# SCH-02: Variant.sim_port_config + sim_config
# ---------------------------------------------------------------------------

def test_variant_sim_config_defaults():
    """SimGlobalConfig의 모든 필드가 기본값을 가진다."""
    cfg = SimGlobalConfig()
    assert cfg.asv_group == 4
    assert cfg.sw_margin == 0.25
    assert cfg.bw_power_coeff == 80.0
    assert cfg.dvfs_overrides == {}


def test_port_input_config_compression_literal():
    """PortInputConfig.compression이 허용되지 않는 값이면 ValidationError."""
    with pytest.raises(ValidationError):
        PortInputConfig(
            port="RDMA", format="BAYER", width=1920, height=1080,
            compression="INVALID_CODEC",
        )


def test_ip_port_config_roundtrip():
    """IPPortConfig가 dict -> model -> dict 직렬화를 통과한다."""
    raw = {
        "mode": "Normal",
        "inputs": [
            {"port": "RDMA_FE", "format": "BAYER", "width": 4000, "height": 2252}
        ],
    }
    cfg = IPPortConfig.model_validate(raw)
    out = cfg.model_dump(exclude_none=True)
    cfg2 = IPPortConfig.model_validate(out)
    assert cfg == cfg2


def test_usecase_backward_compat_no_sim_config():
    """기존 uc-camera-recording.yaml (sim_port_config 없음)이 ValidationError 없이 파싱된다."""
    obj = roundtrip(Usecase, FIXTURES / "definition" / "uc-camera-recording.yaml")
    for v in obj.variants:
        assert v.sim_port_config is None
        assert v.sim_config is None


# ---------------------------------------------------------------------------
# SCH-03: Usecase.sensor (SensorSpec)
# ---------------------------------------------------------------------------

def test_sensor_spec_roundtrip():
    """SensorSpec inline dict가 round-trip 직렬화를 통과한다."""
    raw = {
        "ip_ref": "ip-csis-v8",
        "frame_width": 4000,
        "frame_height": 3000,
        "fps": 60.0,
    }
    spec = SensorSpec.model_validate(raw)
    assert spec.v_valid_ratio == 0.85  # 기본값 확인
    out = spec.model_dump(exclude_none=True)
    spec2 = SensorSpec.model_validate(out)
    assert spec == spec2


def test_sensor_spec_extra_field_forbidden():
    """SensorSpec에 알 수 없는 필드를 추가하면 ValidationError."""
    with pytest.raises(ValidationError):
        SensorSpec.model_validate({
            "ip_ref": "ip-csis-v8",
            "frame_width": 4000,
            "frame_height": 3000,
            "fps": 60.0,
            "unknown_field": "oops",
        })


def test_usecase_backward_compat_no_sensor():
    """기존 uc-camera-recording.yaml (sensor 없음)이 ValidationError 없이 파싱된다."""
    obj = roundtrip(Usecase, FIXTURES / "definition" / "uc-camera-recording.yaml")
    assert obj.sensor is None


# ---------------------------------------------------------------------------
# SCH-04: SimulationEvidence.dma_breakdown + timing_breakdown
# ---------------------------------------------------------------------------

def test_sim_evidence_backward_compat_no_breakdown():
    """기존 sim-*.yaml (dma_breakdown 없음)이 ValidationError 없이 파싱된다."""
    obj = roundtrip(
        SimulationEvidence,
        FIXTURES / "evidence" / "sim-camera-recording-UHD60-A0-sw123.yaml",
    )
    assert obj.dma_breakdown == []
    assert obj.timing_breakdown == []


def test_port_bw_result_roundtrip():
    """PortBWResult inline dict가 round-trip 직렬화를 통과한다."""
    raw = {
        "ip": "isp0",
        "port": "RDMA_FE",
        "direction": "read",
        "bw_mbs": 4800.0,
        "bw_power_mw": 384.0,
    }
    result = PortBWResult.model_validate(raw)
    assert result.llc_enabled is False   # 기본값 확인
    assert result.bw_mbs_worst is None   # Optional None 확인
    out = result.model_dump(exclude_none=True)
    result2 = PortBWResult.model_validate(out)
    assert result == result2


def test_ip_timing_result_roundtrip():
    """IPTimingResult inline dict가 round-trip 직렬화를 통과한다."""
    raw = {
        "ip": "isp0",
        "hw_time_ms": 16.67,
        "required_clock_mhz": 533.0,
        "set_clock_mhz": 533.0,
        "set_voltage_mv": 750.0,
        "feasible": True,
    }
    result = IPTimingResult.model_validate(raw)
    assert result.feasible is True
    out = result.model_dump(exclude_none=True)
    result2 = IPTimingResult.model_validate(out)
    assert result == result2


def test_port_bw_direction_invalid():
    """PortBWResult.direction이 'read'/'write' 외 값이면 ValidationError."""
    with pytest.raises(ValidationError):
        PortBWResult.model_validate({
            "ip": "isp0",
            "port": "RDMA_FE",
            "direction": "readwrite",   # 잘못된 값
            "bw_mbs": 4800.0,
            "bw_power_mw": 384.0,
        })


def test_sim_evidence_with_breakdown_roundtrip():
    """dma_breakdown + timing_breakdown이 있는 fixture가 round-trip을 통과한다."""
    obj = roundtrip(
        SimulationEvidence,
        FIXTURES / "evidence" / "sim-FHD30-with-breakdown.yaml",
    )
    assert len(obj.dma_breakdown) >= 1
    assert len(obj.timing_breakdown) >= 1
    assert obj.dma_breakdown[0].ip == "isp0"
    assert obj.timing_breakdown[0].feasible is True
```
  </action>

  <verify>
    <automated>cd /d E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB && uv run pytest tests/unit/test_schema_extensions.py -q --tb=short</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "def test_" tests/unit/test_schema_extensions.py` — 13 이상 출력
    - `grep -n "sim-FHD30-with-breakdown.yaml" tests/unit/fixtures/evidence/sim-FHD30-with-breakdown.yaml` 파일 존재 확인
    - `grep -n "sim_params:" tests/unit/fixtures/hw/ip-isp-v12-with-sim.yaml` — 출력 있음
    - `grep -n "dma_breakdown:" tests/unit/fixtures/evidence/sim-FHD30-with-breakdown.yaml` — 출력 있음
    - `uv run pytest tests/unit/test_schema_extensions.py -q` — PASSED, 0 failed
    - `uv run pytest tests/unit/ -q --tb=short` — 기존 테스트 포함 전체 통과 (회귀 없음)
  </acceptance_criteria>

  <done>test_schema_extensions.py의 모든 테스트 통과, 기존 단위 테스트 회귀 없음, 신규 YAML fixture 2종 유효함</done>
</task>

</tasks>

<verification>
Wave 1 완료 기준:

```
uv run pytest tests/unit/test_schema_extensions.py -v
```
- 13개 이상 테스트 PASSED

```
uv run pytest tests/unit/ -q --tb=short
```
- 기존 단위 테스트 전체 회귀 없음

```
uv run python -c "
from scenario_db.models.capability.hw import IpCatalog, IPSimParams, PortSpec, PortType
from scenario_db.models.definition.usecase import Usecase, SensorSpec, IPPortConfig, SimGlobalConfig
from scenario_db.models.evidence.simulation import SimulationEvidence, PortBWResult, IPTimingResult
print('All imports OK')
"
```
- 순환 import 없이 성공
</verification>

<success_criteria>
- [ ] PortType / PortSpec / IPSimParams 클래스가 hw.py에 존재하고 IpCatalog.sim_params = None 기본값 있음
- [ ] PortInputConfig / IPPortConfig / SimGlobalConfig / SensorSpec이 usecase.py에 존재
- [ ] Variant.sim_port_config / sim_config, Usecase.sensor가 None 기본값으로 추가됨
- [ ] PortBWResult / IPTimingResult가 simulation.py에 존재 (PortType import 없음)
- [ ] SimulationEvidence.dma_breakdown / timing_breakdown이 list 기본값으로 추가됨
- [ ] 기존 fixture(sim_params 없는 ip-isp-v12.yaml, breakdown 없는 sim yaml)가 backward compat 테스트 통과
- [ ] 신규 fixture 2종 round-trip 테스트 통과
- [ ] uv run pytest tests/unit/ -q 전체 통과 (회귀 없음)
</success_criteria>

<output>
완료 후 `.planning/phases/05-schema-extensions/05-01-SUMMARY.md` 생성:
- 수정된 Pydantic 모델 파일 3개 목록
- 신규 정의된 클래스 목록 (8개)
- 테스트 통과 수
- backward compat 확인 fixture 목록
</output>
