---
phase: 05-schema-extensions
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scenario_db/db/models/capability.py
  - src/scenario_db/db/models/definition.py
  - src/scenario_db/db/models/evidence.py
  - alembic/versions/0002_schema_extensions.py
autonomous: true
requirements:
  - SCH-01
  - SCH-02
  - SCH-03
  - SCH-04
  - SCH-05

must_haves:
  truths:
    - "IpCatalog ORM 모델에 sim_params = Column(JSONB) 컬럼이 존재한다"
    - "Scenario ORM 모델에 sensor = Column(JSONB) 컬럼이 존재한다"
    - "ScenarioVariant ORM 모델에 sim_port_config + sim_config = Column(JSONB) 컬럼 2개가 존재한다"
    - "Evidence ORM 모델에 dma_breakdown + timing_breakdown = Column(JSONB) 컬럼 2개가 존재한다"
    - "alembic/versions/0002_schema_extensions.py 파일이 존재하고 upgrade() 6개 add_column + downgrade() 6개 drop_column이 일치한다"
    - "0002 migration이 0001을 down_revision으로 참조한다"
  artifacts:
    - path: "src/scenario_db/db/models/capability.py"
      provides: "IpCatalog ORM sim_params JSONB 컬럼"
      contains: "sim_params"
    - path: "src/scenario_db/db/models/definition.py"
      provides: "Scenario ORM sensor, ScenarioVariant ORM sim_port_config/sim_config JSONB 컬럼"
      contains: "sim_port_config"
    - path: "src/scenario_db/db/models/evidence.py"
      provides: "Evidence ORM dma_breakdown/timing_breakdown JSONB 컬럼"
      contains: "dma_breakdown"
    - path: "alembic/versions/0002_schema_extensions.py"
      provides: "Alembic migration — 6개 nullable JSONB 컬럼 추가 + rollback"
      contains: "down_revision = \"0001\""
  key_links:
    - from: "alembic/versions/0002_schema_extensions.py"
      to: "alembic/versions/0001_initial_schema.py"
      via: "down_revision 체인"
      pattern: "down_revision = .0001."
    - from: "ORM IpCatalog.sim_params"
      to: "DB ip_catalog.sim_params 컬럼"
      via: "Column(JSONB) 선언"
      pattern: "sim_params.*Column.*JSONB"
---

<objective>
Phase 5 ORM + Alembic migration 레이어 확장 — 4개 ORM 파일에 총 6개 nullable JSONB 컬럼을 추가하고, Alembic migration 0002를 수동 작성하여 `alembic upgrade head`로 기존 DB에 적용 가능하게 한다.

Purpose: Plan 05-01(Pydantic 모델)과 파일 충돌 없이 Wave 1에서 병렬 진행. Wave 2 ETL 매퍼(Plan 05-03)가 이 ORM 컬럼과 migration 파일에 의존한다.

Output:
- 3개 ORM 파일 수정 (db/models/capability.py, definition.py, evidence.py)
- 1개 Alembic migration 파일 신규 생성 (0002_schema_extensions.py)
</objective>

<execution_context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\05-schema-extensions\05-RESEARCH.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\phases\05-schema-extensions\05-PATTERNS.md
</execution_context>

<context>
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\ROADMAP.md
@E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB\.planning\STATE.md

<interfaces>
<!-- ORM 파일 현재 구조 — 읽기 후 이 내용을 기반으로 컬럼 추가 -->

From src/scenario_db/db/models/capability.py (IpCatalog):
```python
class IpCatalog(Base):
    __tablename__ = "ip_catalog"
    id             = Column(Text, primary_key=True)
    schema_version = Column(Text, nullable=False)
    category       = Column(Text)
    hierarchy      = Column(JSONB)
    capabilities   = Column(JSONB)
    rtl_version    = Column(Text)
    compatible_soc = Column(JSONB)
    yaml_sha256    = Column(Text, nullable=False)
    # sim_params 없음 — 추가 대상
```

From src/scenario_db/db/models/definition.py (Scenario + ScenarioVariant):
```python
class Scenario(Base):
    __tablename__ = "scenarios"
    id             = Column(Text, primary_key=True)
    schema_version = Column(Text, nullable=False)
    project_ref    = Column(Text, ForeignKey("projects.id"), nullable=False)
    metadata_      = Column("metadata", JSONB, nullable=False)
    pipeline       = Column(JSONB, nullable=False)
    size_profile   = Column(JSONB)
    design_axes    = Column(JSONB)
    yaml_sha256    = Column(Text, nullable=False)
    # sensor 없음 — 추가 대상

class ScenarioVariant(Base):
    __tablename__ = "scenario_variants"
    scenario_id          = Column(Text, ForeignKey("scenarios.id"), primary_key=True)
    id                   = Column(Text, primary_key=True)
    ...
    derived_from_variant = Column(Text)
    # sim_port_config, sim_config 없음 — 추가 대상
```

From src/scenario_db/db/models/evidence.py (Evidence):
```python
class Evidence(Base):
    __tablename__ = "evidence"
    ...
    run_info     = Column(JSONB)   # sim only
    ip_breakdown = Column(JSONB)   # sim only
    provenance   = Column(JSONB)   # meas only
    artifacts    = Column(JSONB)
    # dma_breakdown, timing_breakdown 없음 — 추가 대상
```

From alembic/versions/0001_initial_schema.py (migration 패턴):
```python
revision = "0001"
down_revision = None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ORM 3개 파일에 nullable JSONB 컬럼 추가</name>
  <files>
    src/scenario_db/db/models/capability.py,
    src/scenario_db/db/models/definition.py,
    src/scenario_db/db/models/evidence.py
  </files>

  <read_first>
    - src/scenario_db/db/models/capability.py (전체 — IpCatalog 클래스 구조, imports 확인)
    - src/scenario_db/db/models/definition.py (전체 — Scenario / ScenarioVariant 클래스 구조, Computed 컬럼 유무 확인)
    - src/scenario_db/db/models/evidence.py (전체 — Evidence 클래스 구조, Computed 컬럼 위치 확인)
  </read_first>

  <action>
각 파일을 읽은 후 아래 컬럼을 추가한다.

**capability.py — IpCatalog 클래스:**
`yaml_sha256 = Column(Text, nullable=False)` 라인 아래에 추가:
```python
    sim_params     = Column(JSONB)            # nullable — Phase 5 추가 (SCH-01)
```

**definition.py — Scenario 클래스:**
`yaml_sha256 = Column(Text, nullable=False)` 라인 아래에 추가:
```python
    sensor         = Column(JSONB)            # nullable — Phase 5 추가 (SCH-03)
```

**definition.py — ScenarioVariant 클래스:**
`derived_from_variant = Column(Text)` 라인 아래에 추가:
```python
    sim_port_config = Column(JSONB)           # nullable — Phase 5 추가 (SCH-02)
    sim_config      = Column(JSONB)           # nullable — Phase 5 추가 (SCH-02)
```

**evidence.py — Evidence 클래스:**
`ip_breakdown = Column(JSONB)` 라인 아래에 추가:
```python
    dma_breakdown    = Column(JSONB)          # sim only — Phase 5 추가 (SCH-04)
    timing_breakdown = Column(JSONB)          # sim only — Phase 5 추가 (SCH-04)
```

주의사항:
- `Column(JSONB)` 는 nullable=True가 기본값이므로 `nullable=True` 명시 불필요 (단, 명시해도 무방)
- evidence.py의 `sw_version_hint`, `sweep_value_hint` 같은 Computed 컬럼은 절대 수정 금지
- imports에 JSONB가 이미 있는지 확인 — 없으면 `from sqlalchemy.dialects.postgresql import JSONB` 추가
  </action>

  <verify>
    <automated>cd /d E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB && uv run python -c "from scenario_db.db.models.capability import IpCatalog; from scenario_db.db.models.definition import Scenario, ScenarioVariant; from scenario_db.db.models.evidence import Evidence; print('ORM imports OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "sim_params" src/scenario_db/db/models/capability.py` — "Column(JSONB)" 포함 라인 출력
    - `grep -n "sensor" src/scenario_db/db/models/definition.py` — Scenario 클래스의 "Column(JSONB)" 라인 출력
    - `grep -n "sim_port_config\|sim_config" src/scenario_db/db/models/definition.py` — 2개 라인 출력
    - `grep -n "dma_breakdown\|timing_breakdown" src/scenario_db/db/models/evidence.py` — 2개 라인 출력
    - `grep -n "Computed" src/scenario_db/db/models/evidence.py` — 기존 Computed 컬럼이 존재하면 수정 없이 그대로여야 함
  </acceptance_criteria>

  <done>ORM 3개 파일 수정 완료, Python import 성공, Computed 컬럼 미수정 확인</done>
</task>

<task type="auto">
  <name>Task 2: Alembic migration 0002_schema_extensions.py 수동 작성</name>
  <files>alembic/versions/0002_schema_extensions.py</files>

  <read_first>
    - alembic/versions/0001_initial_schema.py (전체 — 헤더 형식, revision/down_revision 패턴, op.create_table 구조 확인)
    - alembic/env.py (선택적 — migration 환경 설정 확인)
  </read_first>

  <action>
**주의: autogenerate 사용 금지. 아래 내용을 그대로 파일로 작성한다.**

`alembic/versions/0002_schema_extensions.py`:

```python
"""schema extensions for sim/ package

Revision ID: 0002
Revises: 0001
Create Date: 2026-05-10

SCH-01: ip_catalog.sim_params (JSONB nullable)
SCH-02: scenario_variants.sim_port_config, sim_config (JSONB nullable)
SCH-03: scenarios.sensor (JSONB nullable)
SCH-04: evidence.dma_breakdown, timing_breakdown (JSONB nullable)
SCH-05: 이 파일이 alembic upgrade head 대상
"""
from __future__ import annotations

import sqlalchemy as sa
from alembic import op
from sqlalchemy.dialects.postgresql import JSONB

revision = "0002"
down_revision = "0001"
branch_labels = None
depends_on = None


def upgrade() -> None:
    # SCH-01: IpCatalog — sim_params
    op.add_column(
        "ip_catalog",
        sa.Column("sim_params", JSONB, nullable=True),
    )

    # SCH-03: Scenario — sensor
    op.add_column(
        "scenarios",
        sa.Column("sensor", JSONB, nullable=True),
    )

    # SCH-02: ScenarioVariant — sim_port_config, sim_config
    op.add_column(
        "scenario_variants",
        sa.Column("sim_port_config", JSONB, nullable=True),
    )
    op.add_column(
        "scenario_variants",
        sa.Column("sim_config", JSONB, nullable=True),
    )

    # SCH-04: Evidence — dma_breakdown, timing_breakdown
    op.add_column(
        "evidence",
        sa.Column("dma_breakdown", JSONB, nullable=True),
    )
    op.add_column(
        "evidence",
        sa.Column("timing_breakdown", JSONB, nullable=True),
    )


def downgrade() -> None:
    # upgrade()의 역순으로 삭제
    op.drop_column("evidence", "timing_breakdown")
    op.drop_column("evidence", "dma_breakdown")
    op.drop_column("scenario_variants", "sim_config")
    op.drop_column("scenario_variants", "sim_port_config")
    op.drop_column("scenarios", "sensor")
    op.drop_column("ip_catalog", "sim_params")
```

작성 후 컬럼 계수 확인:
- upgrade(): add_column 6개 (`sim_params`, `sensor`, `sim_port_config`, `sim_config`, `dma_breakdown`, `timing_breakdown`)
- downgrade(): drop_column 6개 — 동일 컬럼, 역순
- 합계 일치 확인 필수
  </action>

  <verify>
    <automated>cd /d E:\15_ClaudeCode_IP_Modeling\02_ScenarioDB && uv run python -c "import importlib.util; spec = importlib.util.spec_from_file_location('m', 'alembic/versions/0002_schema_extensions.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print('revision:', m.revision); print('down_revision:', m.down_revision)"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "op.add_column" alembic/versions/0002_schema_extensions.py` — 6 출력
    - `grep -c "op.drop_column" alembic/versions/0002_schema_extensions.py` — 6 출력
    - `grep -n "down_revision" alembic/versions/0002_schema_extensions.py` — "0001" 포함 라인 출력
    - `grep -n "revision = .0002." alembic/versions/0002_schema_extensions.py` — 출력 있음
    - `grep -n "autogenerate\|--autogenerate" alembic/versions/0002_schema_extensions.py` — 출력 없음 (수동 작성 확인)
    - 파일 파싱 성공 (Python import 오류 없음)
  </acceptance_criteria>

  <done>0002_schema_extensions.py 생성 완료, revision="0002" / down_revision="0001", upgrade 6개 add_column / downgrade 6개 drop_column 일치</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration 실행 | 운영 DB에 DDL 변경 — upgrade/downgrade 대칭성 보장 필요 |
| ORM 모델 ↔ Migration 동기화 | ORM 컬럼 선언과 migration DDL이 불일치하면 런타임 오류 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Alembic migration 파일 | mitigate | autogenerate 금지 + 수동 작성, upgrade/downgrade 컬럼 수 일치 검증 |
| T-05-02 | Denial | Evidence.Computed 컬럼 오수정 | mitigate | evidence.py 수정 시 sw_version_hint/sweep_value_hint 절대 건드리지 않음 |
| T-05-03 | Information Disclosure | JSONB 필드에 민감 데이터 | accept | Phase 5는 내부 SoC 설계 파라미터 — 외부 API 신규 노출 없음 |
</threat_model>

<verification>
Wave 1 Plan 05-02 완료 기준:

```
# ORM import 확인
uv run python -c "
from scenario_db.db.models.capability import IpCatalog
from scenario_db.db.models.definition import Scenario, ScenarioVariant
from scenario_db.db.models.evidence import Evidence
# 컬럼 이름 집합 확인
cols = {c.name for c in IpCatalog.__table__.columns}
assert 'sim_params' in cols, f'sim_params missing: {cols}'
cols2 = {c.name for c in Scenario.__table__.columns}
assert 'sensor' in cols2, f'sensor missing: {cols2}'
cols3 = {c.name for c in ScenarioVariant.__table__.columns}
assert 'sim_port_config' in cols3, f'sim_port_config missing: {cols3}'
cols4 = {c.name for c in Evidence.__table__.columns}
assert 'dma_breakdown' in cols4, f'dma_breakdown missing: {cols4}'
print('All ORM columns verified OK')
"
```

```
# Migration 파일 구문 확인
uv run python -c "
import importlib.util
spec = importlib.util.spec_from_file_location('m0002', 'alembic/versions/0002_schema_extensions.py')
m = importlib.util.module_from_spec(spec)
spec.loader.exec_module(m)
assert m.revision == '0002'
assert m.down_revision == '0001'
print('Migration file OK:', m.revision, '->', m.down_revision)
"
```
</verification>

<success_criteria>
- [ ] capability.py IpCatalog에 sim_params Column(JSONB) 추가됨
- [ ] definition.py Scenario에 sensor Column(JSONB) 추가됨
- [ ] definition.py ScenarioVariant에 sim_port_config + sim_config Column(JSONB) 추가됨
- [ ] evidence.py Evidence에 dma_breakdown + timing_breakdown Column(JSONB) 추가됨
- [ ] 0002_schema_extensions.py 존재, revision="0002", down_revision="0001"
- [ ] upgrade() add_column 6개, downgrade() drop_column 6개 — 수 일치
- [ ] ORM 컬럼 선언과 migration 컬럼이 1:1 대응 (이름 일치)
- [ ] Python import 오류 없음
</success_criteria>

<output>
완료 후 `.planning/phases/05-schema-extensions/05-02-SUMMARY.md` 생성:
- 수정된 ORM 파일 3개 목록 + 추가된 컬럼 이름
- 생성된 migration 파일 경로 + revision 체인
- ORM 컬럼 ↔ migration 대응표
</output>
