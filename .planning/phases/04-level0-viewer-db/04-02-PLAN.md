# Phase 4 Plan 2: Dashboard HTTP 연동 — 1_Pipeline_Viewer.py 전면 재작성

**Wave**: 1B
**Blocked on**: none (04-01-PLAN.md와 병렬 실행 가능)
**Requirements**: VIEW-01, VIEW-05
**Estimated tasks**: 2

---

## Goal

`dashboard/pages/1_Pipeline_Viewer.py`를 전면 재작성하여 HTTP API 연동으로 교체한다. `build_sample_level0()` 직접 import를 제거하고, FastAPI 서버에서 scenario/variant 목록과 Level 0 view 데이터를 조회한다. Sidebar에 API URL 설정, Scenario/Variant dropdown, mode selector(architecture|topology), Gate status 토글을 추가한다.

**04-01-PLAN.md와 파일 충돌 없음**: 이 Plan은 `dashboard/` 파일만 수정하고 04-01은 `src/` 파일만 수정한다.

---

## Threat Model

| Boundary | Description |
|----------|-------------|
| Dashboard → FastAPI | 사용자가 입력한 API URL로 HTTP 요청. 내부 도구 — 인증 없음 |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | DoS | requests.get() | mitigate | `timeout=10` 모든 API 호출에 적용 (D-01 제약) |
| T-04-02 | Tampering | sidebar API URL input | accept | 내부 도구, 사용자가 직접 설정 — 인증 불필요 |

---

## Tasks

### Task 1: 1_Pipeline_Viewer.py 전면 재작성

**File**: `dashboard/pages/1_Pipeline_Viewer.py`
**Type**: modify (전면 재작성)
**Depends on**: none

현재 파일에서 `build_sample_level0()` import와 `_load_view()` cache 함수를 제거하고 HTTP API 연동 구조로 교체한다. 파일을 먼저 읽어 기존 CSS 스타일 블록(`.viewer-header`, `.meta-chip` 등)을 보존한다.

**재작성할 구조:**

```python
"""Pipeline Viewer — Level 0 Lane Architecture View (DB-backed, HTTP API).

Run:
  Terminal 1: uv run uvicorn scenario_db.api.app:app --reload
  Terminal 2: uv run --group dashboard streamlit run dashboard/Home.py
"""
from __future__ import annotations

import sys
from pathlib import Path

_root = Path(__file__).resolve().parents[2]
for p in [str(_root / "src"), str(_root), str(_root / "dashboard")]:
    if p not in sys.path:
        sys.path.insert(0, p)

import requests
import streamlit as st

st.set_page_config(
    page_title="Pipeline Viewer — ScenarioDB",
    page_icon="🔬",
    layout="wide",
    initial_sidebar_state="expanded",
)

from scenario_db.api.schemas.view import ViewResponse
from dashboard.components.elk_viewer import render_level0
from dashboard.components.node_detail_panel import render_inspector

# ── CSS (기존 스타일 블록 보존) ───────────────────────────────────────────────
# [기존 st.markdown CSS 블록 그대로 유지]
```

**cache_data 함수 3개 정의:**

```python
@st.cache_data(ttl=60)
def _fetch_scenarios(api_url: str) -> list[dict]:
    """GET /api/v1/scenarios → items 목록."""
    r = requests.get(f"{api_url}/api/v1/scenarios", params={"limit": 100}, timeout=10)
    r.raise_for_status()
    return r.json()["items"]   # PagedResponse.items 필드 확정 (common.py 검증 완료)


@st.cache_data(ttl=60)
def _fetch_variants(api_url: str, scenario_id: str) -> list[dict]:
    """GET /api/v1/scenarios/{id}/variants → items 목록."""
    r = requests.get(
        f"{api_url}/api/v1/scenarios/{scenario_id}/variants",
        params={"limit": 100}, timeout=10
    )
    r.raise_for_status()
    return r.json()["items"]


@st.cache_data(ttl=60)
def _load_view(api_url: str, scenario_id: str, variant_id: str, mode: str) -> ViewResponse:
    """GET /view?level=0&mode=... → ViewResponse."""
    r = requests.get(
        f"{api_url}/api/v1/scenarios/{scenario_id}/variants/{variant_id}/view",
        params={"level": 0, "mode": mode},
        timeout=10,
    )
    r.raise_for_status()
    return ViewResponse.model_validate(r.json())


@st.cache_data(ttl=30)
def _fetch_gate(api_url: str, scenario_id: str, variant_id: str):
    """GET /gate → GateExecutionResult (lazy, toggle ON 시에만 호출)."""
    from scenario_db.gate.models import GateExecutionResult
    r = requests.get(
        f"{api_url}/api/v1/scenarios/{scenario_id}/variants/{variant_id}/gate",
        timeout=10,
    )
    r.raise_for_status()
    return GateExecutionResult.model_validate(r.json())
```

**Sidebar 구성:**

```python
with st.sidebar:
    st.markdown("### ScenarioDB Viewer")

    # API URL 설정 (D-02)
    st.markdown("**API Server**")
    api_url = st.text_input(
        "Base URL",
        value=st.session_state.get("api_url", "http://localhost:8000"),
        label_visibility="collapsed",
    )
    if api_url != st.session_state.get("api_url"):
        st.session_state["api_url"] = api_url
        st.cache_data.clear()
        st.rerun()
    st.session_state["api_url"] = api_url

    st.divider()

    # Scenario dropdown (D-03)
    st.markdown("**Scenario**")
    try:
        scenarios = _fetch_scenarios(api_url)
    except Exception as e:
        st.error(f"API 연결 실패: {e}")
        st.stop()

    # ScenarioResponse.metadata_는 dict → name 추출
    scenario_labels = [
        s.get("metadata_", {}).get("name", s["id"]) for s in scenarios
    ]
    scenario_ids = [s["id"] for s in scenarios]
    sel_scenario_idx = st.selectbox(
        "scenario",
        range(len(scenario_ids)),
        format_func=lambda i: scenario_labels[i],
        label_visibility="collapsed",
    )
    scenario_id = scenario_ids[sel_scenario_idx]

    # Variant dropdown (D-03)
    st.markdown("**Variant**")
    try:
        variants = _fetch_variants(api_url, scenario_id)
    except Exception as e:
        st.error(f"Variant 조회 실패: {e}")
        st.stop()

    variant_ids = [v["id"] for v in variants]
    if not variant_ids:
        st.warning("Variant 없음")
        st.stop()
    variant_id = st.selectbox(
        "variant",
        variant_ids,
        label_visibility="collapsed",
    )

    st.divider()

    # Mode selector (D-05, VIEW-05)
    st.markdown("**View Mode**")
    mode = st.radio(
        "mode",
        ["architecture", "topology"],
        index=0,
        label_visibility="collapsed",
    )

    st.divider()

    # Gate status 토글 (D-06, lazy fetch)
    show_gate = st.toggle("Show Gate Status", value=False)

    st.divider()
    st.markdown("**View Level**")
    st.radio(
        "Level",
        ["0 — Lane View", "1 — IP DAG (Phase C)", "2 — Drill-Down (Phase C)"],
        index=0,
        label_visibility="collapsed",
    )
```

**메인 컨텐츠 영역:**

```python
# ── View 데이터 로드 ─────────────────────────────────────────────────────────
try:
    view = _load_view(api_url, scenario_id, variant_id, mode)
except requests.HTTPError as e:
    if e.response is not None and e.response.status_code == 501:
        st.warning("Topology mode는 Wave 2에서 구현됩니다 (04-PLAN-03)")
        st.stop()
    st.error(f"View 로드 실패: {e}")
    st.stop()
except Exception as e:
    st.error(f"API 오류: {e}")
    st.stop()

s = view.summary

# ── Gate 데이터 (toggle ON 시에만 lazy fetch) (D-06) ─────────────────────────
gate_result = None
if show_gate:
    try:
        gate_result = _fetch_gate(api_url, scenario_id, variant_id)
    except Exception as e:
        st.sidebar.warning(f"Gate 조회 실패: {e}")

# ── Header (기존 스타일 유지) ────────────────────────────────────────────────
over_budget = s.period_ms > s.budget_ms if s.budget_ms else False
st.markdown(f"""
<div class="viewer-header">
  <span class="viewer-title">{s.name}</span>
  <span class="meta-chip">{mode} mode</span>
  ...
</div>
""", unsafe_allow_html=True)

# ── Mode별 visible_layers 설정 (VIEW-02/03) ──────────────────────────────────
ARCH_LAYERS = ["hw", "memory"]
TOPO_LAYERS = ["app", "framework", "hal", "kernel", "hw"]
visible_layers = ARCH_LAYERS if mode == "architecture" else TOPO_LAYERS

# ── Main 레이아웃 ─────────────────────────────────────────────────────────────
main_col, inspector_col = st.columns([3, 1], gap="small")

with main_col:
    render_level0(
        view_response=view,
        visible_layers=visible_layers,
        canvas_height=660,
    )

with inspector_col:
    render_inspector(view)
    # Gate 패널은 Wave 2 (04-PLAN-03)에서 render_gate_inspector() 추가

# ── Sidebar 하단 통계 ────────────────────────────────────────────────────────
with st.sidebar:
    st.caption(f"Nodes: {len(view.nodes)} | Edges: {len(view.edges)}")
    st.caption(f"Risks: {len(view.risks)}")
    if gate_result:
        st.caption(f"Gate: {gate_result.status}")
```

Key implementation notes:
- `requests` 모듈: 프로젝트에 이미 설치됨 (pyproject.toml에 없으면 `uv add requests` 필요 — 실행 시 확인)
- `@st.cache_data` 함수 내에서 `requests.Session()` 사용 금지 (pickle 실패). `requests.get()` 직접 호출만 허용
- `scenario_labels`의 `metadata_` 필드: `ScenarioResponse.metadata_` (API response에서 `metadata_` 키로 직렬화됨 — Pydantic alias 확인 필요)
- topology mode에서 API가 501을 반환할 수 있음 (Wave 2 전까지 NotImplementedError) → HTTPError 핸들러에서 501 체크
- `st.cache_data.clear()` 호출 후 `st.rerun()`으로 캐시 무효화

Acceptance criteria:
- [ ] `build_sample_level0` import가 파일에 존재하지 않는다
- [ ] sidebar에 API URL text_input, Scenario selectbox, Variant selectbox, mode radio, Gate toggle이 모두 존재한다
- [ ] `@st.cache_data(ttl=60)` 데코레이터가 `api_url, scenario_id, variant_id, mode`를 인수로 포함한다
- [ ] `requests.get()` 호출에 `timeout=10`이 모두 적용되어 있다
- [ ] topology mode 선택 시 `visible_layers = ["app", "framework", "hal", "kernel", "hw"]`가 전달된다
- [ ] Gate toggle이 OFF일 때 `/gate` API를 호출하지 않는다 (lazy fetch)

---

### Task 2: Dashboard 동작 검증 (아키텍처 mode)

**File**: (파일 없음 — 실행 검증)
**Type**: verify
**Depends on**: Task 1

FastAPI 서버와 Streamlit을 실행하여 architecture mode가 정상 동작하는지 확인한다.

**검증 절차:**

1. FastAPI 서버 실행 상태 확인 또는 시작:
   ```bash
   # 백그라운드에서 실행 중인지 확인
   uv run uvicorn scenario_db.api.app:app --port 8000 --reload
   ```

2. Streamlit 문법/import 오류 확인:
   ```bash
   uv run python -c "
   import sys
   sys.path.insert(0, 'src')
   sys.path.insert(0, '.')
   # 신규 imports 확인
   import requests
   from scenario_db.api.schemas.view import ViewResponse
   from dashboard.components.elk_viewer import render_level0
   print('imports OK')
   "
   ```

3. API 엔드포인트 직접 확인 (서버 실행 중인 경우):
   ```bash
   uv run python -c "
   import requests
   r = requests.get('http://localhost:8000/api/v1/scenarios', params={'limit': 5}, timeout=5)
   print(r.status_code, r.json().get('items', [])[:1])
   "
   ```

4. `requests` 패키지 설치 확인:
   ```bash
   uv run python -c "import requests; print(requests.__version__)"
   ```
   설치되지 않은 경우: `uv add requests`

5. Streamlit 실행 방법 확인 (실행 자체는 수동):
   ```bash
   # 실행 명령 제공 (executor는 명령만 출력, 실제 UI 확인은 사용자)
   echo "Streamlit 실행: uv run --group dashboard streamlit run dashboard/Home.py"
   ```

Key implementation notes:
- Streamlit 앱을 실제로 headless로 실행하여 오류를 잡는 것은 어렵다 — Python import 검증 + API 연결 테스트로 대체
- `requests` 패키지가 pyproject.toml의 어떤 group에도 없으면 `uv add requests` 실행 (dashboard group에 추가: `uv add --group dashboard requests`)
- `ScenarioResponse.metadata_` 직렬화 키 확인: API response에서 실제 키 이름 확인 필요 (`metadata_` vs `metadata`)

Acceptance criteria:
- [ ] `uv run python -c "import requests; from scenario_db.api.schemas.view import ViewResponse"` 오류 없이 실행
- [ ] `requests` 패키지가 설치되어 있다
- [ ] Streamlit 앱 실행 명령이 출력된다
- [ ] API 서버가 실행 중인 경우 `/api/v1/scenarios` 응답 확인 (선택적)

---

## Test Commands

```bash
# Import 검증
uv run python -c "
import sys; sys.path.insert(0,'src'); sys.path.insert(0,'.')
import requests
from scenario_db.api.schemas.view import ViewResponse
from dashboard.components.elk_viewer import render_level0
print('All imports OK')
"

# requests 패키지 확인
uv run python -c "import requests; print(requests.__version__)"

# 전체 단위 테스트 (04-01 완료 전제)
uv run pytest tests/unit/ -x -q
```

---

## Definition of Done

- [ ] `1_Pipeline_Viewer.py`에서 `build_sample_level0` import가 완전히 제거되었다 (VIEW-01)
- [ ] HTTP API 연동 4개 함수(`_fetch_scenarios`, `_fetch_variants`, `_load_view`, `_fetch_gate`)가 구현되어 있다
- [ ] sidebar에 API URL / Scenario / Variant / mode / Gate toggle이 모두 존재한다 (D-02, D-03, VIEW-05)
- [ ] mode별 `visible_layers`가 `render_level0()`에 전달된다 (VIEW-02)
- [ ] `requests.get()` 호출 전부에 `timeout=10`이 적용되어 있다
- [ ] `requests` 패키지가 설치되어 있다
- [ ] Python import 오류 없이 파일이 로드된다
